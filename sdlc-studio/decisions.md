# Project Decisions Log

The canonical, append-only home for load-bearing decisions every later artifact and
delegated agent inherits - both **product** decisions (scope cuts, the answers to the
PRD's open questions) and **implementation conventions** (error-envelope shape, ID scheme,
token strategy, migration style, test harness). One record, two views: an open question
lives in `PRD §Open Questions`; when resolved it is promoted here with a back-link, never
duplicated as free text in both. This block is injected into the handoff context delegated
agents read, so a decision is referenced once, not pasted N times.

## Decisions

| ID | Decision | Rationale | Status | Supersedes | Date |
| --- | --- | --- | --- | --- | --- |
| D0001 | npm scope and package names are `@luzentialabs/clara-tokens`, `@luzentialabs/clara-icons`, `@luzentialabs/clara-react`. The CSS custom property prefix stays `--clara-`. | A company scope rather than a common-noun one. Registry search returns zero packages and zero text matches for "luzentia"; `@clara` could not be confirmed free and short common-word scopes are frequently claimed-but-empty. The scope names the publisher, the prefix names the design system. Package names are permanent once published. Promoted from PRD Open Questions. | accepted | -- | 2026-08-21 |
| D0002 | License is MIT. | Standard for design systems (Radix, Chakra, Mantine). Maximum reuse, usable in client work, no adoption friction. Promoted from PRD Open Questions. | accepted | -- | 2026-08-21 |
| D0003 | Behavioural primitive layer is Radix UI, paired with a primitive-isolation rule (PRD Section 4 rule 7). | Mature, proven, largest community; for a solo maintainer proven-and-stable beats actively-churning. The isolation rule keeps `asChild`, `onOpenChange`, and `data-state` out of Clara's public API, so the dependency stays reversible instead of becoming permanent surface. Promoted from PRD Open Questions. | accepted | -- | 2026-08-21 |
| D0004 | Clara's visual identity is decided in F00, a time-boxed foundations pass of 5 working days. Component work begins on day 6 regardless of whether the pass is satisfied. | The ux seat's documented failure mode is refining foundations indefinitely, and that seat explicitly asked to be capped rather than trusted. A deadline held by the document is the cap. F00 blocks F01 and therefore every component. Promoted from PRD Open Questions. | accepted | -- | 2026-08-21 |
| D0005 | All Clara CSS is emitted inside named cascade layers, declared `@layer clara.reset, clara.tokens, clara.components;`. | Unlayered CSS wins over layered CSS regardless of specificity, and a consuming app's stylesheets are unlayered by default, so any consumer rule beats any Clara rule with no `!important` and no specificity contest. This is what makes the Section 4 className contract true. It cannot be retrofitted: introducing layers after v1.0 would silently change the resolved styles of every consumer override already shipped. | accepted | -- | 2026-08-21 |
| D0006 | CSS delivery is one stylesheet per package, deliberately not tree-shaken. The `exports` map enumerates its subpaths and contains no `./*` wildcard. | Simple and robust across bundlers. Forced the v0.1.0 per-component CSS budget to be restated honestly as a fixed stylesheet ceiling (15KB gzipped) plus JS-only per-component budgets, because under a single-stylesheet model no component has a separable CSS cost. The delivery model and every reachable subpath live in the exports map and are permanent once published. | accepted | -- | 2026-08-21 |
| D0007 | Tier 2 (semantic) tokens are public API covered by the breaking-change rule. Tier 1 (primitive) and tier 3 (component) tokens are private and may change in a minor. | Without a declared boundary the distinction is an honour system and consumers settle it for us by overriding tier 3. Enforced mechanically via a generated `tokens.public.json`, with CI failing if the docs site or a published example references a token outside it. | accepted | -- | 2026-08-21 |
| D0008 | `as` is Clara's single polymorphism and composition idiom, used on layout primitives, Button, and overlay triggers. `asChild` is not Clara API. | v0.1.0 carried three idioms answering one question (`asChild` for overlay triggers, `as` for layout primitives, `href` for Button), which broke PRD design principle 2 - "guessable by someone who has used another Clara component" - on paper before any code existed. | accepted | -- | 2026-08-21 |
| D0009 | Package builds use Vite library mode with `vite-plugin-dts`. | Native CSS Modules handling is decisive given the single-stylesheet delivery model (D0006); tsup and unbuild have weak or experimental CSS support and Clara's styling layer is CSS Modules throughout. Shares one tool with Storybook and the reference app. Recorded as TRD ADR-007. | accepted | -- | 2026-08-21 |
| D0010 | Monorepo orchestration is plain pnpm workspaces with scripts. Turborepo is deferred until CI build time is a measured cost. | Three packages and two apps do not yet need a task graph, and no build exists to be slow. Adding Turborepo later is cheap because it is build tooling rather than public API, so the decision stays reversible - unlike the decisions being made alongside it. Recorded as TRD ADR-009. | accepted | -- | 2026-08-21 |
| D0011 | Calendar math for F12 uses `@internationalized/date`. Clara's public props accept and return ISO date strings, not library types. | F12 must document timezone behaviour precisely and operates on calendar dates rather than instants; `date-fns` operates on JS Date, which conflates the two, so it would paper over the exact ambiguity F12 has to resolve. The string boundary keeps the dependency out of Clara's permanent surface. Closes a PRD open question. Recorded as TRD ADR-008. | accepted | -- | 2026-08-21 |
| D0012 | A generated API surface report (`api-extractor` `.api.md` per package) is committed, and CI fails the build on an uncommitted change. | The declared risk class is irreversible distribution; without a surface diff an accidental public API change is discovered by a consumer on install rather than in review. Also enforces the Radix isolation boundary mechanically. The raising seat flagged this as possibly a personal working method promoted to a project requirement; adopted deliberately on the operator's decision. Recorded as TRD ADR-010. | accepted | -- | 2026-08-21 |
| D0013 | Visual regression tooling is Chromatic, consuming the existing Storybook stories. | Built by the Storybook team, so with the PRD's requirement that every component has stories for every state the baseline matrix comes nearly free with no separate driving code. The review interface is the deciding factor at 100+ baselines (components x 2 themes x 2 densities): bulk-approving diffs without looking is the exact failure this gate exists to prevent. Accepts an external service dependency. Closes a PRD open question. | accepted | -- | 2026-08-21 |
| D0014 | Coverage gates are 90% statements AND 85% branches, both blocking. | PRD F22 put its only hard number on statement coverage, which is the metric that can rise while assertions weaken. Branches are where error paths and conditional rendering live, which is where component libraries actually leak, and branch coverage is harder to satisfy vacuously. | accepted | -- | 2026-08-21 |
| D0015 | Mutation testing uses Stryker Mutator over changed files only, with a blocking 70% mutation score. | PRD F22 required a mutation check but named no tool, scope, or threshold and did not say whether it blocked, so the strongest quality signal had no teeth. Changed-file scope keeps the PR path fast; 70% is achievable from day one so the gate is real rather than aspirational, and ratchets as the suite matures. The risk that a threshold gets chosen to be passable rather than meaningful is recorded in the TSD's Known Gaps rather than treated as solved. | accepted | -- | 2026-08-21 |
| D0016 | Manual screen reader verification is VoiceOver/Safari only. NVDA is not performed and is recorded as a stated known gap in the public accessibility statement. | The QA seat scoped NVDA/Firefox for breadth, but NVDA is Windows-only and the maintainer works on macOS, so that half needs a VM or a paid service the project does not have. NVDA and VoiceOver differ in real ways, so this is a genuine gap. Naming it explicitly is the honest option; a blanket WCAG 2.2 AA claim while silently testing one screen reader is exactly the failure the QA seat exists to prevent. Manual keyboard verification remains mandatory for every exported component. | accepted | -- | 2026-08-21 |
| D0017 | `createTheme()` brand ramp generation is deferred to v1.1. | All four seats agreed it was over-promised and proposed four different remedies, which is itself a signal the feature is not well understood. No application needs a brand other than Clara's default; the only consumer today is the maintainer. Deferring dissolves the build-time-vs-runtime question and shrinks the contrast test to the shipped palette. Revival condition: an application requires a non-default brand. Note the amber problem survives independently - `bg-warning-emphasis` is hard in Clara's own palette and is decided in F00. | accepted | -- | 2026-08-21 |
| D0018 | F12 ships DatePicker and DateRangePicker in v1.0; TimePicker moves to v1.1. | Applying the consuming-need test per component rather than to the feature as a whole. Date ranges are common in ERP (reporting periods, filter bars) and F31's list screen consumes one. TimePicker has no named consumer in either reference screen and carries its own 12h/24h, locale, and keyboard model. Revival condition for TimePicker: a screen requires time-of-day entry. | accepted | -- | 2026-08-21 |
| D0019 | Client-side virtualization is cut from F11. Large option sets are served by async option loading, with a documented client-side option-count ceiling. | F15 defers DataGrid on the reasoning that building it badly early is worse than building it properly once an application defines the requirements; F11's 'virtualize 100+ options without breaking aria-activedescendant' is the same class of work in a v1.0 row, so the same reasoning applies. The real ERP answer to a 2,000-account picker is server-side search, which async loading already covers. Also removes the accessibility risk most likely to be subtly broken while passing every automated check. | accepted | -- | 2026-08-21 |
| D0020 | F16 `Menu` (navigation) is deferred to v1.1 alongside F25 AppShell. `DropdownMenu` (actions) remains in v1.0. | DropdownMenu covers what both reference screens need. Navigation Menu belongs with the app shell where navigation actually lives; building it first means guessing at its API, and the two were distinguished only in documentation, which is where consumers pick wrong. | accepted | -- | 2026-08-21 |
| D0021 | A change to a tier 2 token's VALUE (not its name) is a minor release, batched into designated releases and accompanied by a visual changelog entry. A value change that breaks a documented contrast pairing is a bug, not a release. | Literally a value change alters every consuming app's appearance at once, which argues for major; but requiring a major for every neutral-ramp tweak makes the system unusable and no real design system does it. Batching plus a visual changelog gives consumers the warning they need without freezing the palette. Resolves a question that was open in the PRD, the TRD checklist, and the Foundations epic simultaneously. | accepted | -- | 2026-08-21 |
| D0022 | Disabled buttons and controls use `aria-disabled` and REMAIN FOCUSABLE. Native `disabled` is not used where an explanation matters. | Resolves a direct contradiction: F07 permitted a non-focusable disabled button paired with a tooltip, while F13 requires Tooltip to appear on keyboard focus - so the explanation was unreachable by exactly the users who need it. Keeping the control in the tab order means it announces as disabled and its explanation is reachable. In enterprise forms the reason a control is unavailable is often the most important thing on screen. | accepted | -- | 2026-08-21 |
| D0023 | Story granularity is one story per component. | The definition of done is per-component (stories, tests, axe assertion, visual baseline, keyboard pass, docs page, mutation score). A story covering three components carries three times that bar and can never be partially done, so progress would be invisible for long stretches. One story per component makes the DoD and the story boundary the same thing. | accepted | -- | 2026-08-21 |
| D0024 | TDD is used for any component with a documented keyboard interaction table; presentational components are test-after. | Project-specific rule that fits Clara better than the shipped edge-case-count heuristic: the keyboard interaction table IS the specification, so writing its tests before the implementation is natural rather than ceremonial. The boundary is structural and checkable (does this component have a keyboard table?) rather than an estimate made during grooming. | accepted | -- | 2026-08-21 |
| D0025 | v1.0 requires: every must-have feature row Complete, the reference application built on the PUBLISHED package, and the API surface report stable across two consecutive releases with no change. The 1.x support window after 2.0 ships is 6 months, critical and security fixes only. | The PRD said pre-1.0 was unstable but never said what made it 1.0, so 1.0 would have been declared by feel. API stability across two releases is the criterion that actually proves the surface has settled, which is the whole meaning of 1.0 for a library. The support window is stated because consumers running three applications need to know where they stand before they upgrade. | accepted | -- | 2026-08-21 |
| D0026 | F27-F30 (command palette, filter builder, charts, Tailwind preset) move out of the Feature Inventory into a Future Considerations section, each with a revival condition. | A Nice-to-have with no version and no revival condition is a component waiting for someone to feel like building it, and it makes the inventory read as a list of commitments when it is not. Moving them preserves the reasoning without the rows implying a plan. | accepted | -- | 2026-08-21 |
| D0027 | Docs site and Storybook are hosted on GitHub Pages. | Keeps everything on one platform with no additional account or service, and is trivially movable later since static hosting is not public API. Preview deploys per PR would be nicer on Vercel; not worth an external dependency at this stage. | accepted | -- | 2026-08-21 |
| D0028 | `clara-tokens` is built with Style Dictionary plus `tsc`, not a Vite build. | The package emits no components and no CSS Modules, so the reason Vite was chosen for the other two packages does not apply. A simpler pipeline means less build surface to maintain. `clara-icons` and `clara-react` remain on Vite library mode (D0009). | accepted | -- | 2026-08-21 |
| D0029 | tokens.pairings.json is NOT part of the published surface of @luzentialabs/clara-tokens. It is build-time input to the contrast gate, read from inside the repository. The TRD Section 5 published-surface table is corrected to match the closed exports table, which already omitted it. | The TRD contradicted itself: Section 5 listed tokens.pairings.json in the published surface while the closed exports table in the same section omitted it, so it was promised and unreachable at once (CR-01M0HTB4, raised by the anton-reis seat in the US-01M0GMPJ delivery review). Both sides could not stand. Resolved toward NOT publishing because publishing is a one-way door: a subpath can always be added later, never removed. The delivered manifests already follow the closed table, so this direction requires no code change. Nothing is broken today - the contrast test reads the file inside the repo. The revival condition is a named consumer that needs the pairing table at runtime; adding the subpath then is a minor release, removing it later would be a major one. | accepted | -- | 2026-08-21 |
| D0030 | The attw gate (TRD Section 9 gate 10) runs with --profile node16 and --exclude-entrypoints for CSS subpaths. It does NOT use --ignore-rules. | attw reports a resolution failure for every non-JavaScript subpath, because a CSS file has no type declarations under any profile. Every Clara package publishes CSS, so an unconfigured attw can never exit 0 and the gate would be permanently red - which in practice means permanently ignored. Two ways to silence it, and they are not equivalent: --ignore-rules no-resolution suppresses the rule globally, which would also hide a genuinely broken JavaScript entrypoint, exactly the defect the gate exists to catch. --exclude-entrypoints names the specific asset subpaths and leaves every rule armed for the JavaScript entries. The narrower tool is the right one. --profile node16 is chosen because the packages declare engines.node >= 20 and ship an exports map only; node10 resolution describes TypeScript's pre-exports-map algorithm, which no supported consumer uses. Recorded because it configures a blocking gate: a later change to a --ignore-rules form would weaken the gate silently. | superseded | -- | 2026-08-21 |
| D0031 | attw's --exclude-entrypoints is VARIADIC (space-separated), not comma-separated, and the canonical invocation for all three packages is `pnpm check:attw`. Amends D0030. | D0030 recorded the flag names but not their argument form. An independent reviewer transcribed it the natural way - `--exclude-entrypoints ./tokens.css,./themes/dark.css` - and got exit 1, because attw read the whole comma string as one entrypoint name and the real ones were never excluded. Only the space-separated form exits 0. Whoever wired TRD gate 10 from D0030 alone would have produced a permanently red gate, which D0030 itself says 'in practice means permanently ignored'. The invocation is now a script (`check:attw`) rather than prose in a decision, so there is one copy and it is executable. Same for `check:publint`. Recorded because a gate configuration that only works when transcribed exactly is a gate waiting to be mis-transcribed. | accepted | D0030 | 2026-08-21 |
| D0032 | The axe assertion is built directly on axe-core in test/axe.ts, not on vitest-axe as the TSD specifies. The TSD is amended to match. | vitest-axe has been at 0.1.0 since 2025-01-22, roughly 19 months. It declares peerDependencies { vitest: '>=0.16.0' } with no upper bound, so it claims to support Vitest 4 only by omission rather than by testing. It pulls in six transitive dependencies - chalk, redent, lodash-es, aria-query, dom-accessibility-api - to wrap a library Clara depends on directly anyway. Anton's non-negotiable is that every dependency carries a written justification weighed against its cost; an unmaintained wrapper over a maintained core does not have one. It also sits in the accessibility path, which AGENTS.md deliberately splits across two seats (Idris decides inclusive design, Mira proves it) precisely because a11y coverage is easy to fake - an unmaintained assertion layer is the wrong place to save ten lines. The replacement is about thirty lines calling axe.run and filtering to serious and critical, the blocking severities the TSD already names, and its ability to FAIL is asserted by a test rather than assumed. color-contrast is disabled in jsdom because jsdom cannot compute layout; contrast is proven against real token values by the contrast gate in US-01M0GM66, so it is covered rather than dropped. | accepted | -- | 2026-08-21 |
| D0033 | Test toolchain versions are pinned to the newest line compatible with the project's Node 20.19 floor, not to latest. jsdom ^29 (not 30), @stryker-mutator/core ^9.2 (not 10), size-limit ^12 (not 13). | engines.node is >=20.19.0 and CI runs Node 20 LTS. Three tools shipped a major that raised their own floor above that: jsdom 30 requires ^22.22.2 \|\| ^24.15.0 \|\| >=26, Stryker 10 requires >=22.0.0, size-limit 13 requires ^22.18.0 \|\| ^24 \|\| >=26. Each was installed at latest first and failed loudly - jsdom with 'webidl.util.markAsUncloneable is not a function' at worker start, Stryker with an explicit engine guard, so nothing was discovered by guesswork. Taking latest would have meant either a broken local test run or silently raising Clara's Node floor, which is a consumer-visible change that belongs in a decision rather than in a lockfile. The pins are therefore deliberate and each has a documented reason to be revisited: when the project raises its Node floor, these three can move together. Recorded because the natural maintenance reflex - 'upgrade everything to latest' - will break Node 20 CI, and the next person needs to know the constraint was known rather than missed. | accepted | -- | 2026-08-21 |
| D0034 | D0015's "changed files only" mutation scope is NOT in force and never was. `since` is not a Stryker option. The gate runs over the full mutate glob until an alternative is implemented. | stryker.conf.json carried `since: { ignoreStaticChanges: true }` and US-01M0GM3X AC3 asserted "scope is changed files". Stryker 9.6.1 rejects the key outright - it warns `Unknown stryker config option "since"` and its schema has no such property (only ignorePatterns, ignoreStatic, incremental, incrementalFile, ignorers). The scope half of that AC was therefore false from the day it was written, and the guard asserting `conf.since` proved only that a string had been typed - the exact defect the guard's own docblock says it exists to replace. Found by the anton-reis seat in round 5. D0015's REASON for changed-file scope stands: keeping the PR path fast. The mechanism does not. Recorded rather than silently corrected, because a threshold whose scope nobody can state is the aspirational gate D0015 was written to prevent. Revisit when the mutate surface grows enough that full-glob runs are slow: `incremental` with a committed incrementalFile is the likely replacement, and it is a real option. | accepted | -- | 2026-08-22 |
| D0035 | F00's timebox-expiry rule. (1) An undecided deliverable on day 5 takes a stated fallback and lands as a REAL tier 1 token marked Provisional with a revisit condition - it ships, and F01 is unblocked. (2) A contrast pairing failing its threshold NEVER ships provisional; the palette changes instead, and F00 does not close on a failing table. (3) The CALENDAR declares the box expired - five working days, no operator judgment call. (4) Day 6 starts the F01 token chain only (US-01M0GME0, US-01M0GMAE, US-01M0GM66, US-01M0GM5M); actual components wait for the token layer they consume. | D0004 fixed a five-day box but never said what day 6 does with an unfinished deliverable, so 'component work begins regardless' had no mechanism and would have degraded to whatever was in the file. Operator interviewed 2026-08-22; all four clauses are their answers, not inferences. Clause 1 makes the box real - a provisional value is still a value, so F01 is never blocked by an aesthetic question. Clause 2 draws the one line that does not move: a failing pairing is inaccessible rather than unfinished, and Grace Adeyemi (red-green CVD, scanning by peripheral vision at 4pm) is the person the floors exist for; the accent hue is the cheapest lever, so it moves. Clause 3 keeps D0004's own logic intact - 'a deadline held by the document is the cap' - because an operator judgment call on day 5 reintroduces exactly the pressure the ux seat asked to be protected from, having named indefinite foundations refinement as its own shadow. Clause 4 prevents a component being built on provisional tokens that then move; it also leaves room for US-01M0GM16 (cascade layers) to land before any component stylesheet exists, which AGENTS.md says can never be retrofitted. | accepted | -- | 2026-08-22 |
| D0036 | F00 visual direction, operator sign-off 2026-08-22, taking the Idris (ux seat) leanings in full: (1) NEUTRAL RAMP is a slightly warm neutral, 2-4% warm at the mid steps. (2) ACCENT HUE is indigo, approximately 275deg. (3) RAMP GENERATION COLOUR SPACE is OKLCH, compiled to hex at build time. (4) RADIUS CHARACTER is subtle, 4px base. (5) THE HARD CASE resolves as option A: `fg-on-emphasis` becomes PER-INTENT rather than a single token - white on accent/danger/success/info, near-black on warning. | Decided for two named people. Grace Adeyemi processes 200-400 invoice lines a day, works from peripheral vision by 4pm, and has an undisclosed mild red-green colour vision deficiency; Sofia Marchetti needs the 26th component predictable from the 25th. (1) A slightly warm neutral is the only temperature that serves an 8-hour reading day without spending the accent axis - cool greys compete with a cool accent, true neutral invites the genericness the ux seat itself lists under Pushes Back. (2) Indigo is far from both the success and danger axes, so it stays distinguishable for Grace, and it has contrast headroom: measured at decision time as #4640c4 carrying white at 7.52:1 - note that value is NOT in the shipped ramp; after the gamut correction (review X11) accent.700 is #4046b3, and the headroom claim still holds. Green and red/orange were refused outright - they collide with status intents AND sit on Grace's confusable axis. (3) OKLCH's lightness is perceptually uniform, so an 11-step ramp steps evenly and the dark theme becomes derivable rather than a second hand-tuned design pass; browser support is irrelevant because the build emits hex. (4) Rounded corners become chrome at 200 rows, and the ux seat's craft goal is density achieved by removing chrome, never by crowding - so subtle wins on the seat's own test. (5) Measured during the F00 contrast work: NO amber carries both white and near-black at 4.5:1 - the curves cross at #b26a00 (4.24 white, 3.91 near-black) without ever overlapping, which is sharper than PRD:1207's wording. Darkening warning to #9a5c00 makes white work but the colour reads brown, and a brown warning chip is a warning Grace does not see at 4pm. Per-intent fg-on-emphasis costs one tier 2 taxonomy change now against a permanently wrong colour later. This last clause changes what US-01M0GMAE builds and interacts with CR-01M0J0Z6. | accepted | -- | 2026-08-22 |
| D0037 | F00 compact density floors (closes US-01M0GMN0 AC3). Minimum internal padding in compact: 4px vertical, 8px horizontal. Minimum spacing between adjacent interactive targets: 4px in compact, 8px in comfortable. Both are tier 1 values already on the space scale (space.2 = 4px, space.3 = 8px), so no scale change is needed. CONSULTED, NOT OPERATOR-SIGNED - see rationale. | PRD:312 requires both floors and fixes them in F00, and they were the last open F00 deliverable. Derived rather than chosen: the PRD sets compact control height at 32px and body text at 14px in both modes, so a 24px line box leaves (32-24)/2 = 4px vertical padding, which lands on the existing scale. Horizontal takes the next step up at 8px, because 4px puts a label visually against a control edge. For adjacent targets, the PRD's own argument is that two 24x24 targets touching satisfies the letter of the target rule and is still crowding; a 4px gap gives a 28px pitch, which clears WCAG 2.2 SC 2.5.8's spacing-based exception and is enough for the eye to separate two chips at a glance. Comfortable doubles it to 8px. Consulted through the Idris (ux) seat, whose non-negotiables bound the answer - targets never below 24x24, body never below 14px, and density achieved by removing chrome rather than crowding content - under the operator's standing instruction to consult personas rather than block. It is recorded as consulted rather than signed off, so it is reversible: unlike a tier 2 name, a spacing floor is not permanent at publish, and US-01M0GMC6 will exercise it against real components. | accepted | -- | 2026-08-22 |
| D0038 | ci.yml enumerates all fourteen TRD gates. The seven with a runnable command execute and block; the seven without are listed in a committed gates manifest naming the OPEN story that lands each, and a guard fails if a pending gate names no open story or if a wired gate is absent from the workflow. | Seven of the fourteen gates the TRD names have no runnable command yet: the token-tier lint rules (US-01M0GME0), axe, keyboard, and computed geometry (all need components), visual regression (no tool chosen), and consumer verification (US-01M0GMDV). Leaving them out of ci.yml entirely would make a fourteen-gate contract look like a seven-gate one, and the TSD is explicit that it 'defines no gate without an enforcement point'. Listing them without a mechanism would be the reports-without-blocking antipattern the same document forbids. So the manifest is the enforcement point for the gap itself: a pending gate must name an open story, which means it cannot be quietly forgotten, and it can never read as passing. This is the same construction already used for the 27 waived contrast pairings and the coverage-scope distinction - enumerate the gap, bind it to the story that closes it, and let the count only shrink. Decided by the agent under the operator's standing instruction to work autonomously and consult rather than block; it is reversible and costs one file. | accepted | -- | 2026-08-22 |
| D0039 | Extends D0033. `@changesets/cli` is pinned to ^2.29, not 3.x, for the same reason as jsdom, Stryker and size-limit: 3.0.1 declares `engines.node: ^22.11 \|\| ^24 \|\| >=26`, above this project's 20.19 floor. | Fourth tool in this toolchain whose current major raised its own Node floor above the one Clara's packages publish. It failed loudly rather than subtly - `TypeError: enableCompileCache is not a function`, a Node 22 API called at module load - so nothing was discovered by guesswork. The pattern is now established enough to state as a rule rather than a series of one-offs: when a dev tool's latest requires a Node newer than `engines.node` in the published manifests, pin below it and record why. Raising Clara's floor instead is a consumer-visible change and belongs in its own decision, taken deliberately, not as a side effect of wanting a newer devDependency. The four pins move together when that decision is taken. | accepted | -- | 2026-08-22 |
| D0040 | Tarball install-safety is gated, not assumed. check-pack.mjs packs every publishable package the way the release publishes it and fails on any workspace:/link:/file: range surviving into dependencies, peerDependencies, or optionalDependencies, and on any exports target absent from the tarball. | npm pack ships `workspace:*` verbatim; only pnpm rewrites it to a real version. Our release path uses changesets (pnpm publish), so we are correct today by accident of tool choice rather than by assertion - switching to `npm publish` would be a one-line change that breaks every consumer install with EUNSUPPORTEDPROTOCOL, permanently, because a release can be superseded but never withdrawn. Found while descoping US-01M0GMDV: the consumer apps could not run yet, but the packaging question they exist to answer could be answered now. Inspection rules live in scripts/lib/pack-inspect.mjs as a pure function with 11 unit tests, because packing a deliberately-broken package would require a deliberately-broken workspace. | accepted | -- | 2026-08-22 |
| D0041 | CR-01M0MK20 is resolved with Option A: manual chunks driven by the classification. Rollup `manualChunks` reads client-boundary.json and splits the build into a client chunk and a server chunk; a build step prepends `"use client"` to the client chunk only. The entry re-exports from both and carries no directive, so it stays server-capable and the boundary forms where a consumer imports a client component. Options B (preserveModules) and C (blanket directive) are rejected. | Operator call, 2026-08-22. Option A keeps the output small and enumerable, so finalize-dual's dual-format rename, the bundle-record sha256 pairing, the size budgets, and the api-extractor entry all keep working, and the closed exports map still prevents deep imports. The decisive property is that client-boundary.json becomes load-bearing BUILD INPUT rather than documentation - the list and the shipped output cannot disagree, because the list is what cuts the chunks. B was rejected because one file per source module breaks four parts of the pipeline at once and widens the deep-import surface the closed exports map exists to prevent. C was rejected because TRD Section 7 requires server-capable components to carry no directive, and a blanket directive forces every consumer into a client boundary - the exact outcome PRD F23's user story rules out. Taken before F01 deliberately: the output shape is reachable from the exports map, so changing it after the first publish is a breaking change. | accepted | -- | 2026-08-22 |
| D0042 | BG-01M0MFMZ changes approach: stop asserting over our own build output and verify the property in a real consumer instead. Install the published tarball into a throwaway app and assert React resolves to the consumer's own copy. The work folds into US-01M0GMDV's consumer verification apps when F01 lands. | Operator call, 2026-08-22. The guard has been defeated seven times, each by a different root cause: marker strings React renamed, an output-text regex a license comment satisfied, a source grep defeated by the jsx: react-jsx transform, a filename deciding whether the record was read at all, a relocated test that fabricated records, a derived `inlined` field that shipped 55,955 bytes, and describeArtifacts letting a caller name files to hash while supplying a graph from elsewhere. An independent reviewer concluded another oracle rewrite will not close it. The pattern across all seven is the same: every oracle inspects an artifact we also produce, so each fix moves the trust boundary rather than closing it. A consumer install has no such loop - if React resolves to the consumer's copy, the property holds regardless of how our build reached that state. It also tests what we actually care about rather than a proxy for it. | accepted | -- | 2026-08-22 |
| D0043 | Open Question 2 is closed: Radix UI is Clara's headless primitive layer. Base UI is not adopted. This confirms what TRD ADR-004, D0003 and D0008 already assume, and closes the question Anton required closed before the first component. | Operator call, 2026-08-22. The specs already committed to Radix in three places - ADR-004 names it, D0003 forbids it from the public surface, D0008 makes `as` the single polymorphism idiom precisely so `asChild` never becomes Clara API - and the api-report guard now enforces all of that mechanically. Leaving the question nominally open while every artifact assumed the answer was the worst of both: no evaluation happened, and the decision was being made by default anyway. Closing it makes the assumption explicit and reviewable. The isolation rule Anton asked for is already in force and now machine-checked: no `@radix-ui` type in a public report, and `asChild`, `onOpenChange` and `data-state` are never Clara API. Reversibility: the choice is one-way only once components publish, because the primitive determines overlay behaviour and focus management; it is not encoded in the token layer or the build, so a pre-publish reversal costs component rewrites but no consumer break. | accepted | -- | 2026-08-22 |
| D0044 | CR-01M0J0Z6 resolves in favour of the TRD: tier 2 semantic token names are neutral, accent and selected, plus the four status intents across fg/bg/border, plus fg-readonly and the two focus tokens. The placeholder scheme delivered in US-01M0GM9N (semantic.surface/text/border/action/spacing) is replaced in US-01M0GMAE. | Operator call, 2026-08-22. Tier 2 is public API under D0007, so the names are permanent once published and this had to be settled before the semantic layer is built, not after. The TRD is the specification; the placeholder scheme was an implementation convenience that shipped without being recorded as a deviation - which is why it was filed as a DISCLOSURE failure rather than a scope violation. Choosing the TRD keeps the intent-based vocabulary (neutral/accent/selected + status intents) that the contrast pairings, the focus indicator contract and the fg-on-emphasis per-theme rule are all already written against; the surface/text/border scheme would have required restating those too. Consequence to carry into US-01M0GMAE: tokens.public.lock.json currently locks 15 keys under the placeholder names, so the lock file and the 27 required contrast pairings are rewritten with the token source, in one commit, before anything publishes. | accepted | -- | 2026-08-22 |
| D0045 | Each output format names its chunks with its own extension: ESM emits `<name>.js`, CJS emits `<name>.cjs`, declared as two explicit entries in `rollupOptions.output` rather than one shared block. Declarations are bundled into a single `index.d.ts` via `bundleTypes`. | Found while implementing D0041, and neither half was anticipated by CR-01M0MK20. A single shared `output` block gave both format passes the same `chunkFileNames`, so the CJS pass silently OVERWROTE the ESM chunks: dist/index.js, an ES module, imported a chunk containing `"use strict"` and `require()` calls. Both passes reported success and the build printed no warning - only reading the emitted bytes caught it. The extension has to distinguish the chunks exactly as it already distinguishes the entry, and doing so also makes finalize-dual's rename step a no-op rather than a dependency, since Rollup then emits requires that already resolve. Second half: with per-module declarations the entry .d.ts re-exports `./components/Box/Box` with no extension, which node16 ESM resolution cannot follow - attw reports InternalResolutionError and TRD Section 9 gate 10 fails. Bundling the types fixes that and additionally stops a dist/components/ tree from shipping, which would widen the deep-import surface D0006's closed exports map exists to prevent. | accepted | -- | 2026-08-22 |
| D0046 | The bundle record forgives exactly one post-build transformation: a leading `"use client"` directive. The guard undoes that single edit and requires the result to reproduce the recorded sha256; it never re-derives the record from the final bytes. | finalize-dual stamps the directive after Rollup has hashed the chunk, so an exact match is impossible for that one file. The obvious fix - re-hashing after finalize - would let ANY post-build step launder arbitrary changes through the record, which is precisely the hole the record exists to close and which took seven attempts to close in BG-01M0MFMZ. Undoing the one permitted transformation and comparing keeps the binding tight: a directive prepend is forgiven, and a single appended statement is not. Pinned by a mutation that appends one line to the client chunk and must be rejected. | accepted | -- | 2026-08-22 |
| D0047 | Chunk placement is decided by what a module DEFINES, not by which directory it sits in, and own-source modules that define no component go to a third `clara-shared` chunk that carries no directive. | Both halves fix a Critical found by adversarial review of US-01M0MQYN, and both were confirmed by reproduction. Keying on the directory was wrong in two directions: a client component co-located with a server one - `src/components/Table/TableSortButton.tsx`, exactly the split the classification's own `special.Table` entry describes - chunked as `Table` and shipped with NO directive, crashing the server render of every App Router consumer; and a flat `src/components/Switch.tsx` matched no component directory at all, so it was inlined into the ENTRY, the one file that must stay undirectived. Reading each module's own exported definitions places both correctly and lets co-location work rather than merely detecting it. The shared chunk fixes the second Critical: left unplaced, a module imported by both a client and a server component is folded by Rollup into ONE of the two chunks, chosen by module-graph order - reordering two export lines in src/index.ts moved `cx` into the client chunk, at which point the server chunk imported it and every server-capable component sat behind the client boundary. Under RSC every export of a `use client` module is a client reference, so the server render throws. A third undirectived chunk can be imported by both sides and is owned by neither. check-client-boundary independently asserts placement from the bundle record and rejects any server-or-shared chunk importing the client chunk. | superseded | -- | 2026-08-22 |
| D0048 | Client components get one chunk EACH, not one shared `clara-client` chunk. `manualChunks` names a client component's chunk after the component; server-capable components continue to share `clara-server`, and cross-cutting modules continue to share the undirectived `clara-shared`. | Operator call, 2026-08-23, on review finding F7. `use client` boundaries are module-granular: with one client chunk, a consumer importing only Button takes Dialog, Combobox, DatePicker, DateRangePicker, Drawer and MultiSelect into their client bundle as well. That directly contradicts AGENTS.md's `per-component budgets apply to JavaScript only` - the budgets cannot be meaningful when the unit of delivery is all-or-nothing - so one of the two had to give, and the budget is the thing consumers actually feel. It also puts Clara behind the ecosystem norm: RSC-aware libraries ship per-component modules so consumers pull only what they import. Server components do NOT need the same treatment: they carry no directive, so a server chunk crossing no boundary costs a consumer nothing beyond bytes their bundler can tree-shake. The wiring cost (bundle record, size budgets, the placement guard) is per-component and is paid once; doing it at two components rather than forty is the entire reason to decide now, and the output shape is reachable from the exports map, so it is a breaking change after the first publish. Supersedes the one-client-chunk shape introduced with D0041/D0047, which stands in every other respect. | accepted | -- | 2026-08-23 |
| D0049 | CR-01M0MND5 resolves to a caret: internal workspace dependencies are declared `workspace:^`, which pnpm rewrites to `^<version>` at publish, rather than `workspace:*`, which rewrites to an exact pin. | Operator call, 2026-08-23. `pnpm pack` rewrites `workspace:*` to the EXACT version, so every published clara-react would hard-pin one build of clara-tokens. A consumer who ends up with clara-react@1.2.0 and clara-tokens@1.2.1 then installs BOTH copies: duplicate CSS custom properties, two stylesheets, and size budgets that no longer describe what ships. A caret is also the honest expression of a contract we have already made - D0007 puts tier 2 tokens under the breaking-change rule, so any 1.x tokens build is by definition compatible with any 1.x react build, and the range should say so. Exact pinning would only be right if we did NOT trust our own semver. The alternative reading - that a Clara release is atomic and the packages should move together - is served by releasing them together via changesets, which we already do; it does not require forbidding the consumer from deduping. | accepted | -- | 2026-08-23 |
| D0050 | CR-01M0HWDQ is approved but scheduled with the first real component work (F01), not closed now. The CSS Modules path stays formally unproven until a component has real styles. | Triaged 2026-08-23. The CR was blocked on components existing; two now do, so it is technically unblockable today. It is still the wrong moment. Box and Button carry placeholder class names (`clara-box--md`) and no token references at all, so converting one to a CSS Module would prove that Vite compiles a `.module.css` and emits one stylesheet - the mechanical half - while proving nothing about the part that actually carries risk: that component CSS references tier 2 and tier 3 tokens only, which is a CI rule with no enforcement point yet (gate 2, lint, is still pending on US-01M0GME0). It would also be rework: D0044 renames every tier 2 token, so any stylesheet written now against the placeholder scheme is rewritten before it ships. The CR stays open with its risk stated rather than being closed on a mechanical proof that would let the real question look answered. | accepted | -- | 2026-08-23 |
| D0051 | The server/client boundary is proved by THREE mechanisms that do not share a reader: the chunk planner parses source with TypeScript's own parser; check-client-boundary asserts placement and computes transitive reachability from the bundle record; and an independent oracle reads the EMITTED bytes for client-only React hooks in any undirectived chunk. A real Vite build over the shapes that have broken placement runs as a test. | A second adversarial review found both Criticals from the first round were NOT fixed, and reproduced each with a real build while every guard reported PASS. Two root causes. First, `definedNames` was a regex over TypeScript - the ninth hand-rolled parser in this repo - and it could not see `const Switch = () => {}; export default Switch`, the most common React idiom; the component landed in the undirectived shared chunk. A suffix heuristic (`/Props$\|Context$\|Provider$/`) separately excluded every `*Provider`, and a React context Provider is client-only by definition. Second, the guard re-derived placement with the SAME reader, so a blind spot in it made the planner misplace a module AND the guard iterate an empty set - D0047's claim that the guard checked placement `independently` was false. TypeScript is already a dependency of every package here; there was never a reason to guess. The hook oracle exists so that no single reader can be a single point of failure again: it reads what was emitted, so it holds however placement was decided. Reachability replaces adjacency because `server -> shared -> client` left every server component behind the boundary while the direct-import check passed. The end-to-end build test exists because the shipping package has two components, both written `export function` - the one shape the broken reader handled - so a working reader and a broken one produced identical green runs. Corrects D0047, which stands on chunk topology and is wrong only about independence and about the shared chunk being checked. | accepted | D0047 | 2026-08-23 |
| D0052 | Releases are cut by a deliberate local `pnpm changeset version` committed to main; CI publishes on any push to main where no changeset is pending. `changesets/action` is removed - no Version PR, no `changeset-release/main` branch, and `pull-requests: write` is dropped from the release workflow. | Operator call, 2026-08-23, after the first real release run failed with `GitHub Actions is not permitted to create or approve pull requests` - having already versioned the packages and pushed a bot branch. Three candidate shapes. Enabling that repo permission works and is standard changesets practice, but it adds a second branch and a second merge to every release purely to create a pause. Version-and-publish in one CI run removes the pause entirely, which is wrong for an artifact that cannot be withdrawn; it also does not work as two runs, because a push made with GITHUB_TOKEN does not trigger another workflow. The chosen shape keeps the checkpoint and drops the machinery: the human runs `changeset version`, READS the computed versions and CHANGELOGs in the diff, and pushes - which is exactly the review the Version PR existed to provide, minus the branch. It also fits trunk-based development, which is how this project works. The safety property is that `changeset publish` ships whatever version is in the manifest, so publishing with a changeset still pending would ship an un-bumped version permanently; the workflow therefore skips (not fails - a pending changeset is the normal state of main between releases) and check-release fails if that guard is removed, with a mutation proving it can fail. Cost accepted: nothing publishes until someone runs `changeset version`, so the failure mode is a release that did not happen rather than a wrong one that did. | accepted | -- | 2026-08-23 |
| D0053 | `.size-limit.json` is GENERATED from `client-boundary.json` by `scripts/sync-size-budgets.mjs`, and `check:size-budgets` fails when the file and the classification disagree. | D0048 made client chunks per-component so the per-component JS budgets AGENTS.md declares could be real. A hand-maintained budget list would not deliver that: the failure mode is a component landing with NO budget, which is silent - the size gate passes, because nothing measures the thing that was never listed. Generating the list means a component arrives with a budget or the build fails, and it removes a class of drift this project has already been bitten by twice (gate counts disagreeing across four files; Affects paths naming files that were never created). The starting figure is 5 kB gzipped per client component, chosen rather than measured - Button is the only built client component and it is a fixture - and it is recorded as an open limit to calibrate during F01. | accepted | -- | 2026-08-23 |
| D0054 | The focus indicator is two-part and its pairings are modelled as such: the OUTER ring contrasts with the surround (canvas, surface, subtle) at 3:1, the INNER offset contrasts with the ring, and the offset contrasts with each emphasis surface it outlines. This replaces PRD Section 7's rows pairing `border-focus` directly against every emphasis surface. | PRD Section 7 lists `border-focus` against all five emphasis surfaces and says in the same breath that a single ring colour CANNOT do this - which is why it specifies a two-part indicator and leaves the resolution to F00. This is that resolution, and it is measured rather than argued: with a single ring, the accent, info, success, warning and danger emphasis surfaces come out at 1.52-1.68:1 against a 3:1 floor, from every candidate step in the accent ramp. No single colour in any ramp clears 3:1 against both a white canvas and a saturated 600-step emphasis fill; that is a property of the geometry, not of this palette. The two-part model works because the parts have different jobs: the ring only ever has to be seen against the page, and the offset only ever against the control. Recorded as a decision rather than a quiet edit because it CHANGES the PRD's enumerated table, which is exactly the kind of drift CR-01M0J0Z6 was filed about. | accepted | -- | 2026-08-23 |
| D0055 | Table row surface precedence is focus > selected > hover > striped, with selected and hover COMPOSING into `bg-selected-hover` rather than one replacing the other, and focus drawn as the two-part indicator OVER whatever surface the row already has. Implemented as a pure resolver in scripts/lib/row-surface.mjs with tier 2 tokens bg-row-striped, bg-row-hover and bg-selected-hover. | An ERP table row is routinely striped and hovered and selected and focused at once, and 'whichever CSS rule happens to win' is not a design decision - it is the absence of one, discovered later as a bug report about rows that stop looking selected. Two consequences are deliberate. Hovering a selected row still looks selected, because the user loses track of what they picked at the moment they reach for it otherwise; that needs a composed surface, not a precedence order alone. And focus is not a background at all - it is drawn over the row, so focusing a selected row never hides the selection. The resolver is pure and tested rather than left as prose, because the F01 Table component will need the same answer and prose does not survive being reimplemented. | accepted | -- | 2026-08-23 |
| D0056 | The semantic layer covers GEOMETRY as well as colour: space, size, radius, border-width and type steps are tier 2, and density is a scoped override of those tokens rather than a second scale. Component CSS therefore has a tier 2 to reach for and never needs a primitive. | Discovered by the tier gate, not designed in advance. The first real component stylesheet immediately reached for `--clara-space-2` and `--clara-radius-md`, because the semantic layer was colour-only - so the choice was to weaken the gate or to add the tokens it was asking for. The docs page written the day before says it outright: if you find yourself reaching for a primitive, the semantic layer is missing a token, and that is a change request rather than a workaround. This applies that to ourselves. Density as an OVERRIDE rather than a parallel scale matters for the same reason the dark theme overrides rather than redefines: compact redeclares only the six spacing and size tokens D0037 fixes, and everything else keeps resolving through the base layer, so the two densities cannot drift when either changes. The selector is an attribute so a subtree can be compact inside a comfortable page, which is what ADR-006's context propagation writes onto a portal root. | accepted | -- | 2026-08-23 |
| D0057 | A module that defines no component but uses client-only React goes to `clara-client-shared`, a shared chunk that CARRIES the directive - not to the undirectived `clara-shared`. Theme and density propagate through React context (TRD ADR-006), and no component other than ClaraProvider/ClaraScope may take a `theme`, `density` or `portalContainer` prop. | The first half was found by the emitted-bytes oracle rather than by review, on the first tree that had such a module: `clara-shared.js` held useState, useEffect and useContext with no directive, because the hooks it contained (`useClaraSettings`, `useSystemTheme`) define no COMPONENT and so fell through to the undirectived shared chunk. Any client component importing it would have crashed the server render. That oracle exists precisely because the planner and the placement guard share a reader; this is the case where an independent signal earned its keep. The second half is TRD ADR-006 made enforceable. DOM inheritance cannot carry theme to an overlay, because portalling to document.body leaves the themed subtree in the DOM while remaining a descendant in the React tree - so the setting has to travel by context, and the portal root re-applies it as attributes. The prop ban is the other half of the same decision: solving it with props would mean `theme`, `density` and `portalContainer` on nine overlays, permanently, and D0018 already ruled that out. It is now checked by a pure, unit-tested surface contract rather than by a grep, because inline in the guard the rule could only be exercised by editing a real .api.md - which api-extractor's own drift check rejects first, so it had never been proven to fire. | accepted | -- | 2026-08-23 |
| D0058 | `as` is implemented as a typed polymorphic contract in `lib/polymorphic.ts`, and its type-level behaviour is asserted at COMPILE time with `@ts-expect-error` rather than at runtime. Disabled controls use `aria-disabled` and stay focusable; `IconButton.label` and `ButtonGroup.label` are required props. | Three choices, one principle: put the guarantee where it can actually be checked. (1) `as` is a type-level feature, so a runtime test proves only that the tag changed - not that `<Box as="div" href="/x" />` is rejected. The negative cases live in a compile-time file where every `@ts-expect-error` is an assertion in the strongest available form: if the type system stops rejecting that line, the unused directive itself becomes an error and typecheck fails. Verified by satisfying one case and watching TS2578 fire. A loose polymorphic type is worse than none, because it type-checks and then breaks at runtime, which is the failure a consumer cannot see coming. (2) `aria-disabled` rather than the `disabled` attribute (D0028): a natively disabled control leaves the tab order, so a keyboard user can never reach it - and an ERP form is frequently mostly disabled, with the reason attached to the control they cannot reach. The cost is that the click handler must suppress activation itself, which is explicit in the component. (3) Required `label` props make the most common ERP accessibility defect - an icon-only button with no accessible name - a compile error rather than an audit finding. Axe proved the value of testing this rather than asserting it: on ButtonGroup's first run it reported a CRITICAL violation, `aria-orientation` on `role="group"`, which is not a supported attribute there and would have been meaningless anyway. | accepted | -- | 2026-08-23 |
| D0059 | ButtonGroup is `role="toolbar"` with a roving tabindex - one tab stop for the group, arrow keys within it, wrapping, plus Home and End. The reservation that this makes a form footer a single tab stop is recorded on the component rather than acted on. | The component was first built as `role="group"` leaving every button in the tab order, on my reasoning that toolbar semantics suit a dense icon bar but not a Save/Cancel footer - and that a component silently changing its keyboard model based on its contents would be worse than either. US-01M0GM3S AC1 specifies roving focus with a single tab stop, which IS toolbar behaviour, so the spec decided and the implementation follows it. The two halves go together: roving tabindex without the toolbar role announces a set of buttons that mysteriously cannot be tabbed to, and the toolbar role without roving tabindex promises arrow navigation that does nothing. Consistency across every ButtonGroup is worth more than optimising each call site; if a footer variant is wanted later that is a CR, not a second silent behaviour. Recorded because the reasoning against it was real and should not have to be rediscovered. | accepted | -- | 2026-08-23 |
| D0060 | Field and every form control are CLIENT-only. A component that renders a React context Provider cannot be a Server Component, and anything calling useContext is client too - so the whole form framework sits behind the boundary. `Field` passes its wiring by context rather than by cloning children, and the context object lives in `lib/`, not `components/`, because it is infrastructure rather than a component. | Reclassified during implementation, not designed that way. The first classification called Field, Input, Textarea and SearchInput server-capable on the reasoning that they only wire ids and render markup - and the build refused, because the context module defines a capitalised function-like export nobody had classified. Chasing that down surfaced the real rule: a Provider in a Server Component is not possible, and a control reading the wiring calls useContext, so every one of them is client-only. The boundary system caught a mistake that would otherwise have shipped as a server-render crash in the first App Router consumer to use a form. Context rather than cloning because a control is frequently wrapped - in a Tooltip, a layout primitive, a fragment - and React.Children.map sees only the immediate child, so cloning would silently skip exactly the compositions a real form is built from. Moving the context out of components/ also stops a context object being listed in a classification of components, which was the shape of the original confusion. | accepted | -- | 2026-08-23 |
| D0061 | A `Verify:` expression whose only target is a markdown file is not acceptable evidence. The ten "definition of done" ACs and the two docs ACs in EP-01M0GKM2 are verified by `scripts/check-verification.mjs`, which checks each record AGAINST the things it claims: the boundary it states must match client-boundary.json, every relative path it cites must resolve, the required sections must be present, and each docs page must make the substantive claim the story required it to settle. | The stories were authored with `file packages/react/src/components/X/verification.md` as the verifier, which passes the moment the file exists and says nothing about whether it is true - and `verify_ac.py` refuses exactly that shape, so the ACs could not have been stamped anyway. Writing a guard instead of weakening the AC is the point: on its first run it found eight components from earlier epics (the layout primitives and Table) shipping with NO record at all, which PRD F17 requires and which doc-only review had not noticed across two epics. It also caught a record whose stated boundary contradicted the manifest the build reads - two sources of truth about which components are client is how a directive lands on the wrong chunk (D0051). The docs half is checked as CLAIMS rather than keywords for the same reason: `grep "debounce"` matches the word in a page that never says whose decision debouncing is, and the whole reason US-01M0GM2X AC2 exists is to settle that it is the caller's. Four mutations prove the guard fails - a component with no record, a contradicted boundary, a citation renamed away, and the debounce passage removed from a page that is otherwise intact. | accepted | -- | 2026-08-23 |
| D0062 | Clara does not debounce SearchInput, does not auto-resize Textarea unless `maxRows` is given, and ignores the mouse wheel on NumberInput. Each is a deliberate refusal to make a timing or sizing decision on the caller's behalf, documented where the caller will look for it. | Three controls, one principle: a default that is right half the time is worse than no default, because the half it is wrong for cannot tell. Debouncing a LOCAL filter adds latency to an operation that had none and makes the list visibly lag the typing, while a SERVER query needs both a debounce and out-of-order response handling that a component cannot supply - so Clara fires onChange natively and the docs page states the two cases and what to do in each. Auto-resize is opt-in because an unbounded textarea grows until the submit button is off screen, which is worse than a scrollbar: the user cannot see the action they are about to take; `maxRows` makes the cap a required part of asking for the behaviour. The wheel is ignored because `type="number"` silently changed values on page scroll in every browser that shipped it, which is why it has the reputation it has in data entry - a scroll gesture aimed at the page must never edit a figure the user is not looking at. | accepted | -- | 2026-08-23 |
| D0063 | A `describe` block that an AC selects on is named after the ACCEPTANCE CRITERION, not after the component. RadioGroup's AC2 verifier is `^export (declare )?(function\|const\|interface) Radio[^A-Za-z0-9_]`, anchored and boundary-terminated, rather than `export.*Radio`. | Both halves are the same failure: a selector that matches nothing exits 0, so the AC passes without running anything. The Checkbox indeterminate AC selected `-t "Checkbox indeterminate is aria-checked mixed"` against a block named plain `Checkbox`; the tests existed and passed, and the AC's own run skipped all 27 of them. verify_ac.py caught it because it treats an all-skipped run as a FAIL, which is the only reason it was not stamped green. The RadioGroup verifier was worse in two ways at once: `export.*Radio` also matches `RadioGroup`, so it fails on a CORRECT implementation - and the stored line had literal backspace bytes (0x08) where `\b` was intended, swallowed by a heredoc when the story was authored, so the pattern could never match anything and `! grep` passed vacuously. A negated grep fails OPEN, which makes a corrupted pattern indistinguishable from a satisfied one. Both directions are now proven: the expression is silent on the real api.md and fires on a copy with a bare `Radio` export appended. Every story file was swept for the same swallowed-escape damage; none remained. | accepted | -- | 2026-08-23 |
| D0064 | Every form control is `aria-disabled` plus `readOnly`, never the native `disabled` attribute; the groups use it too, and a toggle suppresses its own change. `aria-required` goes only where the role supports it - RadioGroup becomes `role="radiogroup"` to carry it, and CheckboxGroup carries none. NumberInput becomes a `spinbutton` when a bound is supplied, and stays a plain textbox when none is. | Three findings, one root cause: an ARIA attribute was placed by intent rather than by what the role actually permits, and nothing checked. (1) The framework used the native `disabled` attribute, contradicting D0058 - accepted one epic earlier and already shipped in Button - with no decision recorded and two tests asserting the violation as correct. D0058's reasoning applies MORE to a form field than to a button: an ERP form is frequently mostly disabled, with the reason attached to the very control a keyboard user cannot reach. Reproduced: two tabs skipped a disabled Input and its description entirely. (2) `aria-valuemin`/`max` sat on an implicit `textbox` role, where they are invalid - nothing announced them, axe reported a CRITICAL `aria-allowed-attr`, and four documents claimed the bounds were announced. The axe fixture rendered the control without bounds, the one configuration that could not fail. The full spinbutton contract travels with the role (aria-valuenow, Home/End/PageUp/PageDown), because a control that announces bounds and offers no way to reach them is half a contract; with no bound there is no role and no aria-value*, which is correct - an account code is not a value in a range. (3) Adding `aria-required` to the groups reproduced the same defect within minutes, and the axe gate caught it immediately: `role=group` does not support it. RadioGroup takes `role="radiogroup"`, which does; CheckboxGroup takes none, because "at least one of these" is a form-level rule rather than a property of a group whose boxes are each independently optional. | accepted | -- | 2026-08-23 |
| D0065 | A test asserts the PROPERTY, not a proxy for it. Concretely: no assertion on a class name standing in for a rendered dimension; no assertion that an attribute is present standing in for it being valid; no axe fixture that omits the prop which triggers the failure; and where jsdom cannot observe the property, the environment is stood in for (a mocked `scrollHeight`) or the claim is moved out of the acceptance criterion. | Two independent reviews, run in parallel and without contact, converged on this as the single structural cause of the batch's defects - and between them reproduced eight instances. `expect(className).toContain('clara-input--md')` under compact density was the default prop echoed back and passed with the provider removed. The theme-and-density matrix asserted ClaraProvider echoing its own props and passed with all four rows set to one combination. `expect(el.style.height).toBeTruthy()` passed on "0px" - a textarea collapsed to nothing - under a test named "grows with content", because jsdom's scrollHeight is always 0. `type === 'checkbox' && checked` was the whole proof that the checked state is not colour alone, and passes against an implementation that erased the tick. The wheel test asserted a value that a text input could never have changed, and passed with the handler deleted. The SSR test compared the COUNT of ids, which is constant for a fixed tree and therefore blind to the differing-id defect its own comment named. Every one of those was stamped `Verified: yes`. The remedy is not more tests but a different question: for each assertion, what edit to the production code would make this fail? Where the answer was "none", the test was rewritten and the rewrite proven by making that edit and watching it die - which is now recorded per case in the test comments rather than asserted here. | accepted | -- | 2026-08-23 |
| D0066 | `runAxe` blocks on `form-field-multiple-labels` and `aria-deprecated-role` regardless of impact, and blocks on their `incomplete` results too. A gate cited by a verification record must actually RUN that component's tests, and `check-verification.mjs` now checks that. | The a11y gap register named `form-field-multiple-labels` (WCAG 2.0 A, SC 3.3.2) as one of exactly two rules reachable from Clara's surface that the serious-or-critical matcher can never fail - and the epic then shipped a fixture that triggers it. A Checkbox with its own `label` inside a `<Field label>` pointed two labels at one control, so the accessible name became both concatenated; it was invisible twice over, once for being moderate and once for being returned as `incomplete`, which the harness did not read at all. A prediction that comes true unnoticed is worse than no prediction. Separately, all 22 verification records claimed `- axe ... - check:axe`, and check:axe ran one file containing none of the eleven form components: a false claim inside the exact artefact the definition-of-done criterion exists to prove, and the citation check could not see it because a script name is not a path. Both halves are now checked - the script must exist, and if it runs named test files it must run one that imports the component. Fixing it added real coverage rather than removing a claim: Heading, Text and Table had no axe test at all, which only surfaced because the guard started asking. | accepted | -- | 2026-08-23 |
| D0067 | Neither `check-verification.mjs` nor `check-stylesheets.mjs` decides anything from a name shape or a substring. "Built" is read from TypeScript's own parser; a citation is any backticked token that looks like a path; a custom property is a declaration PostCSS identifies. `check-component-css.mjs` additionally asserts a shape contract - the declarations that give a control a box must exist. | The verification guard was written on the same day as a review record warning that a category must never be inferred from a name (D0051), and inferred "built" from a filename shape - a directory containing `<dirname>.tsx`. It had a live miss immediately: `TableSortButton` is a real component with its own tests and its own classification entry, and the guard was silent about it; a component declared in `index.tsx` was equally invisible. Its citation check was prefix-gated to six directory prefixes, so a citation to `e2e/` - a real directory here, and the one an author would most plausibly write - was neither resolved nor counted; switching to a shape test took the resolved-citation count from 36 to 114, meaning two thirds of all citations had never been checked. Its docs check matched bare keywords despite a docblock claiming it did not, and was defeated by replacing a section with one sentence saying the opposite; it now anchors on structure and forbids the inverted claim. `check-stylesheets.mjs` read `--[\w-]+\s*:` out of the raw text and so parsed the SELECTOR `.clara-input--search::-webkit-search-cancel-button` as a declaration of `--search`, failing a clean build - the tenth hand-rolled parser in this repo, replaced with the PostCSS already imported ten lines away. The shape contract exists because a review deleted `width`, `min-height` and `border` from `.clara-input` and the entire suite stayed green: jsdom computes no layout, so an invisible control is invisible to every test that runs there. It is explicitly not a substitute for visual regression (gate 7, US-01M0GMZW) - it sees that the rules exist, never what they look like. | accepted | -- | 2026-08-23 |
| D0068 | `aria-disabled` obliges the component to suppress the INTERACTION, not the click. Every control routes both `onClick` and `onChange` through `fieldChangeGuard`; NumberInput refuses its own synthetic writes; SearchInput and Input refuse to clear; the groups guard each option and use `aria-disabled` per input rather than the native attribute; the PasswordInput toggle does the same. A disabled field consequently still SUBMITS, and that is documented. | The fix for the native-disabled defect (D0064) reintroduced the defect it was fixing, one layer down. The guard was installed on `onClick` only - and React derives a checkbox's change from the same native click, queueing both listeners before either runs, so `preventDefault()` reverted the DOM toggle while the queued `onChange` still fired reporting `checked === true`. In the idiomatic controlled form that flips the consumer's state on a disabled control, looks correct for exactly one frame, and paints the tick on the next unrelated render: the "select all lies about what a bulk action will affect" failure, arrived at from the opposite direction. Nothing caught it because no test in the suite asserted that a disabled control suppresses anything - the whole model was added without a single test of its central promise. Five sibling holes were found by the same question, each a place where `aria-disabled` was applied and the behaviour was not: arrow-stepping a disabled NumberInput, clearing a disabled SearchInput, both group option paths, and the reveal toggle. The submission consequence is recorded rather than discovered later: native `disabled` excludes a control from form submission and `aria-disabled` does not, so a consumer migrating will start posting fields they used to omit. That is stated in the Field docs page with the remedy. | accepted | -- | 2026-08-23 |
| D0069 | A fix is not landed until its evidence exists. Concretely: a mechanism with no test that dies when it is deleted is treated as unbuilt, an acceptance criterion may not name a feature the repo does not contain, and a cited gate must both run the component's test file AND carry a selector that matches a block in it. | Round two of review found the same shape as round one, moved one step along: the implementation had been corrected and the evidence had not. `labelFor="group"` - the whole fix for a Field label bound to a fieldset - appeared in no test file at all; deleting both halves of it left 751 tests and 23 gates green, while every group fixture in the suite still rendered the broken composition and an AC was stamped `Verified: yes` against a test that never constructed its own Given. Two ACs described features that were never built: Input's prefix, suffix, clearable and character counter, and NumberInput's thousands separators. `check:axe` named the file holding 54 axe assertions and selected them with `-t "axe"`, which matched no block in it - 86 skipped, 0 run, exit 0 - and the guard added to catch exactly that class of miss only checked that the FILENAME appeared in the script. `check:keyboard` covered none of the eleven components. The response was to build what was claimed (the four Input affordances), to de-scope explicitly what should not be built here (separators, which need caret preservation across every insertion, deletion and paste and deserve their own story), and to close each hole with a mutation that proves the guard fails. The general rule is the cheap one: after fixing something, delete the fix and check that something goes red. | accepted | -- | 2026-08-23 |
| D0070 | An adversarial review agent runs in its own git worktree, never against the live working tree the author is committing from. Until that is wired, no commit is made while a review agent is running without re-reading `git diff` immediately before staging. | Two concrete failures in one run, both from sharing a tree. (1) `prove-guards-fail.mjs` stages a copy of the working tree, so when the reviewer had `check-verification.mjs` temporarily mutated, `pnpm check` reported a SURVIVED mutation that was purely the reviewer's in-flight edit - a red gate with no defect behind it, and the kind that trains an operator to re-run until green. (2) Worse in the other direction: the reviewer reverted its mutations with a broad `git checkout`, which discarded two of MY uncommitted files, and I staged them by name and committed without re-reading the diff - so a commit landed on main whose message described a change it did not contain. The mistake was mine, not the reviewer's: staging by name is a claim about content, and `git add <path>` does not verify one. This is the same class as everything else this epic caught - trusting a proxy (the filename I asked for) instead of the property (what is actually staged) - and the fix is the same shape: look at the thing itself. A worktree removes the hazard entirely; the diff re-read is what holds until then. | accepted | -- | 2026-08-23 |
| D0071 | `aria-labelledby` outranks a native `<legend>`, so anything a group must ANNOUNCE goes inside the element the Field names it with - not into the legend. A test asserting a group's `textContent` is not evidence about its name; assert `toHaveAccessibleName`. | D0064 concluded that a required CheckboxGroup should convey the requirement as text because role=group cannot carry `aria-required`. The implementation appended `(required)` to the legend - and the same commit set `aria-labelledby` on the fieldset, which overrides the legend in accessible-name computation, and visually hid the legend besides. The requirement therefore reached nobody by any route: not the name, not `aria-required`, and not the Field's asterisk, which is `aria-hidden` by design. The test passed because it asserted `textContent`, which includes visually-hidden text AND text that `aria-labelledby` has overridden - a proxy for the name rather than the name (D0065), on the one property the whole mechanism existed to deliver. RadioGroup escaped only by accident: it sets `aria-required` separately because `role="radiogroup"` supports it, and its own legend text was equally inert. The marker now lives in the Field's label element, which is what `aria-labelledby` points at, and only in group mode - in control mode `aria-required` already carries it and both would announce it twice. | accepted | -- | 2026-08-23 |
| D0072 | Every focusable element Clara's stylesheet renders must declare a `:focus-visible` rule, checked by `check-component-css.mjs`; and the shape contract covers `.clara-input-group` as well as `.clara-input`. | Three keyboard-reachable buttons - the search clear, the input clear and the password reveal - shipped with no focus ring at all, under a comment in the same file claiming the two-part indicator applied to "every other control". Deleting the entire `.clara-input:focus-visible` block separately left every gate green. A control that can be tabbed to and cannot be seen when focused is unusable by precisely the people the tab stop was kept for (D0058), and jsdom computes no layout, so a static check on the stylesheet is the only place this is catchable before visual regression exists. The group entry closes the same hole one level along: a decorated Input strips the inner control's box and draws it on `.clara-input-group`, which the shape contract did not name - so a decorated Input could lose its whole visible boundary with every gate green, which is the exact scenario the contract was written for in D0067. | accepted | -- | 2026-08-23 |

## Notes

- Decisions are numbered globally and zero-padded: `D{NNNN}`.
- Append with `scripts/decisions.py add`; list with `scripts/decisions.py list`.
- `Status`: `accepted` | `superseded` | `revisited`. A superseding decision names the one
  it replaces in `Supersedes`.
- Distinct from the sprint per-tranche ledger (`scripts/ledger.py`), which is scoped to
  a single delivery run; this is the durable project spine.
