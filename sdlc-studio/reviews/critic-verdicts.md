# Critic Verdicts

> Append-only. The independent non-author critic's verdict per unit.
> APPROVE = ready; REJECT = repair before Done. Latest row per unit wins.
> Reviewer must differ from Author - a self-review never clears the Done gate.

| Unit | Verdict | Reviewer | Author | Date | Brief | Tier | Issues |
| --- | --- | --- | --- | --- | --- | --- | --- |
| US01M0GMPJ | APPROVE | anton-reis | sdlc-studio | 2026-08-21 | 082325d27d56 | light | [new] typescript is a phantom tool dependency - three typecheck scripts invoke tsc with no `typescript` in any manifest or the lockfile (packages/tokens/package.json:25, packages/icons/package.json:27, packages/react/package.json:30); [new] .npmrc:5-7 comment describes phantom-dependency/store-layout protection but the setting is `resolution-mode=highest`, which does neither and restates pnpm's default; [new] check-peers.mjs:19 uses OR where its own message says AND, so narrowing the react peer to `^18.2.0` and dropping React 19 passes (mutation M11 SURVIVED, exit 0); [new] check-peers.mjs:11-16 cannot detect a missing peer - deleting `peerDependencies` entirely still prints "react is peer-only" (M10 SURVIVED), and optionalDependencies plus bundleDependencies are unread (M13, M14 SURVIVED); [new] check-layers.mjs:8,17,20 hardcodes the three package names, so a cycle through any fourth package reports "PASS 4 package(s), no cycle" (M5 SURVIVED, confirmed in isolation) and renaming a package drops it from the guard (M16 SURVIVED); [new] packages/{tokens,icons,react}/package.json:12 share one `index.d.ts` across import and require under `"type":"module"` - attw FalseCJS, which TRD Section 9 gate 10 fails on; [new] no guard detects a `./*` exports wildcard - all three checks exit 0 with one present (M15), and TRD:206 requires CI to fail on it; [new] scripts/lib/workspace.mjs:7 hardcodes ['packages','apps'] instead of reading pnpm-workspace.yaml globs, so a new workspace glob is invisible to all three guards; [new] repository.url names GitHub org "Luzentia-Labs" in three published manifests (packages/*/package.json:6) with no decision or doc recording it; [new] files:["dist"] with no per-package LICENSE means published tarballs ship without a license file; [new] package.json:15 `pnpm lint` exits 0 over zero packages; [new] plan deviation - PL-01M0HRA0 Phase 2 requires `"private": false` on each package manifest, none declares it (behaviorally equivalent); [pre-existing] TRD:195 lists `tokens.pairings.json` in the published surface but the closed exports table at TRD:203 omits it, so it is unreachable |
| US01M0GM9N | REJECT | anton-reis | sdlc-studio | 2026-08-21 | - | - | - |
| US01M0GMFB | REJECT | anton-reis | sdlc-studio | 2026-08-21 | - | - | - |
| US01M0GM9N | REJECT | anton-reis | sdlc-studio | 2026-08-21 | - | - | - |
| US01M0GMFB | REJECT | anton-reis | sdlc-studio | 2026-08-21 | - | - | - |
| US01M0GM9N | REJECT | anton-reis | sdlc-studio | 2026-08-21 | - | - | - |
| US01M0GMFB | REJECT | anton-reis | sdlc-studio | 2026-08-21 | - | - | - |
| US01M0GMFB | APPROVE | anton-reis | sdlc-studio | 2026-08-21 | - | - | - |
| US01M0GM9N | REJECT | anton-reis | sdlc-studio | 2026-08-21 | - | - | - |
| US01M0GM3X | REJECT | anton-reis | sdlc-studio | 2026-08-21 | - | - | - |
| US01M0GM9N | REJECT | anton-reis | sdlc-studio | 2026-08-22 | - | - | - |
| US01M0GM3X | REJECT | anton-reis | sdlc-studio | 2026-08-22 | - | - | - |
| US01M0GM9N | REJECT | anton-reis | sdlc-studio | 2026-08-22 | - | - | - |
| US01M0GM3X | REJECT | anton-reis | sdlc-studio | 2026-08-22 | - | - | - |
| US01M0GM3D | APPROVE | mira-calderon | claude-implementer | 2026-08-23 | 278c26421c04 | full | [new] LOW behaviour.test.tsx:767 'keeps every disabled control REACHABLE' is AC10's verifier but exercises Input alone, the tab-stop mechanism being shared via fieldAriaProps so coverage is adequate and only the name overreaches; [new] LOW controls.test.tsx:25 'is disabled by the Field, not by its own prop' asserts nothing for the second clause; [new] LOW decisions.md D0068 claims every control routes both onClick and onChange through fieldChangeGuard, but the five text controls route neither and rely on readOnly plus per-affordance guards, so the behaviour is correct and the decision record overclaims |
| US01M0GMBM | APPROVE | mira-calderon | claude-implementer | 2026-08-23 | accaa49be8b7 | full | [new] MEDIUM disabled is not omitted from InputProps and rest is spread last onto the element, so an Input disabled emits the native attribute D0064 forbids and the control leaves the tab order, which is disclosed on the recorded public surface but named in no verification record and becomes a one-way door once published; [new] LOW AC4 autofill is correctly marked manual and the record states no webkit-autofill rule exists, which is true of the tree |
| US01M0GM2K | APPROVE | mira-calderon | claude-implementer | 2026-08-23 | 7678c0be356f | full | [new] MEDIUM disabled is not omitted from TextareaProps and rest is spread last, so a Textarea disabled emits the native attribute D0064 forbids; [new] LOW Textarea is the only control with no explicit disabled logic and relies entirely on readOnly from fieldAriaProps, and no test asserts that a Field-disabled Textarea carries readOnly, the shared mechanism being covered only through the Input assertion |
| US01M0GMF3 | APPROVE | mira-calderon | claude-implementer | 2026-08-23 | 85555cf70c9d | full | [new] MEDIUM disabled is not omitted from NumberInputProps and rest is spread last, so a NumberInput disabled emits the native attribute D0064 forbids; [new] INFO the disabled stepping guard sits on keydown rather than on fieldChangeGuard, which is a third suppression route D0068 does not describe |
| US01M0GMMM | APPROVE | mira-calderon | claude-implementer | 2026-08-23 | 5fd72d419840 | full | [new] MEDIUM disabled is not omitted from PasswordInputProps and rest is spread last, so a PasswordInput disabled emits the native attribute D0064 forbids |
| US01M0GM2X | APPROVE | mira-calderon | claude-implementer | 2026-08-23 | b0f3d324795f | full | [new] MEDIUM disabled is not omitted from SearchInputProps and rest is spread last, so a SearchInput disabled emits the native attribute D0064 forbids |
| US01M0GMAG | APPROVE | mira-calderon | claude-implementer | 2026-08-23 | 70dd6ae8d4a3 | full | [new] MEDIUM disabled is not omitted from CheckboxProps and rest is spread last, so a Checkbox disabled emits the native attribute D0064 forbids; [new] INFO deleting the onChange half of fieldChangeGuard was re-tested here at review time and reddens 'does not report a change from a disabled Checkbox', so the D0068 mechanism is genuinely witnessed |
| US01M0GM9E | APPROVE | mira-calderon | claude-implementer | 2026-08-23 | ffdc7b6c0ad0 | full | [new] MEDIUM disabled is not omitted from SwitchProps and rest is spread last, so a Switch disabled emits the native attribute D0064 forbids |
| US01M0GMQT | APPROVE | mira-calderon | claude-implementer | 2026-08-23 | 084797683426 | full | none |
| US01M0GM0D | APPROVE | mira-calderon | claude-implementer | 2026-08-23 | e6f4127575da | full | none |
| US01M0GMRK | REJECT | idris-vale | claude-implementer | 2026-08-28 | f9479c8378dc | full | [new] The activedescendant cursor fails WCAG 1.4.11 in BOTH themes and is carried by colour alone. .clara-select\_\_option--active background is the only visual channel (packages/react/src/styles.css:1166). Measured with the repo's own scripts/lib/wcag.mjs, light #f2f0ed on #ffffff = 1.14:1 and dark #585654 on #1f1e1d = 2.28:1, against the 3:1 PRD Section 7 sets for non-text. Focus never leaves the trigger by design, so no option ever matches :focus-visible (the only Select focus ring is on the trigger, styles.css:1118) and the tint is the sole indicator of which option Enter will choose. The token resolves to --clara-color-bg-row-hover, calibrated as a Table pointer-hover affordance and reused as a keyboard cursor. No gate can see it - check-contrast.mjs covers no Select pairing, bg-row-hover is in no pairing, and jsdom resolves no var() ; [new] The selected CHOICE has no visible carrier at all. Select.tsx:129-131 applies only --active and --disabled, no \_\_option--selected rule exists in Select.tsx or styles.css, and there is no [aria-selected] rule. aria-selected="true" (listbox.ts:183) is announced to a screen reader while a sighted user sees nothing. The comment at styles.css:1164-1165 reasons correctly that cursor and choice must not share one visual treatment, then gives the choice no treatment. They coincide only at open - one arrow press, or any pointer move (listbox.ts:187), erases the current value. This is the shape D0106 ruled on for Tag one day earlier, and it propagates through the shared engine to Combobox and MultiSelect ; [new] The portalled listbox declares no font-size, so option labels take the consumer's body size. The panel declares color explicitly with a comment reasoning about portal non-inheritance (styles.css:1143) but never applies that reasoning to font-size (none in styles.css:1100-1170). ClaraPortal's host is a bare createElement div (ClaraPortal.tsx:101) and no rule sets font-size on [data-clara-theme]. Tooltip hit this and fixed it with a measured note (styles.css:506-510). An option label is the accessible name of an interactive element, so D0104's test Q1 answers yes and it is 14px, no second question ; [new] Three APG deviations, none recorded, confirmed by execution with a scratch probe since deleted. Space while OPEN gave onValueChange calls 0 and listbox still open true, where APG requires it to set the value and close - it falls into the typeahead default branch (listbox.ts:154-155) which preventDefaults and searches for a label starting with a space, so it is silently inert on a key Clara itself teaches by using it to OPEN (listbox.ts:126). Home when CLOSED did not open. A printable character when CLOSED did not open. The "A printable character - Typeahead" row in verification.md is false for the closed state. D0108 set the precedent that APG deviations are recorded in the keyboard table ; [new] The listbox has no accessible name (listbox.ts:175) where APG's example gives it aria-labelledby. Low impact, since focus never enters the list ; [new] Story Affects omits the two files carrying the work. It declares Select/** and scripts/check-component-css.mjs, but the commit touched lib/listbox.ts and styles.css and never touched check-component-css.mjs. The Test Plan's Touches column is correct and check-story-verifiers passes, so this is header hygiene - but it is why a reviewer pointed at the declared scope finds no stylesheet at all ; [pre-existing] BG-01M11WQZ, a Select outside a Field has no accessible name. Filed by the author, disclosed in verification.md, does not hold the gate ; [pre-existing] BG-01M10BWX, check:keyboard's hand-typed file list omits Select (package.json). The tests still run under pnpm test, so this is a gate-coverage gap only; BLOCKING: [new] The 1.4.11 contrast failure of the active-option highlight in both themes (styles.css:1166) ; [new] The absent visible carrier for the selected choice (Select.tsx:129-131 and styles.css:1166-1167). Both violate the existing PRD Section 7 threshold and the house colour-never-alone rule (D0100, D0102, D0104, D0106) rather than any reviewer preference, and no gate in the repository can see either |
| US01M0GMJ8 | REJECT | idris-vale | claude-implementer | 2026-08-28 | f4f1503eaa88 | full | [new] Space is swallowed in the text input. The engine's closed-branch preventDefault (packages/react/src/lib/listbox.ts:126-129) fires for key === ' ', so typing "New", Escape, " York" yields "NewYork" and a leading space is lost. APG requires printable characters to reach the textbox. The comment at listbox.ts:124-125 claiming this is harmless for an input is false by execution. The engine branch is from 8bde87f, but no defect existed until 090edfb wired an input to it, and Select's button trigger is unaffected ; [new] The activedescendant highlight is background colour alone, measured in Chromium against the built dist across all four theme and density scopes at 1.14:1 light and 2.28:1 dark against the panel, below the 3:1 WCAG 2.2 SC 1.4.11 requires of a state indicator. styles.css:1237 resolves through packages/tokens/src/component/combobox.json option-bg-active to color.bg.row-hover, a mouse-hover tint. It is the sole position signal because focus stays in the input. No second channel exists - font-weight 400 to 400, border 0px to 0px, no box-shadow, and both pseudo-elements none. check:contrast cannot catch it because packages/tokens/contrast-required.json:80-83 lists color-fg-default on color-bg-row-hover under text (7.31:1, passes) and has no nonText entry pairing color-bg-row-hover with color-bg-surface ; [new] aria-selected has no visual carrier at all. No [aria-selected] rule exists anywhere in styles.css and Combobox.tsx:203-213 emits only --active and --disabled, so a selected option is pixel-identical to an unselected one in all four theme and density combinations. Combobox.tsx:160 also blanks the input on open, because select() sets setQuery empty at Combobox.tsx:118, so after one ArrowDown a sighted user has no on-screen trace of their choice anywhere. Select does not have this second half - Select.tsx:99-100 renders the label unconditionally - so the blanking is this diff's ; [new] The local-option ceiling warning can never fire after mount. ceilingRef.current is set true at Combobox.tsx:102-103 BEFORE the condition at :105 is evaluated, so the options.length dep array is dead. Measured 0 warnings after growing a mounted Combobox from 3 to 700 options. The latch is also redundant, since devWarning already dedupes on seen.has(message) at dev-warning.ts:27. Two-line fix, and it is a clean AC3 failure in the most realistic instance of the mistake AC3 exists to catch ; [new] The status region announces "No matches" while the popup is CLOSED. Combobox.tsx:148 showEmpty carries no open term, so a parent clearing options after dismissal moves the live region from empty to "No matches" for a list that is not on screen. Because statusId is also in aria-describedby at Combobox.tsx:147, a Combobox with an empty options array describes itself as "No matches" before it is ever opened. The author's present-and-empty test at combobox.test.tsx:96-103 renders a NON-empty array, which is the one configuration that cannot fail ; [new] Combobox's own suite does not pin aria-selected. The mutant forcing aria-selected always false SURVIVED all 20 Combobox tests and was killed only by Select's tests, so a Combobox-scoped regression in the announced selection would not be caught here ; [new] A public prop docstring is false. Combobox.tsx:13 says "Ungrouped options render before every group", but groups render in first-encounter order, measured as Alpha, Charlie, Bravo. It ships to consumers ; [new] The AC8 keyboard table is wrong for Space. verification.md:26 says any printable character goes to the input as the query, contradicted by measurement, and Space is absent from the table's open-keys row ; [new] verification.md:60-61, "The local-ceiling warning fires past the ceiling", is FALSE for a list that grows past it after mount ; [new] The group label renders at 12px via group-label-size font.caption in combobox.json. It is the sole carrier of the group's identity and its accessible name, so it fails D0104's Q2 with no allowlist citation, and it is invisible to a --clara-font-caption census because it routes through a tier 3 alias ; [new] A disabled Combobox does not use the disabled surface every sibling control uses. styles.css:1203 sets colour to fg-muted, the same colour used for a group label and a message, and sets no background. Measured fg-muted #585654 on #ffffff against Input's fg-disabled #777471 on bg-disabled #f2f0ed ; [new] combobox.test.tsx:47-53 is named "keeps focus and the CARET in the input" but asserts only document.activeElement, with no selectionStart or selectionEnd assertion anywhere, and verification.md:55 repeats the unasserted half ; [new] axe never sees the error state the story's own Definition of Done names. combobox.test.tsx runs axe only over default closed and open plus the four theme and density combinations. Error, disabled and grouped states were axed by hand during this review and are clean at the matcher's threshold, so this is a coverage gap rather than a live violation ; [new] Declared Affects does not match the diff. git show --stat 090edfb for scripts/check-component-css.mjs is empty though the story declares it, while styles.css, packages/tokens/src/component/combobox.json and lib/listbox.ts are all touched and none is declared, which is why the stylesheet and token file carrying three of these findings sat outside the declared review scope ; [pre-existing] An unrelated parent re-render silently resets the highlight to the first option whenever options carries fresh array identity. Isolated re-test with no click and no dismissal: stable identity holds Swedish krona, fresh identity resets to Pound sterling with the listbox still open, on both local and async paths. Root cause listbox.ts:69-76, where the seed effect depends on options identity, present since 8bde87f. The clamp deleted by this commit would not have prevented it, so the deletion rationale in the commit message is correct ; [pre-existing] The identical highlight token and rule ship for Select at styles.css:1166 from 8bde87f, so the contrast failure is a shared-engine class rather than a Combobox one ; [pre-existing] No forced-colors or prefers-contrast support exists anywhere in styles.css and never has, which git log -S confirms. It amplifies the colour-alone finding but is project-wide rather than this unit's ; [pre-existing] DISPUTED BETWEEN SEATS, needs an operator ruling. This pass reports that AC8's docs-page clause is not actually checked, since check-verification.mjs --component Combobox prints "0 docs page(s) checked" while stamping PASS. The idris-vale pass on US-01M0GMRK reached the opposite conclusion, that check-verification.mjs:212 registers the component and :385 asserts docs existence, and that the zero counter is a separate content-claim counter rather than a hole. One of the two readings is wrong; BLOCKING: [new] The Space swallow, since AC1's WAI-ARIA combobox pattern does not hold for a printable character and it silently discards user input in a text field ; [new] The ceiling latch, since AC3 is not met in the growth case and the fix is to set the latch only when the warning actually fires ; [new] The colour-alone highlight at 1.14:1 and 2.28:1 together with the entirely unstyled aria-selected, which are one fix to the list's visual state model, under D0104's colour-never-alone rule with D0106 as the direct precedent one day earlier ; [new] The false "ungrouped before every group" docstring, which ships to consumers - correct the doc or sort ungrouped first ; [new] The false Space row in the verification record's keyboard table, which AC8 requires to be accurate ; [new] The 12px group label, blocking unless the operator records the D0104 allowlist citation naming the other carrier, which D0104's own mechanism provides for |
| US01M0GMRK | REJECT | mira-calderon | claude-implementer | 2026-08-28 | 5100cea53f8d | full | [new] verification.md:53 claims the tests verify "The full keyboard table above" and two rows of that table have no test at all. Forcing listbox.ts:109 const cycling to false leaves all 26 tests green, and dropping the metaKey, ctrlKey and altKey exclusion at listbox.ts:155 also leaves all 26 green, so Ctrl+F and Cmd+A would be swallowed by the preventDefault on line 156 with nothing going red. The typeahead cycling behaviour is stated in the table at verification.md:26 and never asserted ; [new] verification.md:59-60 cites check:component-css and check:overlay-contract as verifying the panel's own color, and neither does. Deleting styles.css:1146, the panel's color declaration, leaves component-css, overlay-contract, stylesheets, contrast, foundations, layers, public-tokens, verification and all 26 tests PASS, while the comment directly above that line describes precisely the failure it prevents - an undeclared panel taking its text colour from the PAGE while taking its background from the portal's scope, which is the dark-on-dark case. No e2e names Select either ; [new] Select.tsx:117 onCloseAutoFocus sits under the comment "THE load-bearing line" and is dead. Deleting it leaves 26 of 26 green, and it is redundant by construction: for modal false, Radix's own handler already calls preventDefault, and context.triggerRef.current is never populated because Select anchors with Anchor rather than Trigger ; [new] select.test.tsx:272 is a passenger. With ClaraPortal removed AND the sibling dialog.contains assertion removed it still passes, because the Modal itself portals through ClaraPortal, so a nested listbox still has a Clara-scoped ancestor. Only the contains half kills the AC4 mutant. The property itself is verified elsewhere, by AC6's matrix, killed 4 of 4 ; [new] The story's Affects omits the largest new file in its own diff. packages/react/src/lib/listbox.ts, 197 lines and the shared engine Combobox and MultiSelect inherit, is named in AC1 and AC2 Touches but absent from the Affects line, which instead names scripts/check-component-css.mjs, a file git show --stat 8bde87f shows is not modified at all. Also undeclared: check-verification.mjs, package.json, src/index.ts, src/styles.css, .size-limit.json, client-boundary.json, etc/clara-react.api.md, tokens/src/component/select.json, the docs page and boundary.test.tsx ; [new] Test Plan AC2's Touches column names only listbox.ts while its own mutant (a) edits Select.tsx. This is not a false green, since mutant (a) kills 7 of 8 under AC2's own selector as measured, but the row is inaccurate against the AGENTS.md Touches rule ; [new] AC5's Verify is an unscoped node scripts/check-component-css.mjs with no --component Select, which the guard's own docblock at check-component-css.mjs:32-41 names as exactly the anti-pattern check-verification.mjs articulates and refuses for itself. Sibling stories - CheckboxGroup, SearchInput, Textarea, Field and Modal - all pass --component. This is an attribution defect rather than a coverage one, since the literal and tier rules stay repo-wide and the AC5 mutant does exit 1 ; [new] Five lesser mechanisms each delete clean against all 26 tests: Select.tsx:94 aria-haspopup listbox, listbox.ts:187 onPointerMove, listbox.ts:156 the typeahead preventDefault, Select.tsx:103 the chevron aria-hidden, and most notably swapping RadixPopover.Anchor for Trigger at Select.tsx:87 and :105 - the decision the docblock at Select.tsx:47-50 devotes a whole section to, and whose failure mode is the open bug BG-01M105C0 ; [pre-existing] BG-01M10BWX, pnpm check:keyboard at package.json:61 runs a hand-typed file list that excludes Select's tests, acknowledged in the authoring commit ; [pre-existing] Radix leaks data-radix-popper-side and data-radix-popper-align onto Clara's public trigger button, measured on the shipped open Select. They are stamped by PopperAnchor, which PopoverTrigger also renders through, so Popover, Tooltip and DropdownMenu already ship them. AGENTS.md's "Radix must not leak" rule enumerates only asChild, onOpenChange and data-state, and no guard covers data-radix-* - the enumeration-staleness class again; BLOCKING: [new] verification.md:53's "The full keyboard table above" is false - the typeahead cycling row at verification.md:26 and listbox.ts:109, and the modifier-key exclusion at listbox.ts:155, both delete clean against the whole suite ; [new] verification.md:59-60 cites two guards for the panel's own color and neither sees its deletion at styles.css:1146 |
| US01M0GMJ8 | REJECT | mira-calderon | claude-implementer | 2026-08-28 | e97fb5003bfe | full | [new] AC1's "the highlight never points past the filtered list" assertion is insensitive to the very mechanism that provides it. Removing options from the seeding effect's dependency array at packages/react/src/lib/listbox.ts:76 leaves the AC1 verifier fully green - 5 passed, 1267 total, no file-count drop - because userEvent.type at combobox.test.tsx:61 clicks the input and drives an open, closed, open cycle that re-seats the highlight through the open dep instead. A MutationObserver confirms that mechanism rather than inferring it: the implicit click is an outside pointerdown for Radix's DismissableLayer and the input's onClick reopens, while userEvent.keyboard never does this. Adding skipClick true kills the mutant and still passes on clean code, and a keyboard-only probe shows the unmutated code resolving to option-0 while the mutant names option-3 with only option-0 rendered - exactly the defect the test's own comment says it guards. The story's Test Plan AC1 row reasons FROM this test to justify deleting the clamp effect, so that justification rests on an assertion that cannot fail, and the surviving mechanism is now unpinned ; [new] A leading Space typed into a CLOSED Combobox is swallowed. packages/react/src/lib/listbox.ts:126 calls preventDefault on the space key, so typing " Ac" produces "Ac", proven in jsdom. The comment at listbox.ts:124-125 asserting that Space is harmless for an input, being a printable character the input handles first, is FALSE - keydown precedes insertion. verification.md:20 omits Space as an opening key and verification.md:26 claims any printable character goes to the input as the query, and both are false as written, which makes them AC8 artifacts. Ruled new by execution rather than impression: the line dates to 8bde87f, but git cat-file shows Combobox.tsx does not exist at that commit and git grep -l useListbox at 8bde87f returns Select only, whose trigger is a button where preventDefault on Space is correct. This diff created the defect by attaching the engine to a text input ; [new] Four mechanisms are unpinned. Deleting aria-describedby at Combobox.tsx:164, or the onCloseAutoFocus preventDefault at Combobox.tsx:194, leaves the full suite at 1267 of 1267 green, and both are DOM-observable so both are cheaply pinnable. sideOffset at Combobox.tsx:189 and avoidCollisions at :190 also survive deletion but are jsdom-blind by construction and are correctly scoped out by AC5 as declared gaps ; [pre-existing] BG-01M10BWX, check:keyboard runs a hand-typed file list at package.json:61 that excludes the Combobox test file, so no keyboard-table row is gate-enforced. verification.md does not claim that gate, so this is an accurate declaration rather than a false claim; BLOCKING: [new] AC1's highlight assertion cannot fail. Add skipClick true to combobox.test.tsx:61, proven to kill the options-dep mutant while still passing on clean code, and correct the story's AC1 Test Plan row, which reasons from that insensitive test to justify deleting the clamp effect ; [new] The swallowed leading Space. Stop the space key opening the list when the trigger is a text input at listbox.ts:126, delete the false harmless-for-an-input claim at listbox.ts:124-125, and fix the two false keyboard-table rows at Combobox/verification.md:20 and :26 |
| US01M0GMJ8 | REJECT | idris-vale | claude-implementer | 2026-08-29 | 1a414ea8ac2c | full | [new] D0122's ruling was implemented as a docstring that is ALSO false, so the round-1 blocker MOVED rather than closed. Combobox.tsx:15-19 now tells consumers that ungrouped options "keep their place in the array rather than being hoisted above every group", and D0122 states the worked example renders Alpha, Charlie, Bravo. Measured in Chromium against the built dist, the array of Alpha ungrouped, Charlie in Group G, Bravo ungrouped renders Alpha, Bravo, GROUP Group G, Charlie - Bravo IS hoisted above the group, which is exactly what the new sentence denies. The grouping at Combobox.tsx:149-158 keys every ungrouped option into one empty-string bucket placed at first encounter, so A in G1, B in G2, C in G1 renders G1 with A and C, then G2 with B. The stated rationale, that this is the only order letting a consumer interleave, is false - the component COLLECTS rather than interleaves. Nothing pins render order, so it is unpinned as well as wrong ; [new] D0124 states that in forced-colors "the bar is what survives", and that sentence is the stated ground on which the gap was accepted. Measured with Chromium at forced-colors active in both the light and dark forced palettes, styles.css:1266 computes box-shadow to none and the option background is forced to Canvas, identical to the panel. Pixel-comparing the leading 8px strip of the cursor row against a plain row - normal mode differs, forced-colors is byte-identical. The cursor has ZERO carriers there, not one. It also made the design worse for that user, because box-shadow was chosen over border-inline-start on a text-shift argument and border is the inset option that survives forced-colors ; [new] the input still blanks on open. Combobox.tsx:175 renders open ? query : selected label, measured empty in all four scopes with defaultValue eur. The check glyph closes the round-1 no-trace-anywhere half, but with a placeholder the two on-screen statements now CONTRADICT - an open Combobox with defaultValue eur and a placeholder paints the placeholder, the standard signal for nothing chosen, while the list shows a check on Euro ; [new] the status region still announces "No matches" while the popup is CLOSED, unchanged from round 1. Combobox.tsx:163 showEmpty carries no open term and :162 puts statusId in aria-describedby unconditionally. Measured on a Combobox with an empty options array never opened - aria-expanded false, status text "No matches", statusId present ; [new] a disabled Combobox still uses no disabled surface. styles.css:1224 sets only color fg-muted and no background, measured #585654 on #ffffff, against styles.css:835-837 where a disabled Input sets both bg-disabled and fg-disabled, and against the comment at styles.css:920-927 recording this exact defect class. Select carries the same gap at styles.css:1125 ; [new] the story's AC9 Test Plan row overclaims in the SAFE direction, which still misleads. The row says deleting the cursor bar reddens only the guard while vitest reports 53 passed. Measured, the Combobox suite alone reports 1 failed 25 passed, because the author's own stylesheet-reading test also fails. Both channels see it, not one ; [new] combobox.test.tsx:48 is still named "keeps focus and the CARET in the input" while asserting only document.activeElement, with no selectionStart or selectionEnd anywhere in the file, and verification.md:29 still repeats the unasserted half ; [new] axe still never sees the states the definition of done names - runAxe appears only for default closed, default open, and the four theme and density combinations. Error, disabled and grouped remain uncovered ; [new] .clara-combobox\_\_option--selected is emitted at Combobox.tsx:224 and asserted at combobox.test.tsx:256, but NO rule anywhere in styles.css selects it, so a class that looks load-bearing styles nothing and the glyph is the only carrier ; [new] the cursor bar is declared with a PHYSICAL direction, box-shadow inset 8px 0 at styles.css:1266, in a stylesheet otherwise consistently logical - padding-inline, margin-inline-start, min-inline-size. No RTL support exists so nothing is regressed, but this is the one non-colour carrier of a state indicator and it is the one place a direction is hardcoded ; [pre-existing] BG-01M159WJ, the seed effect at listbox.ts:80-87 still depends on options identity ; [pre-existing] BG-01M159D6, no forced-colors or prefers-contrast rule exists anywhere in styles.css, confirmed in the browser ; [pre-existing] WITHDRAWN from this seat's round-1 verdict, the seat was wrong and the US-01M0GMRK seat was right. The claim that AC8's docs-page clause is unchecked is settled by execution - renaming the docs page makes check-verification print FAIL naming it. The zero counter is a separate content-claim counter, not a hole; BLOCKING: [new] the D0122 docstring at Combobox.tsx:15-19, which ships to consumers on a public prop and asserts a positive behavioural guarantee the code does not honour, on a decision whose own worked example is measurably wrong - correct both the docstring and D0122, or change the grouping so array order really is preserved ; [new] D0124's forced-colors sentence, which records a mitigation that does not exist and is the stated ground on which the gap was accepted - correct it to say the cursor has NO carrier in forced-colors and hand that to BG-01M159D6, or reconsider the carrier now that box-shadow is known to be the one inset option that vanishes there |
| US01M0GMRK | REJECT | idris-vale | claude-implementer | 2026-08-29 | e8a38be801c5 | full | [new] D0124's forced-colors claim is false, measured. The decision says the tint is invisible there "regardless of its contrast - the bar is what survives". In Chromium with forcedColors active against the built dist, .clara-select\_\_option--active reports background rgb(255,255,255), forced to Canvas, AND box-shadow none - the UA forces box-shadow away exactly as it forces background-color, so the cursor has ZERO carriers, not one. Control probe in the same run - border-inline-start SURVIVES at 8px with its colour forced to CanvasText, and outline survives at 2px, and Clara's own .clara-toast accent uses border-inline-start and survives. BG-01M159D6's own step 3 already says no other channel marks the highlighted option, so the decision record contradicts the bug it cites. The claim is load-bearing - D0124 calls the design CONSTRAINED NOT CHOSEN and rejects border-inline-start solely on text-shift, never weighing the carrier that actually survives ; [new] The D0124 repair is UNPINNED at the layer that decides it. Repointing one tier 3 token, select.option-cursor from color.bg.accent-emphasis to color.bg.row-hover, makes the bar identical to the tint it is drawn on at 1.00:1 and therefore invisible - and it SURVIVES every gate: vitest 1282 of 1282, check:contrast PASS at 108 pairings, check-component-css PASS, check:public-tokens PASS, check:token-output PASS, check:axe PASS. The new pairing is declared on the TIER 2 pair in pairings.json, not on the tier 3 alias the component actually paints. That is the identical hole the dropdown-menu.fg and dropdown-menu.bg entry five lines above was added to close, whose own why says repointing a tier 3 fg to a different tier 2 alias measured 3.10:1 with every gate green because nothing measured the pair the component paints. AC8's Test Plan Touches names styles.css and Select.tsx and no token file, so no mutant exercises the value ; [new] The 14px legibility floor is breached by default, with no consumer CSS at all. Measured in Chromium against the built dist on a stock page with body at 16px - .clara-input 14px and .clara-tooltip 14px, but the .clara-select trigger and .clara-select\_\_value render at 13.3333px, the UA button default, because styles.css:1100-1115 declares neither font-size nor font inherit, unlike .clara-button, .clara-input, .clara-combobox\_\_input and .clara-tooltip. On a body at 13px the portalled panel, every option label AND the new .clara-select\_\_check glyph at 1em all render at 13px, and the compact option row falls to 24px. D0104 Q1 admits no judgement here - an option label and the trigger's value are the accessible name and value of a control. Tooltip fixed this exact portal-inheritance mechanism with a measured comment at styles.css:506-510, and D0121 in this same round moved Combobox's group label to 14px while leaving the option labels and trigger value floating ; [new] The verification record's deviation count is wrong and asserts completeness. It says two APG deviations remain and names Home and printable characters. Measured by execution - End when closed also does not open, and Alt+ArrowUp while open gives zero onValueChange calls and leaves the listbox open. The APG select-only example, fetched from the w3c source rather than memory, lists Home, End, printable characters, Down, Alt+Down, Up, Enter and Space as ALL opening the closed listbox, and Alt+Up as committing and closing. Four of this class remain and two are recorded. End is the symmetric twin of a deviation named one clause earlier, and the keyboard table says Home / End jumps to first / last with no closed-state qualifier, so a reader is actively told End works ; [new] Three prose surfaces record a mutant verdict that does not reproduce. The story Test Plan AC8 row, AC8's own note, and the commit message of 10d2824 all state that deleting the box-shadow leaves vitest green. Executed - check-component-css exits 1 as claimed, but vitest ALSO reddens, at select.test.tsx:411. The claim understates coverage rather than overstating it, but it is a false statement in the record ; [new] Compact density puts the 8px cursor bar flush against the label. size.bar-thickness resolves to 8px and is deliberately density-invariant, while the option's padding-inline is 12px comfortable and 8px compact. Measured - compact paddingInlineStart 8px and text offset 8px, so zero clearance between a saturated accent bar and the first glyph, on every row the cursor visits, in both themes. Comfortable leaves 4px. This is crowding rather than legibility ; [new] The listbox still has no accessible name - listboxProps carries only id and role, measured aria-label null and aria-labelledby null. The APG select-only example markup gives it aria-labelledby, though the example's own attributes table and the pattern page do not mandate it. Low impact, since focus never enters the list ; [pre-existing] BG-01M11WQZ, a Select outside a Field has no accessible name, filed by the author and disclosed in the verification record ; [pre-existing] BG-01M10BWX, check:keyboard's hand-typed file list still omits Select, though check:axe now includes it. Tests still run under pnpm test, so this is gate coverage only; BLOCKING: [new] D0124's false forced-colors claim, because the constrained-design argument that produced box-shadow rests on it and a future reader is told the cursor has a carrier in a mode where it has none - the cheap fix is to correct the sentence and re-scope BG-01M159D6, which already contradicts it ; [new] The unpinned token, because a one-token edit silently reverts the entire repair with all 1282 tests and all 30 guards green, which is the exact failure the standing practice on unpinned repairs names ; [new] The 14px floor breach on the trigger, its value, the option labels and the new check glyph. This seat is correcting its OWN round-1 severity rather than moving the goalposts - it reasoned from the CSS last round and filed it non-blocking, and measuring it in a browser shows it fails on a default page with no consumer CSS, and now also drags the new D0124 carrier below the floor |
| US01M0GMJ8 | APPROVE | mira-calderon | claude-implementer | 2026-08-29 | 2b5494e509bd | full | [new] Two DOM-observable mechanisms reported in round 1 as cheaply pinnable are still unpinned and still undocumented. Deleting aria-describedby at Combobox.tsx:179 leaves the full suite at 52 of 52 files and 1282 of 1282 tests green with pnpm check exit 0, and deleting the onCloseAutoFocus preventDefault at Combobox.tsx:209 does the same, with no file-count or test-count drop in either case, so neither survivor is a compile failure. The repair added no test for either and verification.md names neither as a stated gap, so the record neither covers them nor admits it does not ; [new] AC9's own record says vitest cannot see two of its mutants when it can. The AC9 note claims check-component-css reddens on deletion while all 53 tests stay green, and the Test Plan row claims a vitest-only verifier over the first two is green by construction. Measured - deleting the box-shadow at styles.css:1266 reddens combobox.test.tsx:302 as well as the guard, giving 1 failed and 58 passed across the Select and Combobox files. 53 is the PRE-REPAIR count for those two files, 59 minus the 6 tests this round added, so the figure was taken before the stylesheet-reading tests existed and never re-run. It understates coverage rather than certifying anything unverified, but it tells a later maintainer those vitest cases are redundant when they are the assertions that actually fail ; [pre-existing] BG-01M159WJ, the seeding effect at listbox.ts:87 depends on options array identity rather than contents, so a parent building its options inline resets a keyboard user's highlight on any unrelated re-render. Reaches Select and Combobox both ; [pre-existing] BG-01M10BWX, check:keyboard runs a hand-typed file list excluding the Combobox test file, so no keyboard-table row is gate-enforced. verification.md does not claim that gate, so this stays an accurate declaration rather than a false claim ; [pre-existing] BG-01M15FKZ, generate-semantic.mjs rewrites pairings.json wholesale and destroys hand-added tier 3 pairings. Not triggered by this tree, which measures 108 pairings green |
| US01M0GMRK | REJECT | mira-calderon | claude-implementer | 2026-08-29 | daf408925624 | full | [new] AC8's "each has its own visible carrier and they are never the same treatment" is unverified. The CHOICE's only visible carrier is the check glyph at Select.tsx:141, and rebinding it from option.value === current to index === listbox.activeIndex makes the glyph follow the keyboard cursor instead of the user's choice, collapsing the two facts into one treatment, with 1282 of 1282 tests and 27 of 27 guards green. The class the tests assert instead, clara-select\_\_option--selected, has NO rule anywhere in styles.css, so the Test Plan's own mutant (b) reddens a test while changing nothing a user can see ; [new] AC8's 3:1 clause is pinned one tier ABOVE the pair the component paints. The D0124 pairing is color-bg-accent-emphasis on color-bg-surface, while the rule paints --clara-select-option-cursor on --clara-select-listbox-bg, so repointing option-cursor in select.json to color.bg.row-hover - making the bar the same colour as the tint it sits on, the exact 1.14:1 light and 2.28:1 dark defect D0124 records - leaves check:contrast, all 27 guards and all 1282 tests green, measured after a full rebuild. contrast-required.json states in the same file why tier 2 ancestors are not equivalent, and toast and dropdown-menu were moved to tier 3 for precisely this ; [new] Six mechanisms still delete clean against all 33 tests - Select.tsx:118 onCloseAutoFocus under the comment THE load-bearing line, Select.tsx:95 aria-haspopup listbox with check:axe also exiting 0, Select.tsx:104 the chevron's aria-hidden, the RadixPopover.Anchor to Trigger swap at Select.tsx:88 and :106 whose failure mode is open bug BG-01M105C0, listbox.ts:211 onPointerMove, and listbox.ts:180 the typeahead preventDefault ; [new] select.test.tsx:274 remains a passenger - with ClaraPortal replaced by a plain div AND the sibling dialog.contains assertion removed, all 33 tests pass, because the Modal itself portals through ClaraPortal so a nested listbox still has a Clara-scoped ancestor. Only the contains half kills the AC4 mutant, which it does ; [new] The story's Affects still omits nine files its own two commits change, including packages/tokens/contrast-required.json on which AC8's 3:1 clause rests, packages/react/src/index.ts and etc/clara-react.api.md which are the public surface, plus .size-limit.json, client-boundary.json, scripts/check-verification.mjs, boundary.test.tsx, the docs page and generate-semantic.mjs ; [new] Test Plan AC2's Touches still names only lib/listbox.ts while its own mutant (a) edits Select.tsx, inaccurate against the AGENTS.md Touches rule although not a false green, since the onOpenAutoFocus mutant is killed 8 times ; [new] The "53 tests" verdict in AC8's note and its Test Plan row is not reproducible against this tree and its mutant (a) claim is false - Select alone reports 33, Select plus Combobox 59, the workspace 1282, and deleting the cursor box-shadow does NOT leave vitest green, it fails the CURSOR carrier case in AC8's own verifier ; [new] AC2 says both behaviours "were stated in the keyboard table", but the modifier-key exclusion appears nowhere in that table - only the typeahead cycling row does ; [new] verification.md's "What is verified automatically" never names the option state model, so the round's headline repair and the two new SHAPE\_CONTRACT entries that guard it are absent from the record AC7 exists to make checkable ; [pre-existing] BG-01M10BWX, check:keyboard still runs a hand-typed file list excluding Select's tests ; [pre-existing] Radix still stamps data-radix-popper-side and data-radix-popper-align onto Clara's public trigger and data-state open onto the panel, measured on the shipped open Select. data-state is named EXPLICITLY in AGENTS.md's Radix-must-not-leak rule and no guard covers it. Popover, Tooltip and DropdownMenu already ship the same; BLOCKING: [new] AC8's never-the-same-treatment clause is stamped Verified while the CHOICE's only visible carrier can be rebound to the cursor at Select.tsx:141 with 1282 of 1282 tests and 27 of 27 guards green, because the class the tests assert on has no CSS rule ; [new] AC8's 3:1 clause is stamped Verified while option-cursor can be repointed to color.bg.row-hover, reinstating the exact D0124 defect, with check:contrast and every other gate green - the pairing is declared at tier 2 rather than at the tier 3 pair the component paints |
| US01M0GMRK | REJECT | mira-calderon | claude-implementer | 2026-08-29 | a423e4e9b89c | full | [new] AC8's 3:1 clause is still measured one adjacency away from the paint, and the record claims otherwise. The bar is an inset box-shadow drawn INSIDE .clara-select\_\_option--active at styles.css:1188, whose background is select.option-bg-active which resolves to row-hover, but the pairing measures select.option-cursor on select.listbox-bg - the more permissive of the two real adjacencies. Measured - repointing select.option-cursor to color.border.default puts the bar at 2.72:1 light and 2.36:1 dark against the tint it sits in, below the 3:1 AC8 says it clears, while check:contrast PASSES at 110 pairings, check:component-css PASSES and all 7 of AC8's own tests pass. Shipping colours are safe at 4.22 light and 4.51 dark against the tint versus 4.80 and 10.26 against the panel, so this is an unverified claim rather than a shipped defect - but pairings.json and contrast-required.json both assert it is pinned at the tier 3 pair the component actually paints, which the CSS contradicts ; [new] The 14px floor repair asserts DECLARATION, not the floor. Repointing .clara-select and .clara-select\_\_listbox-panel to var(--clara-font-caption), which is 12px, leaves check:component-css PASS and 33 of 33 Select tests green, because there is no font-size entry in VALUE\_CONTRACT - whose own docblock names exactly this failure mode. Tooltip's identical portal-inheritance fix IS proven in a browser at e2e/stacking.spec.ts:352-364, and styles.css cites that fix while Select got the declaration without the proof. No e2e spec references .clara-select at all ; [new] font: inherit at styles.css:1106, added by this diff under a comment justifying it, deletes clean - check:component-css, check:stylesheets and check:overlay-contract all PASS with 33 of 33 Select tests green, because SHAPE\_CONTRACT enrols .clara-select for font-size only ; [new] The comment at styles.css:1103-1104 claims that .clara-input and .clara-button both declare these two lines. .clara-button declares font: inherit and no font-size at all ; [new] The COMBOBOX pairing row's why is a verbatim copy of the Select row and describes repointing select.option-cursor, naming a token that is not the row's ; [new] Both pairing why fields state the mutant measures 1.00:1 while the gate they install reports 1.14:1 light and 2.28:1 dark, which is also what the commit message says - the 1.00:1 figure is the bar against the TINT, which is the adjacency the gate does not measure ; [new] Test Plan AC8 still lists mutant (a) Remove the --selected class so the CHOICE has no carrier, with a KILLED verdict, in the same cell that says the class is gone - the mutant is unrunnable against the current Select.tsx ; [pre-existing] MOVED and untouched, the six unpinned mechanisms, two re-verified by execution - deleting onCloseAutoFocus at Select.tsx:118 leaves 33 of 33 green, and deleting onPointerMove at listbox.ts:211 leaves 60 of 60 green across Select and Combobox ; [pre-existing] MOVED, select.test.tsx:274 is still a passenger - with ClaraPortal replaced by a plain div AND the dialog.contains assertion removed, all 33 tests pass ; [pre-existing] MOVED, the story's Affects still omits the files its own three commits change, now eleven of them including contrast-required.json, src/index.ts and etc/clara-react.api.md ; [pre-existing] MOVED, Test Plan AC2's Touches still names only lib/listbox.ts while its mutant (a) edits Select.tsx, and check:story-verifiers PASSES at 104 verifiers, so no guard catches it ; [pre-existing] MOVED, verification.md's What is verified automatically still never names the option state model or the new font-size contract entries - the file was not touched by this commit ; [pre-existing] BG-01M10BWX, check:keyboard still runs a hand-typed file list excluding Select ; [pre-existing] The Radix leak, re-measured on the shipped open Select - data-radix-popper-side and data-radix-popper-align on the trigger, and data-state open plus data-side and data-align on the panel, with data-state named explicitly in AGENTS.md; BLOCKING: [new] AC8's 3:1 clause is stamped Verified while the pairing measures select.option-cursor on select.listbox-bg rather than on select.option-bg-active, the background the inset bar is actually drawn on - repointing the cursor to color.border.default measures 2.72:1 light and 2.36:1 dark against that tint with check:contrast, check:component-css and all 7 of AC8's own tests green. One pairing row fixes it, and the shipping colours already clear it |
| US01M0GMJ8 | REJECT | idris-vale | claude-implementer | 2026-08-29 | ae0db3dad10a | full | [new] The third attempt at the group docstring is again false in its last sentence. Combobox.tsx:23 tells consumers that interleaving an ungrouped option BETWEEN two groups is not expressible, and measured in Chromium against the built dist, A in G1 then X ungrouped then B in G2 renders G1 with A, then X, then G2 with B - which is exactly one ungrouped option between two groups. What is actually inexpressible is ungrouped options in MORE THAN ONE position, because they share one bucket. D0125 carries the same error, saying the component cannot interleave at all. The four substantive corrections are TRUE and each was re-measured, so the round-2 blocker is otherwise CLOSED - but this sentence was written from D0125's prose rather than derived by execution, which is the mechanism D0125's own HOW THIS HAPPENED section records as the root cause ; [new] The check glyph is now the SOLE carrier of the selected choice and NO gate measures it. combobox-option-check-fg on combobox-panel-bg appears in neither contrast-required.json nor pairings.json, while the cursor bar's tier 3 pair was enrolled with a comment explaining exactly why. Proven by mutation - repointing option-check-fg to color.bg.surface makes the check invisible, confirmed by screenshot, with check:contrast PASS at 110 pairings, check:component-css PASS and 1283 of 1283 tests PASS. This diff removed the --selected class, correctly since it styled nothing, and that is what made the glyph load-bearing - so the gap was CREATED in the same round that made it matter. select.json carries the identical unenrolled token ; [new] The group label is now distinguished from an option by COLOUR ALONE. styles.css:1286-1291 declares padding, colour and font-size and no weight, so measured in all four theme and density scopes the label and an option are both 14px, both weight 400, same style, same letter-spacing and the same left edge - the only difference is fg-muted against fg-default. Under forced-colors both resolve to CanvasText and the panel reads as five identical lines where two of them are headings. At the base ref the token was font.caption and font-size is not a forced property, so the distinction existed before this repair. D0121's 14px ruling is right, and the channel the size difference had been carrying was not replaced. D0124's objection to font-weight, that it shifts text metrics as the cursor moves, does not transfer to a static heading ; [new] Options within a bucket keep their relative order is TRUE but UNPINNED for named groups - reversing insertion order for non-empty keys only SURVIVED all 1283 tests. The new order test pins only the ungrouped bucket, so D0125's claim that a test pins the render order covers one of that paragraph's three claims ; [new] D0126's deferral rests on a measurably false inference. It argues the fix is not a one-line swap because a transparent border for layout stability would paint on EVERY row - the premise is TRUE and was measured, but two per-component forced-colors blocks of three and four lines each give the cursor a carrier with zero text shift and zero row-height change, measured in both forced palettes. The transparent-border problem arises only for a border declared OUTSIDE the media query, which neither candidate needs. D0126 says it hands BG-01M159D6 the answer rather than the question, and the answer it hands over overstates the difficulty. The better argument for deferring is one D0126 does not make - system-colour keywords are invisible to check-component-css's literal rule, so such a block would enter the stylesheet unpoliced by AC6 ; [new] The verification record never names the forced-colors gap. It lists five stated gaps and the most significant limitation of this unit's headline feature, that the cursor has no carrier at all in forced-colors, is not among them - and the at-least-one-stated-gap requirement is satisfied numerically so no gate sees it ; [new] The input still blanks on open, unchanged from round 2 - measured Euro before open and empty while open with the placeholder painted, in all four scopes, so an open Combobox with a value shows the standard signal for nothing chosen while the list shows a check on Euro ; [new] The status region still announces No matches while the popup is CLOSED - showEmpty carries no open term and statusId is in aria-describedby unconditionally, measured on a never-opened Combobox with empty options ; [new] A disabled Combobox still uses no disabled surface - measured, its background is identical to an enabled one at 1.00:1 against the page in all four scopes, while a disabled Input paints a real disabled surface. Disabled is carried by text colour alone, and Select shares it ; [new] axe still never sees the states the definition of done names - runAxe covers default closed, default open and the four theme and density combinations, while error, disabled and grouped remain uncovered ; [new] combobox.test.tsx:48 is still named keeps focus and the CARET in the input while asserting only document.activeElement, with zero occurrences of selectionStart or selectionEnd in the file, and the verification record still repeats the unasserted half ; [new] The cursor bar is still declared with a PHYSICAL direction in a stylesheet otherwise consistently logical. No RTL support exists so nothing is regressed ; [pre-existing] BG-01M159D6 owns forced-colors repo-wide, and no forced-colors or prefers-contrast rule exists anywhere in styles.css, re-confirmed in the browser ; [pre-existing] BG-01M159WJ, the seed effect still depends on options identity; BLOCKING: [new] The check glyph is the sole carrier of the selected choice and its token pair is enrolled in no contrast gate, proven by a mutant that made the glyph invisible with check:contrast, check:component-css and 1283 tests all green. Enrol combobox-option-check-fg on combobox-panel-bg as nonText, and select-option-check-fg on select-listbox-bg with it ; [new] The group label conveys its meaning by colour alone - identical size, weight, style and indent to the options it heads in all four scopes, and identical colour too under forced-colors. Give it a non-colour channel - font-weight 600 costs one declaration and one tier 3 alias, and the metric-shift objection D0124 raised against weight does not apply to a heading that never moves ; [new] The false sentence at Combobox.tsx:23 and its twin in D0125 - the smallest of the three, but it ships on a public prop docstring, it is the third consecutive wrong sentence in that paragraph, and it is wrong by the exact mechanism D0125 was written to stop |
| US01M0GMRK | APPROVE | idris-vale | claude-implementer | 2026-08-29 | 49c8c2bfb99f | full | [new] AC8's Test Plan row records "Remove the --selected class ... KILLED" as a mutant against a tree where that class does not exist - grep exits 1 in both Select.tsx and styles.css, git log -S shows 4da81c7 removed it, and the same cell says "The class is gone" three sentences later. This is the same defect class the round corrected in the adjacent sentence ; [new] The new contrast pairing pins bar-vs-PANEL rather than bar-vs-TINT, which is the adjacency the bar actually abuts along its full height. Repointing select.option-cursor to the tier 2 alias color.border.default leaves check:contrast PASS at 110 pairings, check-component-css PASS and check:public-tokens PASS while measuring 2.72:1 light and 2.36:1 dark against option-bg-active, under the 3:1 AC8 itself demands ; [new] The check glyph, the CHOICE's only carrier, is EXEMPT from forced-colors in Chromium because it is an SVG - measured, its colour and stroke both stay rgb(72,81,139) while the sibling option colour IS forced, so in a forced DARK palette it paints #48518b on a #000000 Canvas at 2.83:1. BG-01M159D6's table does not record this mechanism ; [new] D0126's deferral reasoning is correct on its mechanism but over-claims the cost. Measured, a forced-colors block scoped to .clara-select\_\_option--active alone restores the carrier at Highlight in both forced palettes, needs no transparent border on inactive options, shifts no text and changes nothing outside forced-colors. The transparent-border premise itself is TRUE - border-inline-start 8px solid transparent computes to 8px solid rgb(0,0,0) under forced-colors while background-color transparent keeps alpha 0 ; [pre-existing] MOVED and knowingly deferred, the verification record still says two APG deviations remain where four do - re-executed, End does not open a closed Select and Alt+ArrowUp on an open one gives zero onValueChange calls and leaves the listbox open, while the keyboard table tells a reader Home / End jumps with no closed-state qualifier ; [pre-existing] MOVED and knowingly deferred, compact density puts the 8px cursor bar flush against the first glyph - re-measured paddingInlineStart 8px and text offset 8px, so zero clearance, against 4px comfortable ; [pre-existing] MOVED and knowingly deferred, the listbox still carries only id and role, measured aria-label null and aria-labelledby null. Low impact since focus never enters the list ; [pre-existing] MOVED, the Radix data-state leak AGENTS.md names, disclosed in 4da81c7's own commit body as deliberately untouched - the public prop surface is clean and the leak is in the rendered DOM ; [pre-existing] BG-01M11WQZ, a Select outside a Field has no accessible name, disclosed in the verification record ; [pre-existing] BG-01M10BWX, check:keyboard's hand-typed file list still omits Select ; [pre-existing] The whole type scale is fixed px, so pinning the portalled panel to --clara-font-body also pins it against a user's browser font-size preference. System-wide, consistent with Tooltip and Modal, and recorded in no decision or bug |
| US01M0GMJ8 | APPROVE | mira-calderon | claude-implementer | 2026-08-29 | 1c26117e9580 | full | [new] The two DOM-observable mechanisms carried from rounds 1 and 2 are still unpinned and still undocumented. Deleting aria-describedby at Combobox.tsx:183 and the onCloseAutoFocus preventDefault at Combobox.tsx:213 TOGETHER leaves pnpm build exit 0, 52 of 52 files, 1283 of 1283 tests and pnpm check exit 0 at 27 guards. The file count does not drop, so neither survivor is a compile failure. The verification record is byte-unchanged since 10d2824 and its Stated gaps section names neither, so the record still neither covers them nor admits it does not. Knowingly deferred by the operator, who scoped this repair to the blocking set ; [pre-existing] The triggerKind button half of the Space open key is UNPINNED. Forcing opensOnSpace false at listbox.ts:139 leaves all 1283 tests green, because Select's own Space case passes through jsdom's native button activation rather than the engine branch. Ruled pre-existing by execution rather than impression - the same test title sits at 090edfb line 115 and the pre-repair engine treated the space key unconditionally, so Select's behaviour is identical either side of the repair and that test could never discriminate. The Combobox direction IS pinned, so the fix holds and did not move ; [pre-existing] BG-01M159WJ, the seeding effect at listbox.ts:87 still depends on options array identity rather than contents, so a parent building its options inline resets a keyboard user's highlight on any unrelated re-render. Reaches Select and Combobox both, untouched by this diff ; [pre-existing] BG-01M10BWX, check:keyboard runs a hand-typed six-file list that excludes the Combobox test file, so no Combobox keyboard-table row is gate-enforced. The verification record does not claim that gate, so this remains an accurate declaration rather than a false claim ; [pre-existing] BG-01M15FKZ is now sharper but CONTAINED. Running the semantic generator drops option-cursor in pairings.json from 4 occurrences to 0, destroying both tier 3 rows this repair added. Measured, the loss is LOUD rather than silent - check:contrast then exits 1 with seven required pairings not declared, because contrast-required.json is committed separately and only shrinks by waiver. Not triggered by pnpm build, which does not run that generator, and the tree stays clean across a full build at 110 pairings green |
| US01M0GMRK | APPROVE | idris-vale | claude-implementer | 2026-08-29 | c658de386a04 | full | [new] AC8's Test Plan cell says FOUR mutants, all KILLED and then describes FIVE, with (b) used as the label for two different mutants and an orphaned "TWO mutants, both KILLED." sentence left mid-cell where the deleted --selected mutant used to open that group. The fifth, binding the check glyph to activeIndex, was run and is real and KILLED, so the count is simply wrong ; [new] The four PANEL pairings carry the TINT rows' why verbatim, so each one opens "The listbox cursor bar against the TINT it is actually drawn on" while its own background is the panel. The pairing is right and its stated reason is false ; [new] "Four APG deviations remain" is still incomplete and the claim that the count cannot drift again in either direction is over-claimed. Fetched the APG's select-only combobox table, which says Down Arrow, Enter and Space on a CLOSED combobox open the listbox "without moving focus or changing selection", while Clara SEATS the highlight on open - a difference the record's own keyboard table states one section earlier and the deviation table omits. The eight tests pin eight named BEHAVIOURS, not the count, so nothing enumerates the APG key list and an unrecorded deviation leaves the record false with the suite green ; [pre-existing] MOVED, the check glyph's forced-colors exemption is still unrecorded. The new pairings gate the NORMAL palette only, and BG-01M159D6's measured-mechanism table still has four rows, none of them about fill, stroke or colour-on-SVG. Its summary, proposed fix and acceptance criteria are all framed as a BACKGROUND being discarded, so an implementer working that bug would find the glyph survives and close it with the 2.83:1 intact. Measured at HEAD, the glyph keeps rgb(72,81,139) under forced light AND forced dark with forced-color-adjust preserve-parent-color, while a control SVG with no author colour and the chevron both ARE forced - so the exemption is specific to the two stylesheet rules that declare colour on an SVG element ; [pre-existing] MOVED and knowingly deferred under D0126, the cursor has zero carriers in forced-colors - re-measured box-shadow none, border and outline surviving, transparent border forced to CanvasText, unchanged from round 3 ; [pre-existing] MOVED and knowingly deferred, compact density still gives the 8px cursor bar zero clearance to the first glyph - re-measured option x=1, text x=9, bar 8px, against 4px in comfortable ; [pre-existing] MOVED and knowingly deferred, the listbox still carries only id and role, with aria-label and aria-labelledby both absent. Low impact since focus never enters the list ; [pre-existing] MOVED, the Radix data-state leak AGENTS.md names, disclosed in 4da81c7's own commit body as deliberately untouched - nothing in Select authors it, the public prop surface is clean and the leak is in the rendered DOM ; [pre-existing] BG-01M11WQZ, still inbox, a Select outside a Field has no accessible name ; [pre-existing] BG-01M10BWX, check:keyboard's hand-typed file list still omits Select ; [pre-existing] The whole type scale is fixed px, so pinning the portalled panel to --clara-font-body also pins it against a user's browser font-size preference. System-wide, consistent with Tooltip and Modal, recorded in no decision or bug |
| US01M0GMRK | REJECT | mira-calderon | claude-implementer | 2026-08-29 | 3ab6619e4d61 | full | [new] The APG deviation count is wrong AGAIN, one round after the commit written to correct it. verification.md:28 asserts "Four APG deviations remain" and the commit claims the count cannot drift again in either direction - at least SIX exist. The APG select-only combobox Listbox Popup table lists PageUp and PageDown, verified verbatim in three independent fetches, and measured by execution against the shipped component both leave aria-activedescendant unchanged, because listbox.ts has no case for either and they fall to the typeahead default where key.length === 1 is false. Four more arguable ones sit in the closed table, where the APG says Down Arrow, Alt+Down Arrow, Enter and Space each open without moving focus or changing selection while all four measured as opening WITH the activedescendant set to the first option ; [new] PINNED IN BOTH DIRECTIONS is FALSE for the opening half. Removing key === 'Enter' from the engine's closed branch at listbox.ts:140 leaves all 8 APG tests green, and so does removing opensOnSpace - both keys still open through native button activation, so the four opens-a-closed-Select cases cannot see the engine branch the record itself blames for the deviations. Only the deviation half is genuinely pinned: adding Home to that branch reddens at 1 failed, 7 passed ; [new] Nothing checks the COUNT, which is the commit's own stated thesis. The string "Four APG deviations" occurs exactly once in the repo and no test or guard reads it - check-verification.mjs has no deviation rule. Eight tests pin eight named BEHAVIOURS and are silent about any key not in the list, which is precisely how the count went three to two to four ; [new] The pairing why copy-paste MOVED rather than closed. The Combobox row no longer names Select's token, but one byte-identical string now sits on all four cursor rows, and it opens "The listbox cursor bar against the TINT it is actually drawn on" - false for the two rows whose background is the panel ; [new] The Test Plan AC8 row is incoherent after the repair. It claims FOUR mutants, all KILLED then lists five lettered items with a duplicated (b), and a stranded "TWO mutants, both KILLED." sentence mid-row ; [new] Select and Combobox are the only portalled surfaces with NO tier 3 TEXT pairing. contrast-required.json declares popover-fg, drawer-fg, tooltip-fg, toast-fg and dropdown-menu-fg each against their own bg, and nothing for the listbox panel whose colour is declared precisely because it portals. Measured - repointing select.listbox-fg to color.fg.muted leaves check:contrast PASS at 122 pairings, check:component-css PASS and 43 of 43 Select tests green. Shipping colours are safe at 14.63:1 on the tint and 16.64:1 on the panel in light, so this is an unverified claim rather than a shipped defect ; [new] MOVED and unrepaired, font: inherit at styles.css:1106 still deletes clean - check:component-css, check:stylesheets and check:overlay-contract all exit 0 with 43 of 43 green, because SHAPE\_CONTRACT enrols .clara-select for font-size only ; [new] MOVED and unrepaired, the comment at styles.css:1104 still claims .clara-input and .clara-button both declare these two lines - .clara-button declares font: inherit and no font-size at all ; [new] MOVED and unrepaired, the 14px repair still asserts DECLARATION rather than the floor. Repointing .clara-select and the panel to var(--clara-font-caption), which resolves to 12px, leaves check:component-css PASS and 43 of 43 green, because VALUE\_CONTRACT still has no font-size entry ; [pre-existing] MOVED and untouched, the six unpinned mechanisms - re-executed two, deleting onCloseAutoFocus at Select.tsx:118 and onPointerMove at listbox.ts:211 together leaves 73 of 73 green across both components ; [pre-existing] MOVED, the passenger assertion survives, now documented as a proxy in its successor's comment rather than removed ; [pre-existing] MOVED, Affects still omits eleven files its own commits change, including contrast-required.json, src/index.ts and etc/clara-react.api.md ; [pre-existing] MOVED, Test Plan AC2's Touches still names only lib/listbox.ts while its mutant (a) edits Select.tsx ; [pre-existing] MOVED, verification.md's What is verified automatically names neither the option state model, nor the token pins, nor the font-size contract, nor the eight APG tests added by the very commit under review ; [pre-existing] BG-01M10BWX, check:keyboard still runs a hand-typed file list excluding Select ; [pre-existing] BG-01M159MP, the Radix data-state and popper-side leak; BLOCKING: [new] The APG deviation count asserts FOUR while at least SIX exist - PageUp and PageDown are in the APG's Listbox Popup table verbatim and Clara handles neither, measured on the shipped component with aria-activedescendant unmoved. This is the same defect the commit under review exists to fix, reproduced one depth up, and the record additionally claims the count cannot drift again ; [new] The both-directions pin is vacuous in one direction - removing key === 'Enter' and then opensOnSpace from listbox.ts:140 each leave all 8 tests green, because those keys open through native button activation rather than the engine branch the record blames. Nothing anywhere reads the number four, so the count remains unchecked by the commit that declares an unchecked count is one that drifts |
| US01M0GMJ8 | REJECT | idris-vale | claude-implementer | 2026-08-29 | 9037a48103ff | full | [new] The check glyph is EXEMPT from forced-colors and paints Clara's accent on the user's Canvas, below the 3:1 non-text floor in two of the four theme x forced-palette combinations - measured in Chromium against the built dist, light theme on a forced dark palette is 2.83:1 and dark theme on a forced light palette is 1.62:1, screenshot-confirmed as barely visible, both computed with the repo's own scripts/lib/wcag.mjs. Cause isolated by control probe - an SVG's UA forced-color-adjust is preserve-parent-color, so an author colour set ON the svg is not forced while an inherited one is, and a bare sibling svg in the same option resolved to CanvasText while the check kept rgb(72,81,139). The two check rules are the ONLY two SVG rules in the stylesheet setting an explicit colour and both were created by this unit, and the declaration causing it is the one check-component-css now REQUIRES. No gate can see it, because check:contrast measures Clara-token pairs and one side here is a system colour ; [new] The verification record still names no forced-colors gap, unchanged from round 3 - Stated gaps lists five and omits the largest limitation of the component's headline feature, and the at-least-one requirement is satisfied numerically so no gate sees the omission ; [new] D0126's cost claim was not corrected and still overstates the difficulty - it argues the swap is not one-line because a transparent border for layout stability would paint on every row, which is only true of a border declared OUTSIDE the media query, and BG-01M159D6's measured-mechanism table carries no SVG row at all ; [new] AC9's own Verify expression cannot fail on two of the five mutants its Test Plan names - deleting font-weight from the group label left the vitest selector at 8 passed, and repointing option-check-fg is caught only by check:contrast. The stylesheet-reading case asserts background, box-shadow and the check's colour but no weight and no token value. Both mechanisms ARE pinned by guards that run in pnpm check and CI, so this is a certificate gap rather than an unpinned repair, and check-story-verifiers passes because its rule is that a test READS the asset, not that it asserts the mutated declaration ; [new] The input still blanks on open, re-measured - closed value Euro, open value empty with the placeholder painted, in all four scopes, and it is recorded nowhere as a deliberate deviation while Select's four were pinned in 73f8ff0 ; [new] The status region still announces while the popup is CLOSED - on a never-opened Combobox with empty options, aria-expanded is false and aria-describedby resolves to "No matches" with role=status carrying the same text ; [new] A disabled Combobox still paints no disabled surface - re-measured in all four scopes, background identical to the enabled control and to the page, while a disabled Input paints a real disabled surface. Disabled is carried by text colour alone and Select shares it. Three reports and still no bug id, which is how a system-level inconsistency ships ; [new] axe still never sees the states the definition of done names - runAxe covers default closed and open plus the four theme and density combinations, while error, a disabled Combobox and a GROUPED list remain uncovered, so role=group inside role=listbox is never axe-checked ; [new] The caret test is still named for the CARET while asserting only document.activeElement, with zero occurrences of selectionStart or selectionEnd in the file, and the verification record repeats the unasserted half ; [new] The cursor bar is still declared with a PHYSICAL direction in a stylesheet otherwise consistently logical - no RTL support exists so nothing is regressed ; [pre-existing] BG-01M159D6 re-confirmed by execution - no forced-colors or prefers-contrast rule exists anywhere in styles.css, and the cursor has ZERO carriers in that mode, with box-shadow computing to none and the active row's background forced to Canvas identical to the panel, in all four scopes ; [pre-existing] BG-01M159WJ still inbox, and the seed effect still depends on options identity; BLOCKING: [new] The check glyph, the sole visible carrier of the selected choice and load-bearing only because this unit removed the --selected class, is exempt from forced-colors and lands at 2.83:1 and 1.62:1 in two of four theme x forced-palette combinations. Two seats have now measured it independently and no artefact in the repository records it. Either give the glyph the forced palette or record the defect with these measured numbers - it is TWO RULES, not the repo-wide tint problem BG-01M159D6 describes, so that bug's deferral does not cover it ; [new] The verification record must name the forced-colors state of the option state model, both halves - the cursor's zero carriers and the glyph's exemption. This was reported in round 3, costs one line, was not acted on, and without it the record's five stated gaps satisfy the criterion by COUNT while omitting the component's largest limitation |
| US01M0GMJ8 | APPROVE | mira-calderon | claude-implementer | 2026-08-29 | 8ed14334a932 | full | [new] The Select record's claim that all four opening keys are pinned "so the count cannot drift again in either direction" is false for two of the four. Setting opensOnSpace = false at listbox.ts:139 leaves 52 of 52 files and 1296 of 1296 tests green INCLUDING the brand-new "opens a closed Select on Space" case, and deleting 'Enter' from the same condition also survives, because Select's trigger is a native button that jsdom activates on both keys. Removing 'ArrowDown' DOES redden, so the harness is live and the two survivors are branch-specific. This is my round-3 pre-existing gap ruled OVER-CLAIMED rather than CLOSED - a test was added that claims to pin the branch and cannot fail on it, and a Select AC's Verify string was extended to include it. Behaviour is correct in jsdom and in a real browser, and the PIN is what is vacuous. Scoped to US-01M0GMRK, not to this unit ; [pre-existing] Combobox keyboard traversal order can DISAGREE with rendered order, and the new docstring now documents two shapes that trigger it without saying so. Measured by execution - [X, A in G1, Y] renders X, Y, A while ArrowDown walks X, A, Y, and [A in G, Z in H, B in G] renders A, B, Z while ArrowDown walks A, Z, B, so Down Arrow moves the highlight UP the screen. The bucketing keys rendering by group while the engine numbers options by array index, and git diff shows that code byte-identical to 090edfb, so this diff did not introduce it. AC1 requires the WAI-ARIA combobox pattern, and the existing flat-index test uses options already in group order so it cannot see the divergence. Unfiled - it needs a bug ; [regression] The why text on the two PANEL pairings now reads "The listbox cursor bar against the TINT it is actually drawn on ... the first fix pinned it against the PANEL instead", which describes the TINT rows, not these. At 4da81c7 both rows carried an accurate panel rationale that this diff OVERWROTE with a copy of the tint text, so a reader deduplicating the pair would delete the wrong row. Prose only, no gate and no behaviour ; [new] The group label's font-weight VALUE is unpinned. Deleting it exits 1 via the new SHAPE\_CONTRACT entry, but changing it to 400 - which reinstates the exact heading-indistinguishable-from-options defect the entry's own comment describes - leaves check-component-css PASS, pnpm check exit 0 at 27 guards and 1296 of 1296 tests green. SHAPE\_CONTRACT asserts declaration not value and its docblock says so, so this is an honest gap rather than an over-claim, and the z-index rule shows the script CAN be value-sensitive where it matters ; [new] The two DOM-observable mechanisms carried from rounds 1, 2 and 3 are unchanged - deleting aria-describedby and the onCloseAutoFocus preventDefault TOGETHER still leaves build exit 0, 52 of 52 files, 1296 of 1296 tests and pnpm check exit 0. The file count does not drop, so neither is a compile failure. The verification record is still byte-unchanged since 10d2824 and its Stated gaps names neither. Leaving them a third time is still reasonable - both are correct, neither is user-visible today, and the operator scoped this repair to the blocking set - but the record should ADMIT them rather than stay silent ; [new] Combobox's verification record still reads "Home / End / Jumps to the first / last ENABLED option" with no state qualifier, in a table whose closed-state rows are marked (closed). Measured - Home and End do NOT open a closed Combobox while ArrowDown, ArrowUp, Enter, Space and any printable character do. The IDENTICAL Select row was corrected in THIS commit, so one of two records sharing one engine was fixed - the enumerate-the-rest failure LATEST.md names ; [pre-existing] AC9's own Verify expression cannot see the new mutant (d) - repointing option-check-fg leaves the vitest selector green at 8 passed, and only check:contrast exits 1, which is no Combobox criterion's verifier. The AC's own prose discloses the split and check:contrast runs in pnpm check and CI ; [pre-existing] BG-01M159WJ, the seeding effect still depends on options array identity. Untouched by this diff ; [pre-existing] BG-01M10BWX, check:keyboard runs a hand-typed six-file list excluding the Combobox test file. The verification record does not claim that gate ; [pre-existing] BG-01M15FKZ is unchanged but WIDER - the tier 3 rows the semantic generator would drop grew from 2 to 8. Still LOUD rather than silent, because contrast-required.json is committed separately and check-contrast reads it independently |
| US01M0GMRK | APPROVE | idris-vale | claude-opus-5 | 2026-08-31 | b5a544844e1d | - | - |
