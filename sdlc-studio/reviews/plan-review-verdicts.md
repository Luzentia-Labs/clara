# Plan-Review Verdicts

> Append-only. The independent non-author plan reviewer's verdict per unit -
> the pre-implementation AC-vs-spec check (US0090). Latest row per unit wins.
> Reviewer must differ from the plan author - a self-review never clears the gate.
> Kind names WHICH pre-code artefact was judged; a gate asks for its own kind.

| Unit | Verdict | Reviewer | Author | Date | Brief | Kind | Issues |
| --- | --- | --- | --- | --- | --- | --- | --- |
| US01M0GM0R | APPROVE | Anton Reis | sdlc-studio agent | 2026-08-23 | cdbfb0698abe | spec | [new] chunk placement keyed on directory name, so a co-located client component shipped undirectived - FIXED via TypeScript AST parsing (D0051); [new] a module shared by both sides put the server chunk behind the client boundary - FIXED with a third undirectived chunk plus transitive reachability; [regression] the round-1 repair reported both Criticals fixed when a real build showed neither was - FIXED, and a third oracle reading emitted bytes means no single reader is a point of failure; [pre-existing] the skill's retro parser cannot read this project's ULID ids. Findings sourced from two adversarial reviews by fresh non-author contexts, recorded in reviews/RV-2026-08-22-run-01m0mfxj.md |
| US01M0MQYN | APPROVE | Anton Reis | sdlc-studio agent | 2026-08-23 | 1f2815ecd5c3 | spec | [new] chunk placement keyed on directory name, so a co-located client component shipped undirectived - FIXED via TypeScript AST parsing (D0051); [new] a module shared by both sides put the server chunk behind the client boundary - FIXED with a third undirectived chunk plus transitive reachability; [regression] the round-1 repair reported both Criticals fixed when a real build showed neither was - FIXED, and a third oracle reading emitted bytes means no single reader is a point of failure; [pre-existing] the skill's retro parser cannot read this project's ULID ids. Findings sourced from two adversarial reviews by fresh non-author contexts, recorded in reviews/RV-2026-08-22-run-01m0mfxj.md |
| US01M0GMDV | APPROVE | Mira Calderon | sdlc-studio agent | 2026-08-23 | 287b488a1fec | spec | [pre-existing] hydration is verified by a clean production build rather than a browser load - recorded as an honest limit in the story, not implied; [new] the peer-resolution check compared raw paths and tripped on the macOS /var -> /private/var symlink - FIXED by realpath'ing both sides |
| US01M0NJZN | APPROVE | Anton Reis | sdlc-studio agent | 2026-08-23 | 29e08250bc45 | spec | [new] the older chunk-plan tests asserted the single-chunk name and had to move with the behaviour - updated rather than deleted; [new] prove-guards mutations referenced dist/clara-client.js which no longer exists - repointed, and a new mutation pins the per-component property itself; [pre-existing] the 5 kB per-component figure is a starting number, not a measured one - recorded as an open limit |
| US01M0GMAE | APPROVE | Anton Reis | sdlc-studio agent | 2026-08-23 | beb0d219605b | spec | [new] three guards defined a tier by a hardcoded name prefix (semantic-) and would have gone silently vacuous at the rename - FIXED by consulting the tier manifest; [new] the contrast threshold came from a regex over the token name, which demanded 4.5:1 of fg-disabled where PRD Section 7 sets 3:1 - FIXED by deriving the threshold from the declared role; [new] PRD Section 7 enumerated the same colour pair twice under two roles, making the row count ambiguous - merged to one row at the strictest threshold, giving the 48 the PRD requires; [pre-existing] the two-part focus indicator was specified but never resolved - resolved and measured as D0054 |
| US01M0GM66 | APPROVE | Idris Vale | sdlc-studio agent | 2026-08-23 | ff5d892bfd39 | spec | [new] three guards defined a tier by a hardcoded name prefix (semantic-) and would have gone silently vacuous at the rename - FIXED by consulting the tier manifest; [new] the contrast threshold came from a regex over the token name, which demanded 4.5:1 of fg-disabled where PRD Section 7 sets 3:1 - FIXED by deriving the threshold from the declared role; [new] PRD Section 7 enumerated the same colour pair twice under two roles, making the row count ambiguous - merged to one row at the strictest threshold, giving the 48 the PRD requires; [pre-existing] the two-part focus indicator was specified but never resolved - resolved and measured as D0054 |
| US01M0GMMX | APPROVE | Anton Reis | sdlc-studio agent | 2026-08-23 | 465de733aab4 | spec | [new] three guards defined a tier by a hardcoded name prefix (semantic-) and would have gone silently vacuous at the rename - FIXED by consulting the tier manifest; [new] the contrast threshold came from a regex over the token name, which demanded 4.5:1 of fg-disabled where PRD Section 7 sets 3:1 - FIXED by deriving the threshold from the declared role; [new] PRD Section 7 enumerated the same colour pair twice under two roles, making the row count ambiguous - merged to one row at the strictest threshold, giving the 48 the PRD requires; [pre-existing] the two-part focus indicator was specified but never resolved - resolved and measured as D0054 |
| US01M0GME0 | APPROVE | Anton Reis | sdlc-studio agent | 2026-08-23 | 21e9ca1f6b97 | spec | [new] the semantic layer was colour-only, so the first real stylesheet reached for a tier 1 primitive - FIXED by adding semantic geometry (D0056); [new] client-only hooks fell through to the undirectived shared chunk, which would crash any server render importing them - FIXED with clara-client-shared (D0057), caught by the emitted-bytes oracle; [new] the component-CSS literal rule matched per line and skipped declarations written on the selector line - FIXED with PostCSS; [new] the size budget measured React's weight and called it Clara's - FIXED by ignoring peers; [pre-existing] the surface contract could not be proven to fire because api-extractor's drift check rejects an edited report first - FIXED by extracting it to a pure, unit-tested module |
| US01M0GMC6 | APPROVE | Idris Vale | sdlc-studio agent | 2026-08-23 | b6bc798c9576 | spec | [new] the semantic layer was colour-only, so the first real stylesheet reached for a tier 1 primitive - FIXED by adding semantic geometry (D0056); [new] client-only hooks fell through to the undirectived shared chunk, which would crash any server render importing them - FIXED with clara-client-shared (D0057), caught by the emitted-bytes oracle; [new] the component-CSS literal rule matched per line and skipped declarations written on the selector line - FIXED with PostCSS; [new] the size budget measured React's weight and called it Clara's - FIXED by ignoring peers; [pre-existing] the surface contract could not be proven to fire because api-extractor's drift check rejects an edited report first - FIXED by extracting it to a pure, unit-tested module |
| US01M0GMT2 | APPROVE | Idris Vale | sdlc-studio agent | 2026-08-23 | 5daeeaaf23bc | spec | [new] the semantic layer was colour-only, so the first real stylesheet reached for a tier 1 primitive - FIXED by adding semantic geometry (D0056); [new] client-only hooks fell through to the undirectived shared chunk, which would crash any server render importing them - FIXED with clara-client-shared (D0057), caught by the emitted-bytes oracle; [new] the component-CSS literal rule matched per line and skipped declarations written on the selector line - FIXED with PostCSS; [new] the size budget measured React's weight and called it Clara's - FIXED by ignoring peers; [pre-existing] the surface contract could not be proven to fire because api-extractor's drift check rejects an edited report first - FIXED by extracting it to a pure, unit-tested module |
| US01M0GM5M | APPROVE | Anton Reis | sdlc-studio agent | 2026-08-23 | deea398a6fc3 | spec | [new] the semantic layer was colour-only, so the first real stylesheet reached for a tier 1 primitive - FIXED by adding semantic geometry (D0056); [new] client-only hooks fell through to the undirectived shared chunk, which would crash any server render importing them - FIXED with clara-client-shared (D0057), caught by the emitted-bytes oracle; [new] the component-CSS literal rule matched per line and skipped declarations written on the selector line - FIXED with PostCSS; [new] the size budget measured React's weight and called it Clara's - FIXED by ignoring peers; [pre-existing] the surface contract could not be proven to fire because api-extractor's drift check rejects an edited report first - FIXED by extracting it to a pure, unit-tested module |
| US01M0GMN0 | APPROVE | Idris Vale | sdlc-studio agent | 2026-08-23 | 6879bd67185e | spec | [pre-existing] three deliverables were recorded Provisional that the semantic layer has since made real - border weight, type scale and the pairing table - FIXED by updating the record to match what shipped; [pre-existing] elevation and motion remain Provisional under D0035 clause 1, consumed by components rather than by the token layer, so F01 is where they stop being guesses |
| US01M0GMGZ | APPROVE | Anton Reis | sdlc-studio agent | 2026-08-23 | 785a75c706f5 | spec | [new] ButtonGroup was built as role=group leaving every button in the tab order, contradicting AC1's roving focus - CORRECTED to the spec, with the reservation recorded as D0059 rather than acted on; [new] axe found a CRITICAL violation on ButtonGroup's first run, aria-orientation on role=group, which is unsupported there - removed rather than suppressed; [new] the icons package .d.ts re-exported extensionless paths, failing node16 resolution - FIXED with bundleTypes, same root cause as clara-react; [pre-existing] screen reader announcements are not automated and visual regression is not wired - both stated in each component's verification record rather than implied |
| US01M0GMJK | APPROVE | Anton Reis | sdlc-studio agent | 2026-08-23 | 7e1f1f5bab61 | spec | [new] ButtonGroup was built as role=group leaving every button in the tab order, contradicting AC1's roving focus - CORRECTED to the spec, with the reservation recorded as D0059 rather than acted on; [new] axe found a CRITICAL violation on ButtonGroup's first run, aria-orientation on role=group, which is unsupported there - removed rather than suppressed; [new] the icons package .d.ts re-exported extensionless paths, failing node16 resolution - FIXED with bundleTypes, same root cause as clara-react; [pre-existing] screen reader announcements are not automated and visual regression is not wired - both stated in each component's verification record rather than implied |
| US01M0GMVN | APPROVE | Anton Reis | sdlc-studio agent | 2026-08-23 | 38fd70758af5 | spec | [new] ButtonGroup was built as role=group leaving every button in the tab order, contradicting AC1's roving focus - CORRECTED to the spec, with the reservation recorded as D0059 rather than acted on; [new] axe found a CRITICAL violation on ButtonGroup's first run, aria-orientation on role=group, which is unsupported there - removed rather than suppressed; [new] the icons package .d.ts re-exported extensionless paths, failing node16 resolution - FIXED with bundleTypes, same root cause as clara-react; [pre-existing] screen reader announcements are not automated and visual regression is not wired - both stated in each component's verification record rather than implied |
| US01M0GMQ7 | APPROVE | Anton Reis | sdlc-studio agent | 2026-08-23 | 6b8b043325d7 | spec | [new] ButtonGroup was built as role=group leaving every button in the tab order, contradicting AC1's roving focus - CORRECTED to the spec, with the reservation recorded as D0059 rather than acted on; [new] axe found a CRITICAL violation on ButtonGroup's first run, aria-orientation on role=group, which is unsupported there - removed rather than suppressed; [new] the icons package .d.ts re-exported extensionless paths, failing node16 resolution - FIXED with bundleTypes, same root cause as clara-react; [pre-existing] screen reader announcements are not automated and visual regression is not wired - both stated in each component's verification record rather than implied |
| US01M0GMZC | APPROVE | Anton Reis | sdlc-studio agent | 2026-08-23 | b0807f6ac34a | spec | [new] ButtonGroup was built as role=group leaving every button in the tab order, contradicting AC1's roving focus - CORRECTED to the spec, with the reservation recorded as D0059 rather than acted on; [new] axe found a CRITICAL violation on ButtonGroup's first run, aria-orientation on role=group, which is unsupported there - removed rather than suppressed; [new] the icons package .d.ts re-exported extensionless paths, failing node16 resolution - FIXED with bundleTypes, same root cause as clara-react; [pre-existing] screen reader announcements are not automated and visual regression is not wired - both stated in each component's verification record rather than implied |
| US01M0GMBJ | APPROVE | Anton Reis | sdlc-studio agent | 2026-08-23 | 3fa3e3b9da4e | spec | [new] ButtonGroup was built as role=group leaving every button in the tab order, contradicting AC1's roving focus - CORRECTED to the spec, with the reservation recorded as D0059 rather than acted on; [new] axe found a CRITICAL violation on ButtonGroup's first run, aria-orientation on role=group, which is unsupported there - removed rather than suppressed; [new] the icons package .d.ts re-exported extensionless paths, failing node16 resolution - FIXED with bundleTypes, same root cause as clara-react; [pre-existing] screen reader announcements are not automated and visual regression is not wired - both stated in each component's verification record rather than implied |
| US01M0GM69 | APPROVE | Idris Vale | sdlc-studio agent | 2026-08-23 | d215896fd9e8 | spec | [new] ButtonGroup was built as role=group leaving every button in the tab order, contradicting AC1's roving focus - CORRECTED to the spec, with the reservation recorded as D0059 rather than acted on; [new] axe found a CRITICAL violation on ButtonGroup's first run, aria-orientation on role=group, which is unsupported there - removed rather than suppressed; [new] the icons package .d.ts re-exported extensionless paths, failing node16 resolution - FIXED with bundleTypes, same root cause as clara-react; [pre-existing] screen reader announcements are not automated and visual regression is not wired - both stated in each component's verification record rather than implied |
| US01M0GMW1 | APPROVE | Idris Vale | sdlc-studio agent | 2026-08-23 | 4936a13b5769 | spec | [new] ButtonGroup was built as role=group leaving every button in the tab order, contradicting AC1's roving focus - CORRECTED to the spec, with the reservation recorded as D0059 rather than acted on; [new] axe found a CRITICAL violation on ButtonGroup's first run, aria-orientation on role=group, which is unsupported there - removed rather than suppressed; [new] the icons package .d.ts re-exported extensionless paths, failing node16 resolution - FIXED with bundleTypes, same root cause as clara-react; [pre-existing] screen reader announcements are not automated and visual regression is not wired - both stated in each component's verification record rather than implied |
| US01M0GM3S | APPROVE | Idris Vale | sdlc-studio agent | 2026-08-23 | 03023ada8b80 | spec | [new] ButtonGroup was built as role=group leaving every button in the tab order, contradicting AC1's roving focus - CORRECTED to the spec, with the reservation recorded as D0059 rather than acted on; [new] axe found a CRITICAL violation on ButtonGroup's first run, aria-orientation on role=group, which is unsupported there - removed rather than suppressed; [new] the icons package .d.ts re-exported extensionless paths, failing node16 resolution - FIXED with bundleTypes, same root cause as clara-react; [pre-existing] screen reader announcements are not automated and visual regression is not wired - both stated in each component's verification record rather than implied |
| US01M0GMK8 | APPROVE | Idris Vale | sdlc-studio agent | 2026-08-23 | 39972763abc9 | spec | [new] ButtonGroup was built as role=group leaving every button in the tab order, contradicting AC1's roving focus - CORRECTED to the spec, with the reservation recorded as D0059 rather than acted on; [new] axe found a CRITICAL violation on ButtonGroup's first run, aria-orientation on role=group, which is unsupported there - removed rather than suppressed; [new] the icons package .d.ts re-exported extensionless paths, failing node16 resolution - FIXED with bundleTypes, same root cause as clara-react; [pre-existing] screen reader announcements are not automated and visual regression is not wired - both stated in each component's verification record rather than implied |
| US01M0GMYZ | APPROVE | Anton Reis | sdlc-studio agent | 2026-08-23 | 770f7751588a | spec | [new] ButtonGroup was built as role=group leaving every button in the tab order, contradicting AC1's roving focus - CORRECTED to the spec, with the reservation recorded as D0059 rather than acted on; [new] axe found a CRITICAL violation on ButtonGroup's first run, aria-orientation on role=group, which is unsupported there - removed rather than suppressed; [new] the icons package .d.ts re-exported extensionless paths, failing node16 resolution - FIXED with bundleTypes, same root cause as clara-react; [pre-existing] screen reader announcements are not automated and visual regression is not wired - both stated in each component's verification record rather than implied |
| US01M0GM3D | REJECT | mira-calderon | claude-implementer | 2026-08-23 | 1e7b22237536 | test-plan | [regression] the theme x density AC is unfalsifiable - asserts the provider echoing its own props, axe with color-contrast disabled, no Clara CSS in the DOM, and no visual baseline exists; [regression] every verification record cites check:axe, which runs primitives.test.tsx only and contains none of these components; [regression] check-verification.mjs validates path citations but never bare script names, so the false claim was invisible to it; [regression] no unit carries a Test Plan section, so no AC names the production change its test must fail on |
| US01M0GMBM | REJECT | mira-calderon | claude-implementer | 2026-08-23 | ac1ee2c5e028 | test-plan | [regression] the theme x density AC is unfalsifiable - asserts the provider echoing its own props, axe with color-contrast disabled, no Clara CSS in the DOM, and no visual baseline exists; [regression] every verification record cites check:axe, which runs primitives.test.tsx only and contains none of these components; [regression] check-verification.mjs validates path citations but never bare script names, so the false claim was invisible to it; [regression] no unit carries a Test Plan section, so no AC names the production change its test must fail on |
| US01M0GM2K | REJECT | mira-calderon | claude-implementer | 2026-08-23 | 4bdafd931f00 | test-plan | [regression] the theme x density AC is unfalsifiable - asserts the provider echoing its own props, axe with color-contrast disabled, no Clara CSS in the DOM, and no visual baseline exists; [regression] every verification record cites check:axe, which runs primitives.test.tsx only and contains none of these components; [regression] check-verification.mjs validates path citations but never bare script names, so the false claim was invisible to it; [regression] no unit carries a Test Plan section, so no AC names the production change its test must fail on |
| US01M0GMF3 | REJECT | mira-calderon | claude-implementer | 2026-08-23 | 9d391e50cbc1 | test-plan | [regression] the theme x density AC is unfalsifiable - asserts the provider echoing its own props, axe with color-contrast disabled, no Clara CSS in the DOM, and no visual baseline exists; [regression] every verification record cites check:axe, which runs primitives.test.tsx only and contains none of these components; [regression] check-verification.mjs validates path citations but never bare script names, so the false claim was invisible to it; [regression] no unit carries a Test Plan section, so no AC names the production change its test must fail on |
| US01M0GMMM | REJECT | mira-calderon | claude-implementer | 2026-08-23 | 36df895c14d5 | test-plan | [regression] the theme x density AC is unfalsifiable - asserts the provider echoing its own props, axe with color-contrast disabled, no Clara CSS in the DOM, and no visual baseline exists; [regression] every verification record cites check:axe, which runs primitives.test.tsx only and contains none of these components; [regression] check-verification.mjs validates path citations but never bare script names, so the false claim was invisible to it; [regression] no unit carries a Test Plan section, so no AC names the production change its test must fail on |
| US01M0GM2X | REJECT | mira-calderon | claude-implementer | 2026-08-23 | 0b4aadc92cc9 | test-plan | [regression] the theme x density AC is unfalsifiable - asserts the provider echoing its own props, axe with color-contrast disabled, no Clara CSS in the DOM, and no visual baseline exists; [regression] every verification record cites check:axe, which runs primitives.test.tsx only and contains none of these components; [regression] check-verification.mjs validates path citations but never bare script names, so the false claim was invisible to it; [regression] no unit carries a Test Plan section, so no AC names the production change its test must fail on |
| US01M0GMAG | REJECT | mira-calderon | claude-implementer | 2026-08-23 | de30e03d7df3 | test-plan | [regression] the theme x density AC is unfalsifiable - asserts the provider echoing its own props, axe with color-contrast disabled, no Clara CSS in the DOM, and no visual baseline exists; [regression] every verification record cites check:axe, which runs primitives.test.tsx only and contains none of these components; [regression] check-verification.mjs validates path citations but never bare script names, so the false claim was invisible to it; [regression] no unit carries a Test Plan section, so no AC names the production change its test must fail on |
| US01M0GM9E | REJECT | mira-calderon | claude-implementer | 2026-08-23 | 408fb90940a7 | test-plan | [regression] the theme x density AC is unfalsifiable - asserts the provider echoing its own props, axe with color-contrast disabled, no Clara CSS in the DOM, and no visual baseline exists; [regression] every verification record cites check:axe, which runs primitives.test.tsx only and contains none of these components; [regression] check-verification.mjs validates path citations but never bare script names, so the false claim was invisible to it; [regression] no unit carries a Test Plan section, so no AC names the production change its test must fail on |
| US01M0GMQT | REJECT | mira-calderon | claude-implementer | 2026-08-23 | a0febfc701b6 | test-plan | [regression] the theme x density AC is unfalsifiable - asserts the provider echoing its own props, axe with color-contrast disabled, no Clara CSS in the DOM, and no visual baseline exists; [regression] every verification record cites check:axe, which runs primitives.test.tsx only and contains none of these components; [regression] check-verification.mjs validates path citations but never bare script names, so the false claim was invisible to it; [regression] no unit carries a Test Plan section, so no AC names the production change its test must fail on |
| US01M0GM0D | REJECT | mira-calderon | claude-implementer | 2026-08-23 | 1f1e5d98b122 | test-plan | [regression] the theme x density AC is unfalsifiable - asserts the provider echoing its own props, axe with color-contrast disabled, no Clara CSS in the DOM, and no visual baseline exists; [regression] every verification record cites check:axe, which runs primitives.test.tsx only and contains none of these components; [regression] check-verification.mjs validates path citations but never bare script names, so the false claim was invisible to it; [regression] no unit carries a Test Plan section, so no AC names the production change its test must fail on |
| US01M0GM3D | APPROVE | mira-calderon | claude-implementer | 2026-08-23 | 5dd4e9baba72 | test-plan | [pre-existing] Edge Cases row 'A consumer passes their own id - It wins, and the wiring is built from it' contradicts the code. No id prop exists on FieldProps (packages/react/src/components/Field/Field.tsx:17-45) or on any control (all Omit<...,'id'>), no test covers it, and AC2 requires GENERATED ids, so the row contradicts its own story. Introduced by 121d032, not by this diff; [pre-existing] AC7's verifier turns red only via an uncaught exception thrown inside the onSubmit handler at packages/react/src/components/Field/\_\_tests\_\_/field.test.tsx:226. All 3 tests report PASSED and vitest warns 'This might cause false positive tests'. Capture the FormData and assert after the await |
| US01M0GMBM | APPROVE | mira-calderon | claude-implementer | 2026-08-23 | 914b3502b936 | test-plan | none |
| US01M0GM2K | APPROVE | mira-calderon | claude-implementer | 2026-08-23 | 613c9e52abfa | test-plan | none |
| US01M0GMF3 | APPROVE | mira-calderon | claude-implementer | 2026-08-23 | 1cf7f591b6f7 | test-plan | none |
| US01M0GMMM | APPROVE | mira-calderon | claude-implementer | 2026-08-23 | 800fd3720dff | test-plan | none |
| US01M0GM2X | APPROVE | mira-calderon | claude-implementer | 2026-08-23 | d2ff3456e9dc | test-plan | none |
| US01M0GMAG | APPROVE | mira-calderon | claude-implementer | 2026-08-23 | f612c748f727 | test-plan | none |
| US01M0GM9E | APPROVE | mira-calderon | claude-implementer | 2026-08-23 | 8ef38d478283 | test-plan | none |
| US01M0GM0D | APPROVE | mira-calderon | claude-implementer | 2026-08-23 | b163765efa86 | test-plan | none |
| US01M0GMQT | REJECT | mira-calderon | claude-implementer | 2026-08-23 | e393beb83d33 | test-plan | [new] BLOCKING AC1 - the Test Plan row names 'Add tabIndex={0} to every radio' first and AC1's verifier SURVIVES it. I made the edit at packages/react/src/components/RadioGroup/RadioGroup.tsx:80 and 'vitest -t "RadioGroup roving focus"' exited 0. The mechanism assertion exists but sits in describe('RadioGroup is a single tab stop') at packages/react/src/components/Field/\_\_tests\_\_/behaviour.test.tsx:1133, which AC1's selector does not match. The same mutant kills that block. Both the mechanism test and the Test Plan row were added by bcac2a7 while AC1's Verify line was left untouched since 78973f9. Fix - point AC1 at both describes, e.g. vitest "RadioGroup roving focus/RadioGroup is a single tab stop", then re-verify |
| US01M0GMQT | APPROVE | mira-calderon | claude-implementer | 2026-08-23 | 129d7035e754 | test-plan | [pre-existing] AC4 minor - --component RadioGroup does not scope the literal and tier rules, so a raw literal planted in .clara-table-sort at styles.css:385 reddens RadioGroup's AC4 verifier. Over-broad, never under-broad, and the script header documents it as deliberate; [pre-existing] AC1 AC3 AC5 AC6 minor - vitest run -t exits 0 when zero tests match, so a renamed or deleted describe passes these verifiers vacuously. A DSL property at verify\_ac.py:1139 shared by every vitest-verified story in the repo, unchanged by this diff; [pre-existing] AC2 nit - the Test Plan row and the Test Scenario tick both say 'the API report', but the verifier greps packages/react/dist/index.d.ts while a distinct artifact named the API report exists at packages/react/etc/clara-react.api.md. The mutant dies either way; [pre-existing] AC1 nit - the Playwright gap for the tab-stop outcome is named on verification.md but pinned to no story id, unlike the visual-baseline gap pinned to US-01M0GMZW. The keyboard table's Space row is likewise untested and unnamed in that gap |
| US01M0GM3D | REJECT | anton-reis | claude-implementer | 2026-08-23 | 986cc2e0bd1a | spec | [pre-existing] MAJOR the User Story promises 'required state' and PRD F08 requires the marker, but no AC, Edge Case, Test Scenario or Test Plan row covers it although it is public API at Field.tsx:31 and three decisions govern it (D0064 D0071 D0074); [pre-existing] MAJOR PRD F08's 'stated once per form, not inferred from an asterisk alone' is reinterpreted as a per-field hidden marker with no deviation recorded in the story; [pre-existing] MINOR AC3 claims 'no placeholder-as-label pattern anywhere' with a Field-scoped verifier and no repo-wide guard |
| US01M0GMBM | REJECT | anton-reis | claude-implementer | 2026-08-23 | 2fc701bfffac | spec | [pre-existing] MAJOR AC2's Then claims 'visually distinct and readonly text stays at full contrast' but its verifier asserts only attributes and tab order, and styles.css:227 (the readonly colour) is absent from SHAPE\_CONTRACT at check-component-css.mjs:144, so deleting it leaves every criterion green; [pre-existing] MAJOR PRD F09's 'All inputs forward ref to the underlying native element' has no criterion in this or any epic story although it is implemented at Input.tsx:46 and is permanent public surface; [pre-existing] MINOR AC4 and the Edge Cases state Clara does not restyle autofill, which contradicts PRD F09's autofill criterion with no entry in decisions.md |
| US01M0GM2K | REJECT | anton-reis | claude-implementer | 2026-08-23 | 71719291780e | spec | [pre-existing] MAJOR PRD F09 requires 'auto-resize with a maxRows cap AND a manual resize handle option' and the story covers only the first half, the handle being an always-on 'resize: vertical' at styles.css:191 with no prop, no AC, no Edge Case row, no Deferred note and no decision; [pre-existing] MINOR resize is absent from SHAPE\_CONTRACT at check-component-css.mjs:157 so deleting it leaves every criterion green; [pre-existing] MINOR the Edge Case 'content is removed and the control shrinks back to its rows floor' is claimed as covered but AC1's Then names only growth and the cap |
| US01M0GMF3 | REJECT | anton-reis | claude-implementer | 2026-08-23 | 62bb858af634 | spec | [pre-existing] MAJOR PRD F09 requires stepper buttons and none exist (NumberInput.tsx:138-167 renders only the input and a unit span, and 'stepper' appears nowhere in the repo outside prd.md:458), with no AC, no Edge Case, no decision and no line in this story's own 'Deferred, deliberately' section that handled the sibling omission under D0069; [pre-existing] MINOR PRD F09 names precision but there is no precision prop, it is derived from step at NumberInput.tsx:49-63, and the deviation is unrecorded |
| US01M0GMMM | REJECT | anton-reis | claude-implementer | 2026-08-23 | 2e8fbecde696 | spec | [pre-existing] BLOCKING AC1's Then states the defect - 'its label reflects the current state' is the exact mutant this story's own Test Plan says must kill the test, is the opposite of PasswordInput.tsx:9,40, and is contradicted by the test's own title at behaviour.test.tsx:264, yet AC1 is stamped Verified yes; [pre-existing] MINOR AC1's Given/When says 'reach the toggle by keyboard' but its verifier uses userEvent.click and the keyboard operability test lives under AC4's block at behaviour.test.tsx:491-499; [pre-existing] MINOR AC4's Then speaks only of focus while its Test Plan mutant is the disabled-reveal guard the criterion never mentions, the criterion-vs-mutant drift D0083 was written about |
| US01M0GM2X | REJECT | anton-reis | claude-implementer | 2026-08-23 | 8a515e6cf842 | spec | [pre-existing] MAJOR PRD F09 requires SearchInput to include a search icon and none is rendered (SearchInput.tsx:55-79 is an input plus a text-labelled Clear button, and styles.css:276-295 carries no icon), with no AC, no Edge Case, no deferral note and no decision, while SearchIcon already ships at packages/icons/src/icons/SearchIcon.tsx so it was dropped rather than deferred; [pre-existing] MINOR AC2's verifier reaches into packages/react/src/components/Field/\_\_tests\_\_/behaviour.test.tsx, a file this story's Affects list does not name |
| US01M0GMAG | REJECT | anton-reis | claude-implementer | 2026-08-23 | 13131c031c9a | spec | [pre-existing] MAJOR AC3 asserts 'the hit area is at least 24x24px in compact' but its verifier at behaviour.test.tsx:314-330 asserts only Space and a label click, jsdom computes no layout (which AC5 in the same story states), and .clara-checkbox's SHAPE\_CONTRACT entry at check-component-css.mjs:149 requires only appearance, so deleting the min-width and min-height at styles.css:233-235 leaves every criterion in this story green; [pre-existing] MINOR the Edge Case 'a consumer passes their own ref, it is forwarded' has no criterion although ref forwarding is permanent public surface |
| US01M0GM9E | APPROVE | anton-reis | claude-implementer | 2026-08-23 | 05a585187749 | spec | [pre-existing] MINOR PRD F10's 'checked state is conveyed by shape and mark, not by colour alone' appears in this story's Edge Cases and Test Scenarios but in no AC, though the mechanism is held by .clara-switch's appearance entry in SHAPE\_CONTRACT at check-component-css.mjs:150 reached via AC3; [pre-existing] MINOR PRD F10's 'the full control including its label is a click target' likewise has no AC, though it is asserted inside AC1's own verifier block at behaviour.test.tsx:337-341 - Checkbox states both as criteria and Switch should match it |
| US01M0GMQT | REJECT | anton-reis | claude-implementer | 2026-08-23 | 2296ce6569a9 | spec | [pre-existing] MAJOR AC1's Then asserts an outcome the repo says it cannot observe - 'the group is one tab stop and arrows move and select per WAI-ARIA', stamped Verified yes - while the test at behaviour.test.tsx:1133-1158 asserts a mechanism only and says so, the record files it as a Playwright gap at RadioGroup/verification.md:65-70, and the Test Plan repeats it, so the caveat that Field AC6 and AC9 carry inline is missing from the one place reconcile reads; [pre-existing] MINOR PRD F10's label-click-target and not-colour-alone rows have no criterion here although Checkbox states both |
| US01M0GM0D | REJECT | anton-reis | claude-implementer | 2026-08-23 | 55806d280e31 | spec | [pre-existing] MAJOR the Edge Case 'a fieldset whose legend names the question' and the Test Scenario repeating it are false inside a Field, the primary case, where the legend is visually hidden and aria-labelledby outranks it (CheckboxGroup.tsx:53,66-78) - the D0071 defect surviving in prose after the code was corrected, and AC1's test carries the same stale title at behaviour.test.tsx:385-389; [pre-existing] MAJOR D0074 made CheckboxGroup the one control that composes requiredMarkerId into its own aria-labelledby because role=group cannot carry aria-required, and that mechanism (tested at behaviour.test.tsx:1079-1089) has no criterion in this story or any other; [pre-existing] MINOR AC1's Then also claims aria-describedby for the group error, which is AC5's job and is not asserted in AC1's block |
| US01M0GM3D | APPROVE | anton-reis | claude-implementer | 2026-08-23 | 88625e573f69 | spec | [new] AC13 - the required criterion added by this diff has no Test Plan row, and the row labelled AC13 carries AC14's mutant and title, so the table is off by one from AC13 onward |
| US01M0GMBM | REJECT | anton-reis | claude-implementer | 2026-08-23 | 5017b211f5a8 | spec | [new] AC2 - the readonly SHAPE\_CONTRACT entry this diff added is filtered out of every --component run, so deleting styles.css:227 leaves both AC2's and AC5's verifiers green at rc=0, and only the unscoped run no Input AC names goes red; [new] AC7 - the ref criterion added by this diff has no Test Plan row, and the row labelled AC7 carries AC8's mutant and title; [pre-existing] AC2 - its Test Plan row names a tab-order mutant unrelated to either clause of the criterion |
| US01M0GMF3 | REJECT | anton-reis | claude-implementer | 2026-08-23 | 6c4fb6e27560 | spec | [pre-existing] AC3 - 'Given a focused NumberInput ... the value steps and announces' contradicts D0077, because stepping and the spinbutton role are opt-in on min/max/step at NumberInput.tsx:76,99,128, so the criterion is satisfied by the removed defect and violated by the shipped component in the documented account-code case, while every test under it supplies a bound; [pre-existing] AC2 - 'figures are tabular' asserts a rendered property outside SHAPE\_CONTRACT, and deleting styles.css:190 leaves the criterion, the scoped and unscoped CSS gates and every tabular test green at rc=0 |
| US01M0GMMM | APPROVE | anton-reis | claude-implementer | 2026-08-23 | 6bcbc1f16ccf | spec | none |
| US01M0GM2K | APPROVE | anton-reis | claude-implementer | 2026-08-23 | 6418b0941b68 | spec | [pre-existing] no criterion asserts the resize handle exists, and AC2 catches its deletion only incidentally because the shape contract shares a script with the token rule; [new] scripts/check-component-css.mjs:160 claims resize is 'asserted by the Textarea story' when none of the five criteria assert it |
| US01M0GM2X | APPROVE | anton-reis | claude-implementer | 2026-08-23 | 43b11caa1977 | spec | none |
| US01M0GMAG | APPROVE | anton-reis | claude-implementer | 2026-08-23 | 1e2458455d88 | spec | [pre-existing] AC3 - the '24x24px in compact' clause is not covered by AC3's own verifier, since the declarations are proven by AC4's script and the value by density.test.ts:80 under check:geometry, neither of which AC3 names |
| US01M0GM9E | APPROVE | anton-reis | claude-implementer | 2026-08-23 | 33f78448b8b1 | spec | none |
| US01M0GMQT | APPROVE | anton-reis | claude-implementer | 2026-08-23 | 71be97536c84 | spec | none |
| US01M0GM0D | APPROVE | anton-reis | claude-implementer | 2026-08-23 | 8384af3e5fcb | spec | [pre-existing] AC1 - the Given is a bare CheckboxGroup but the Then asserts aria-describedby for group error, which exists only when there is Field wiring at CheckboxGroup.tsx:53, though AC5 covers that case correctly |
| US01M0GMBM | APPROVE | anton-reis | claude-implementer | 2026-08-23 | a80222b22c65 | spec | none |
| US01M0GMF3 | REJECT | anton-reis | claude-implementer | 2026-08-23 | 0b5e5a7a6e86 | spec | [pre-existing] AC2 - 'min, max and step are enforced' is false of the shipped control: min/max/step never reach the DOM (NumberInput.tsx:26,67), checkValidity() is true for a typed 500 against max=10, and clamp() runs only from stepBy/jumpTo, so the story's own Test Plan row and the docs keyboard table both scope clamping to stepping; [pre-existing] AC2 - the wording conceals a live ARIA defect, aria-valuenow=500 announced beside aria-valuemax=10; [new] AC3 - rewritten in 8cb019a but still stamped Verified: yes (2026-08-23), and its verifier 'NumberInput arrow keys step and clamp' selects three tests, none of which is the account-code test at behaviour.test.tsx:1103 that proves the clause the rewrite was made for |
| US01M0GMF3 | APPROVE | anton-reis | claude-implementer | 2026-08-23 | e715cbdd4fcf | spec | [new] coverage/major: own-disabled step suppression is pinned by nothing - mutating fieldDisabled(wiring, disabled) to fieldDisabled(wiring) in stepBy and jumpTo leaves all 846 tests green, the call arrived in 2e01ae8 and no fixture uses the own-prop composition; [new] comment/minor: NumberInput.tsx:137-138 describes a Field-deferral condition the code never implements (line 184 is unconditional), harmless since both resolve true, but the covering test passes with the spread deleted; [new] a11y/minor: an out-of-range value inside a VALID Field emits aria-invalid with no aria-errormessage, no aria-describedby and no error text - a plausible WCAG 2.2 SC 3.3.1 gap that axe structurally cannot see, mitigated by the announced valuemin and valuemax; [pre-existing] test-comment/minor: behaviour.test.tsx:165-169 still claims the control BLURS on wheel, contradicting D0062 and the toHaveFocus assertion directly below it (from 121d032); [pre-existing] wording/nit: AC6 says 'steps by ten' where the code and verification.md say ten TIMES step (from 121d032) |
| US01M0GM61 | REJECT | mira-calderon | claude-implementer | 2026-08-24 | 28fa4858c312 | test-plan | [new] AC3 - the DOM-order mechanism AC3's Then-clause rests on ("ClaraPortal appends each host to the body ... whichever overlay was opened last is on top") is asserted ONLY by describe "portals stack by open order" (packages/react/src/theme/\_\_tests\_\_/theming.test.tsx:164), which no acceptance criterion's Verify selects. AC3's verifier `vitest "the overlay stacking order"` runs 3 tests in packages/tokens/src/\_\_tests\_\_/layers.test.ts:77-93 that read token JSON and cannot see ClaraPortal at all. REPRODUCED - applied AC3's own Test Plan mutant (appendChild to prepend, ClaraPortal.tsx:29) and all four AC verifiers stayed green (AC1 2 passed, AC2 5 passed, AC3 3 passed, AC4 3 passed) while the orphan describe went red. The Test Plan row's "one test dies" and D0088's "prepending the host instead of appending it now fails a test" are true of `pnpm test` and FALSE of the AC gate. Same class as the prior pass's finding 1, at a different joint. Introduced by 98c75ed. FIX - point AC3's Verify at the portal describe, or add its assertions to a describe AC3 names; [new] AC3 - the model says OPEN order and the implementation gives MOUNT order. ClaraPortal.tsx:27-32 creates and appends the host in a []-dep effect regardless of whether it has children, so `<ClaraPortal>{open && <Menu/>}</ClaraPortal>` - the ordinary React idiom, and ClaraPortal is exported public API (packages/react/src/index.ts, etc/clara-react.api.md) - pins sibling order at MOUNT. REPRODUCED with a probe - two ClaraPortals in one tree left 3 hosts in document.body with NOTHING open, then opening B first and A second made A (opened LAST) the EARLIER sibling, so it paints UNDERNEATH and the model inverts silently. The effect itself predates this unit (e2f2d49) but the open-order model that depends on it does not, so the defect is new with 98c75ed. Nothing states, tests or enforces a mount-at-open contract, and thirteen overlays are about to inherit it. FIX - create the host lazily when there are children, or write the contract down and assert it; [new] AC2 - the z-index rule is defeated on the first two attempts. check-component-css.mjs:112 tests `decl.prop !== 'z-index'` case-sensitively while CSS property names are case-insensitive and postcss preserves case, so appending `.probe { position: fixed. Z-INDEX: 999999 }` to packages/react/src/styles.css PASSED. And line 114 accepts any value merely CONTAINING var(--clara-layer-*), so `z-index: calc(999999 + 0 * var(--clara-layer-overlay))` PASSED. Both reproduced, both real CSS a browser honours. Third hole - the guard walks only .css files (line 123), so inline `style={{ zIndex: 999999 }}` in TSX is entirely unchecked, and Radix-positioned overlays inline-style as a matter of course. Correctly caught - !important, @media, @supports, and a self-defined --clara-layer-* custom property. Introduced by 33e50a2; [new] AC2 - the z-index rule has no entry in scripts/prove-guards-fail.mjs. That harness kills 82 mutations, roughly sixteen of them for check-component-css (tier 1 read, raw literal on the selector line, named colour, focus ring, shape contract, conflicting redeclaration and so on) and none for z-index, which is the one rule D0087 added as the headline enforcement and D0088 carries forward. This repo's own convention is that a guard nobody has watched fail is not a guard, and a control here would have caught both defeats above; [new] AC2 and AC3 - the story still specifies the SUPERSEDED per-role scale it exists to replace. US-01M0GM61-portal-layer-scale-and-scoping-infrastructure.md:46 and :187 say "the eight layer names", :47 says the scale is layer.0 to layer.7, and :199 says "delete one of the eight names" - there are FIVE (base, raised, overlay, tooltip, toast) and tokens.public.lock.json correctly lists five. Worse, the Edge Cases table at :127-129 still teaches the REJECTED model - "popover sits ABOVE modal in the scale", "scrim is above dropdown", and "calc(var(--clara-layer-modal) + 1)" naming a token that no longer exists. That table is the specification the nine overlay stories inherit and it contradicts the decision this story implements; [new] AC3 - z-index on a position:static element is inert, and the guard requires the token without requiring the companion `position`. jsdom computes no layout so no test can see it, which is exactly the class SHAPE\_CONTRACT (check-component-css.mjs:166) exists for. A portalled surface that takes var(--clara-layer-overlay) and forgets `position` gets no stacking at all with every gate green; [new] AC2 - check-component-css.mjs:108-109 still cites "calc(var(--clara-layer-modal) + 1)" and "the scale leaves 100 between steps". Neither survives D0088 - there is no layer-modal, and the tier 1 steps are 0 / 10 / 1000 / 1400 / 1500; [new] AC2 - packages/tokens/src/\_\_tests\_\_/layers.test.ts:68 hardcodes compact.json, dark.json, light.json and silently continues on a missing file. light.json does not exist, so a third of that loop is a no-op today and any future theme file is unguarded by default. readdirSync over src/themes is the same length of code and cannot go stale. The override check itself WORKS - reproduced, adding layer.overlay to compact.json turns AC2 red; [new] AC4 - AC4's verifier owns a test that is not about its subject. "mounts into the document once there IS one, OUTSIDE the React root" (theming.test.tsx:150, added in fce30ed) sits inside describe "portal renders nothing on the server", so AC4 - "renders nothing on the server and does not read document" - goes red on a purely client-side portal mutation. REPRODUCED, replacing createPortal with a plain inline div fails AC4. The assertion is right and the fix is right, it belongs under AC1 or AC3; [pre-existing] PRIOR FINDINGS ALL FOUR CONFIRMED FIXED, by mutation rather than by reading. (1) layers.test.ts now reads src/primitive/base.json and src/semantic/geometry.json and resolves the reference itself, so editing the SOURCE with NO rebuild turns the criteria red - toast retargeted to layer.1 made AC3 red, and overlay given the raw value "1000" made AC2 red twice with "must reference a tier 1 step". (2) theme and density overrides are asserted directly - layer.overlay added to compact.json made AC2 red with "compact.json overrides the layer scale". (3) the vacuous document.body.contains assertion is replaced by expect(container.contains(el)).toBe(false), and a plain inline div now fails it. (4) the false "would not have caught" claim is reworded in both the Test Plan and layers.test.ts:15-18. Baseline green at f5aa67d - pnpm build, pnpm check (82 mutations killed) and pnpm test (32 files, 869 tests) all rc=0 on a clean tree |
| US01M0GM61 | REJECT | anton-reis | claude-implementer | 2026-08-24 | f48f24a55ff6 | spec | [regression] BLOCKING AC3 verifier cannot see the redesign - 98c75ed added the DOM-order tests at theming.test.tsx:163 and left AC3 Verify at 'the overlay stacking order', which selects only the token comparisons in layers.test.ts:79-95, so the story's own AC3 mutant (appendChild to prepend, ClaraPortal.tsx:29) leaves AC3 green at 3 passed and reconcile --verify would stamp it verified; [new] BLOCKING AC3/D0088 precondition unstated and unenforced - ClaraPortal.tsx:26-31 appends the host at MOUNT not at open, and a probe of a mounted-but-closed pair inverts the order silently, which is what Radix forceMount, Presence exit animations and a mounted Toast viewport all produce; [regression] BLOCKING story doc still instructs the deleted per-role model - line 127 popover above modal, line 128 scrim above dropdown, line 129 steps 100 apart plus calc(var(--clara-layer-modal)+1) over a token the build no longer emits, lines 46/187/199 still say eight names, line 48 still cites superseded D0087, and check-component-css.mjs:109 cites --clara-layer-modal in the same docblock; [new] MAJOR the z-index rule is binding but unprotected - no AC in this story runs check-component-css.mjs and prove-guards-fail.mjs proves 82 mutations without this one, so neutering zIndexProblems to return empty left check:component-css and check:prove-guards both PASS; [new] MAJOR the z-index rule reads .css only, so an inline style zIndex 999999 in any of the thirteen React overlays is invisible to it; [new] MAJOR AC2 guard is a seven-word denylist where the model needs an allowlist - layers.test.ts:47 lets layer.sheet re-introduce the constant with all 8 tests green, caught downstream only by the check-token-output lock diff; [pre-existing] MINOR Affects resolves all five paths but omits packages/react/src/theme/\_\_tests\_\_/theming.test.tsx where the AC1, AC3 and AC4 tests live; [new] MINOR tokens.public.lock.json carries the five names at 58 keys but its //why narration recording 31 to 34 to 53 was not extended for 53 to 58; [new] MINOR no consumer documentation of the layer scale or the DOM-order contract, while ClaraPortal is a permanent public export at index.ts:65 and an application must now keep its own chrome below a single --clara-layer-overlay of 1000; [pre-existing] MINOR tooltip 1400 below toast 1500 puts a Tooltip on a Toast action button under the toast, true of the old scale too and flagged for the Toast and Tooltip stories |
