# Plan-Review Verdicts

> Append-only. The independent non-author plan reviewer's verdict per unit -
> the pre-implementation AC-vs-spec check (US0090). Latest row per unit wins.
> Reviewer must differ from the plan author - a self-review never clears the gate.
> Kind names WHICH pre-code artefact was judged; a gate asks for its own kind.

| Unit | Verdict | Reviewer | Author | Date | Brief | Kind | Issues |
| --- | --- | --- | --- | --- | --- | --- | --- |
| US01M0GM0R | APPROVE | Anton Reis | sdlc-studio agent | 2026-08-23 | cdbfb0698abe | spec | [new] chunk placement keyed on directory name, so a co-located client component shipped undirectived - FIXED via TypeScript AST parsing (D0051); [new] a module shared by both sides put the server chunk behind the client boundary - FIXED with a third undirectived chunk plus transitive reachability; [regression] the round-1 repair reported both Criticals fixed when a real build showed neither was - FIXED, and a third oracle reading emitted bytes means no single reader is a point of failure; [pre-existing] the skill's retro parser cannot read this project's ULID ids. Findings sourced from two adversarial reviews by fresh non-author contexts, recorded in reviews/RV-2026-08-22-run-01m0mfxj.md |
| US01M0MQYN | APPROVE | Anton Reis | sdlc-studio agent | 2026-08-23 | 1f2815ecd5c3 | spec | [new] chunk placement keyed on directory name, so a co-located client component shipped undirectived - FIXED via TypeScript AST parsing (D0051); [new] a module shared by both sides put the server chunk behind the client boundary - FIXED with a third undirectived chunk plus transitive reachability; [regression] the round-1 repair reported both Criticals fixed when a real build showed neither was - FIXED, and a third oracle reading emitted bytes means no single reader is a point of failure; [pre-existing] the skill's retro parser cannot read this project's ULID ids. Findings sourced from two adversarial reviews by fresh non-author contexts, recorded in reviews/RV-2026-08-22-run-01m0mfxj.md |
| US01M0GMDV | APPROVE | Mira Calderon | sdlc-studio agent | 2026-08-23 | 287b488a1fec | spec | [pre-existing] hydration is verified by a clean production build rather than a browser load - recorded as an honest limit in the story, not implied; [new] the peer-resolution check compared raw paths and tripped on the macOS /var -> /private/var symlink - FIXED by realpath'ing both sides |
| US01M0NJZN | APPROVE | Anton Reis | sdlc-studio agent | 2026-08-23 | 29e08250bc45 | spec | [new] the older chunk-plan tests asserted the single-chunk name and had to move with the behaviour - updated rather than deleted; [new] prove-guards mutations referenced dist/clara-client.js which no longer exists - repointed, and a new mutation pins the per-component property itself; [pre-existing] the 5 kB per-component figure is a starting number, not a measured one - recorded as an open limit |
| US01M0GMAE | APPROVE | Anton Reis | sdlc-studio agent | 2026-08-23 | beb0d219605b | spec | [new] three guards defined a tier by a hardcoded name prefix (semantic-) and would have gone silently vacuous at the rename - FIXED by consulting the tier manifest; [new] the contrast threshold came from a regex over the token name, which demanded 4.5:1 of fg-disabled where PRD Section 7 sets 3:1 - FIXED by deriving the threshold from the declared role; [new] PRD Section 7 enumerated the same colour pair twice under two roles, making the row count ambiguous - merged to one row at the strictest threshold, giving the 48 the PRD requires; [pre-existing] the two-part focus indicator was specified but never resolved - resolved and measured as D0054 |
| US01M0GM66 | APPROVE | Idris Vale | sdlc-studio agent | 2026-08-23 | ff5d892bfd39 | spec | [new] three guards defined a tier by a hardcoded name prefix (semantic-) and would have gone silently vacuous at the rename - FIXED by consulting the tier manifest; [new] the contrast threshold came from a regex over the token name, which demanded 4.5:1 of fg-disabled where PRD Section 7 sets 3:1 - FIXED by deriving the threshold from the declared role; [new] PRD Section 7 enumerated the same colour pair twice under two roles, making the row count ambiguous - merged to one row at the strictest threshold, giving the 48 the PRD requires; [pre-existing] the two-part focus indicator was specified but never resolved - resolved and measured as D0054 |
| US01M0GMMX | APPROVE | Anton Reis | sdlc-studio agent | 2026-08-23 | 465de733aab4 | spec | [new] three guards defined a tier by a hardcoded name prefix (semantic-) and would have gone silently vacuous at the rename - FIXED by consulting the tier manifest; [new] the contrast threshold came from a regex over the token name, which demanded 4.5:1 of fg-disabled where PRD Section 7 sets 3:1 - FIXED by deriving the threshold from the declared role; [new] PRD Section 7 enumerated the same colour pair twice under two roles, making the row count ambiguous - merged to one row at the strictest threshold, giving the 48 the PRD requires; [pre-existing] the two-part focus indicator was specified but never resolved - resolved and measured as D0054 |
| US01M0GME0 | APPROVE | Anton Reis | sdlc-studio agent | 2026-08-23 | 21e9ca1f6b97 | spec | [new] the semantic layer was colour-only, so the first real stylesheet reached for a tier 1 primitive - FIXED by adding semantic geometry (D0056); [new] client-only hooks fell through to the undirectived shared chunk, which would crash any server render importing them - FIXED with clara-client-shared (D0057), caught by the emitted-bytes oracle; [new] the component-CSS literal rule matched per line and skipped declarations written on the selector line - FIXED with PostCSS; [new] the size budget measured React's weight and called it Clara's - FIXED by ignoring peers; [pre-existing] the surface contract could not be proven to fire because api-extractor's drift check rejects an edited report first - FIXED by extracting it to a pure, unit-tested module |
| US01M0GMC6 | APPROVE | Idris Vale | sdlc-studio agent | 2026-08-23 | b6bc798c9576 | spec | [new] the semantic layer was colour-only, so the first real stylesheet reached for a tier 1 primitive - FIXED by adding semantic geometry (D0056); [new] client-only hooks fell through to the undirectived shared chunk, which would crash any server render importing them - FIXED with clara-client-shared (D0057), caught by the emitted-bytes oracle; [new] the component-CSS literal rule matched per line and skipped declarations written on the selector line - FIXED with PostCSS; [new] the size budget measured React's weight and called it Clara's - FIXED by ignoring peers; [pre-existing] the surface contract could not be proven to fire because api-extractor's drift check rejects an edited report first - FIXED by extracting it to a pure, unit-tested module |
| US01M0GMT2 | APPROVE | Idris Vale | sdlc-studio agent | 2026-08-23 | 5daeeaaf23bc | spec | [new] the semantic layer was colour-only, so the first real stylesheet reached for a tier 1 primitive - FIXED by adding semantic geometry (D0056); [new] client-only hooks fell through to the undirectived shared chunk, which would crash any server render importing them - FIXED with clara-client-shared (D0057), caught by the emitted-bytes oracle; [new] the component-CSS literal rule matched per line and skipped declarations written on the selector line - FIXED with PostCSS; [new] the size budget measured React's weight and called it Clara's - FIXED by ignoring peers; [pre-existing] the surface contract could not be proven to fire because api-extractor's drift check rejects an edited report first - FIXED by extracting it to a pure, unit-tested module |
| US01M0GM5M | APPROVE | Anton Reis | sdlc-studio agent | 2026-08-23 | deea398a6fc3 | spec | [new] the semantic layer was colour-only, so the first real stylesheet reached for a tier 1 primitive - FIXED by adding semantic geometry (D0056); [new] client-only hooks fell through to the undirectived shared chunk, which would crash any server render importing them - FIXED with clara-client-shared (D0057), caught by the emitted-bytes oracle; [new] the component-CSS literal rule matched per line and skipped declarations written on the selector line - FIXED with PostCSS; [new] the size budget measured React's weight and called it Clara's - FIXED by ignoring peers; [pre-existing] the surface contract could not be proven to fire because api-extractor's drift check rejects an edited report first - FIXED by extracting it to a pure, unit-tested module |
| US01M0GMN0 | APPROVE | Idris Vale | sdlc-studio agent | 2026-08-23 | 6879bd67185e | spec | [pre-existing] three deliverables were recorded Provisional that the semantic layer has since made real - border weight, type scale and the pairing table - FIXED by updating the record to match what shipped; [pre-existing] elevation and motion remain Provisional under D0035 clause 1, consumed by components rather than by the token layer, so F01 is where they stop being guesses |
