# Review findings - US-01M0GM9N and US-01M0GMFB

> **Reviewer:** anton-reis (engineering seat, separate instance) · **Author:** sdlc-studio
> **Rounds:** 2 · **Verdict both rounds:** REJECT
>
> Written because round 2 reported the finding detail from round 1 was **unauditable**: it lived
> only in a transcript, and `critic-verdicts.md` carries `-` in its Issues column. A re-reviewer
> cannot check a finding nobody wrote down. This file is that record.

## Mutation vocabulary

Prior rounds cite mutation IDs (M-series, A/B/C/D-series). Those numbers were reviewer-local and
are NOT reproducible from this file alone - a later reviewer cannot re-run "M14" by name. The
durable record is `scripts/prove-guards-fail.mjs`, which now proves 11 named mutations killable on
a staged copy and is the thing to extend rather than a numbering scheme in prose.

## Round 1 - 23 mutations, 8 survived

| ID | Severity | Finding | Round-2 status |
| --- | --- | --- | --- |
| F1 | Critical | React can be bundled into the tarball; all 5 guards, publint, attw and 9 ACs green | Partially fixed -> **N1** |
| F2 | High | AC3 "a wildcard fails CI" ran the guard clean and asserted exit 0 - the opposite proposition | Fixed; mechanism defective -> **N5** |
| F3 | High | `tokens.pairings.json` reaches the tarball on a one-word change (D0029) | Fixed |
| F4 | High | Dark-theme scoping fix had no regression pin | Fixed for the literal case -> **N2** |
| F5 | High | Packaging ACs covered `packages/react` only; tokens' attw invocation existed nowhere | Fixed (D0031) |
| F6 | High | Every custom property missing `--clara-` (PRD:244, D0001); source layout and tier 2 families diverge from TRD S6, undisclosed | Prefix + layout fixed; families disclosed as CR-01M0J0Z6 -> **N8** |
| F7 | Medium | Tier-tier violations and tier 1 leakage into the public manifest unguarded | Fixed for the filter case -> **N3** |
| F8 | Medium | `pnpm typecheck` fails TS18003 from a clean checkout | Fixed |
| F9 | Medium | AC1 stamped Verified while a quarter of its Then clause was admitted-unproven | Partially fixed -> **N7** |
| F10 | Low | `finalize-dual` printed PASS for a non-dual build | Fixed |
| F11 | Low | Tarballs shipped sourcemaps pointing at unshipped `src` | Fixed |
| F12 | Low | Two edge-case rows claimed coverage for scenarios that cannot occur | Fixed |
| F13 | Low | TRD gate 10 credits publint with wildcard detection it does not do | Fixed |
| F14 | Low | `vite.config.ts` invisible to `pnpm typecheck` | Fixed |
| F15 | Low | AC5/BG-01M0HTRM named FalseCJS; the real diagnostic is FalseESM | Fixed |

## Round 2 - the repair pass

All 8 previously-surviving mutations confirmed KILLED (M2, M7, M9, M12, M14, M15, M19, M23), no
regressions. New findings, all in code written during the repair:

| ID | Severity | Finding |
| --- | --- | --- |
| N1 | **Critical** | `check-bundled-peers` matches React 18 strings only. React 19 minified with `legalComments: 'none'` inlines fully and every gate reports green. The peer range is `^18.2.0 \|\| ^19.0.0`, so half the supported range evades. |
| N2 | High | The dark-selector rule DENIES `:root` instead of REQUIRING `[data-clara-theme="dark"]`. `:root:root` is specificity (0,2,0), beats `tokens.css`, forces dark permanently, and passes. |
| N3 | High | The guard derives tier 2 from a DIRECTORY path; the build derives it from `token.path[0] === 'semantic'`. A `semantic` group declared in `src/primitive/` is tier 2 to the build and invisible to the guard - a raw literal reached `tokens.public.json` with all guards green. |
| N4 | Medium | Nothing asserts the emitted tier 2 set is complete or symmetric. A token added to dark only, and a token written in DTCG `$value` form that silently vanished, both passed. |
| N5 | High | `prove-guards-fail` writes to REAL tracked manifests (its docblock claims a temp copy). SIGINT leaves `"./*": "./dist/*"` on disk; concurrent runs give false failures; the reviewer's own `git checkout` recovery destroyed uncommitted work. |
| N6 | Low | `check-bundled-peers` passes vacuously when a build emits no JavaScript. |
| N7 | Medium | AC1 claims "exactly one stylesheet"; its verifier counts none. A second `dist/extra.css` shipped with the AC green. |
| N8 | Low | `packages/tokens/src/README.md` still tables the pre-move filenames. |
| N9 | Low | D0001's "no exceptions" is enforced only inside `packages/tokens`. |

## Round 2 repairs (author, same day)

| ID | Repair | Proven by |
| --- | --- | --- |
| N1 | `check-bundled-peers` no longer matches strings. It asserts **import-specifier survival**: if the source imports a peer, the emitted bundle must still carry a bare specifier for it. A bundler either externalises (specifier survives) or inlines (specifier disappears) - mutually exclusive and minification-proof. | React 19 + `minify: 'esbuild'` + `legalComments: 'none'`: 43,143 bytes, zero React-18 markers, zero `react.production` string, guard exits 1. Same source with `external` restored: 89 bytes, guard exits 0. |
| N2 | Requires the selector to BE `[data-clara-theme="dark"]` rather than denying `:root`. | `:root:root` now reported by name. |
| N3 | The build emits `build/tier-manifest.json` from its own `isTier2` predicate; the guard reads that instead of re-deriving tier from a directory. | A `semantic` group planted in `src/primitive/` with a raw literal is now caught. |
| N4 | Asserts the emitted tier 2 set is complete (every manifest token in `tokens.css`) and symmetric (nothing dark-only). DTCG `$value` refused before any file is written. | Dark-only token caught; `$value` now fails with one actionable line and an empty dist instead of a stack trace over a partial one. |
| N5 | `prove-guards-fail` stages a temp copy; the working tree is only ever read. Also distinguishes a rejecting guard from a crashing one. | 12 SIGINT interrupts and 8 concurrent runs: tracked files byte-identical, zero wildcards left on disk, zero false restore failures. |
| N6 | A build emitting no JavaScript is now a failure, not a vacuous pass. | Reproduced and caught during the rebuild. |
| N7 | New `check-stylesheets` guard: exactly one stylesheet per package, each reachable through a closed exports subpath. AC1's verifier now runs it. | `dist/extra.css` caught. |
| N8 | README tables the post-move filenames. | Every tabled path verified to exist. |
| N9 | `--clara-` enforced repo-wide, not just in `packages/tokens`. | `--brand-accent` shipped from `packages/react` now caught. |

`pnpm check` is 9 guards. Both stories re-verified 5/5 and 6/6; US-01M0GMPJ 3/3 unchanged.

## Round 3 - REJECT. The peer guard failed a third time.

| ID | Sev | Finding | Repair | Proven by |
| --- | --- | --- | --- | --- |
| R1 | **Critical** | `sourceImports()` gated the check on the source containing the literal `react`. `tsconfig.base.json` sets `jsx: react-jsx`, so no component ever names React - **every peer was skipped on the unmodified tree** while the banner claimed 6 files scanned. | Oracle replaced. `scripts/lib/bundle-record.mjs` is a Rollup plugin recording `chunk.modules` (what was inlined) and `chunk.imports` (what stayed external) during `generateBundle`. The guard reads that. | Automatic-JSX component, zero literal `react` in source, `external` dropped: 77 kB bundled, guard exits 1. |
| R2 | **Critical** | Specifier survival was decided by regexing output text, so a comment counted as an import. A `@license` block containing `from "react"` passed with 56 kB of React inlined. | Same replacement - a module record is not text. | Decoy `from "react"` present in output, guard still exits 1. |
| R3 | Medium | `react` external while `react/jsx-runtime` was inlined passed. | Closed for free: the owning package of a `jsx-runtime` module IS `react`. | `external: [/^react$/]`, jsx-runtime inlined, guard exits 1. |
| R4 | High | `tier-manifest.json` is written by the predicate the guard checks against, so narrowing `isTier2` silently deleted 4 tokens from `tokens.public.json` with every gate green. Removing a published token is the one change immutability cannot undo. | `packages/tokens/tokens.public.lock.json` - a committed snapshot the build does not write. CI diffs it and fails on any add OR remove. | Narrowed `isTier2`: 4 × `was REMOVED from tokens.public.json`. |
| R5 | Medium | `pathToJs` capitalized per segment; Style Dictionary camel-cases the whole path. Any hyphenated segment made the tier-1-leak check a silent no-op. | Whole-path camel-case; an unmatched key is now a reported problem, not a skip. | `color.neutral-alt.500` leak now caught. |
| R6 | Medium | All four dark-theme checks were `^`-anchored; a minified single-line `dark.css` no-opped three of them. | Comments stripped, selectors read between `}` boundaries, property scans unanchored. | Minified `:root{}` override caught. |
| R7 | Low | AC1 claimed "exactly one stylesheet"; nothing counted them. | `check-stylesheets` counts, with tokens as the documented two-sheet exception. | Second sheet + matching export entry: caught. |
| R8 | Low | The prover covered 4 of 9 guards - and both Criticals lived in one it did not cover. | Extended to the output guards via a staged copy of real `dist`/`build` artifacts. **11 mutations** now proven killable. | `prove-guards` banner lists all 11. |
| R9 | Low | `finally` does not run on a signal; each interrupt leaked a staged temp dir. | Signal handlers sweep the staged set. | 10 interrupts, 0 leaked. |
| R10 | Low | `build` never cleaned `dist`, so stale artifacts survived into `npm pack`. | `clean` added to every build chain. | 3/3 rebuild clean. |
| R11 | Low | A note claimed AC5 covered all three packages; its `Verify:` ran react only. | Note corrected to describe what the scripts actually iterate. | - |
| R12 | Low | This file cited M-numbers it never defined. | Mutation vocabulary section added; `prove-guards-fail.mjs` named as the durable record. | - |

**The lesson across three rounds:** every version of the peer guard that INFERRED externalization
was defeated - from marker strings, from output text, from source text. The version that reads what
the bundler recorded is the first that holds. Observation beats inference, and a guard with no
fail-proof is where the Criticals accumulated.

## Round 4 - US-01M0GMFB **APPROVED**. US-01M0GM9N and US-01M0GM3X REJECTed.

First sign-off: **US-01M0GMFB is Done**, re-tested by the reviewer against a non-empty export
surface (attw green on node16-from-CJS/ESM/bundler, publint clean, `index.d.cts` resolving, exports
closed, licence packed and byte-matching).

### The peer guard, defeated a fourth time

| ID | Sev | Finding | Repair |
| --- | --- | --- | --- |
| A1 | **Critical** | The guard chose its mode by testing for `vite.config.ts`. Vite also accepts `.mts/.cts/.js/.mjs/.cjs` - renaming the file downgraded it to "does an entry exist" and 56 KB of React shipped **with the record correctly saying so, unread**. | No mode selection at all. Every published package emits a record (Vite via the plugin, tsc via `finalize-dual`); a missing record is a failure, not a branch. |
| A2 | High | `owningPackage()` was a DENYLIST keyed on finding `/node_modules/` in the path. pnpm workspace links are realpathed, so an inlined `@luzentialabs/clara-tokens` had no such segment and was invisible. | Inverted to an **allowlist**: every inlined module must be a package-relative path under that package's own `src/`. Anything else is a finding whether or not the guard recognises it. |
| A3 | Medium | `chunks` never reset - `--watch` accumulated 2 -> 4 -> 6 across rebuilds. | Reset in `buildStart`. |

**The pattern across four rounds.** Every version that INFERRED was defeated: from marker strings,
from output text, from source text, and finally from a filename. The record was right in round 4 -
the guard just never read it. Observation only helps if nothing upstream decides whether to observe.

### Test harness, first review

| ID | Sev | Finding | Repair |
| --- | --- | --- | --- |
| B1 | High | The coverage prover passed thresholds on the command line, so it never read the shipped config. Deleting `thresholds` from `vitest.config.ts` still gave PASS. | No threshold flags; the shipped config is the thing under test. |
| B2 | High | It asserted only a non-zero exit - one unrelated failing test read as "the coverage gate fired". **Fifth instance of the F2 pattern.** | Asserts vitest's threshold DIAGNOSTIC, and refuses to conclude anything when the suite has failing tests. |
| B3 | Medium | Fixed shared fixture path: 3 concurrent runs gave a PASS, a crash, and a false FAIL. | Namespaced per run via `mkdtemp`. |
| B4 | High | `check-mutation-config` claimed to prove Stryker accepts the config by running `--help`, which never reads it. **And `pnpm test:mutation` has never run at all.** | Explicit `plugins` entry (pnpm's layout defeats auto-discovery); guard now asserts it and states plainly that the gate does NOT execute. Filed as **BG-01M0J70K**. |
| B5 | Medium | The axe severity filter excludes 29 of 105 rules, 8 WCAG-tagged, including a Level A criterion. | Documented in `test/axe.ts` and opened `sdlc-studio/reviews/a11y-gap-register.md`. |
| B6 | High | A live `TS2428` in `test/axe.ts` that **nothing typechecked** - `pnpm typecheck` covered only per-package `src` and `vite.config.ts`. F14 reproduced across the whole new harness. | Fixed the type parameter; added a root `tsconfig.check.json` covering `test/`, `e2e/`, and both root configs. |
| B7 | Medium | A co-located component test - the layout TSD:419 mandates - broke typecheck three ways. US-01M0GM69 would have hit it on day one. | Package check configs reach the shared harness; build configs exclude test files so they never reach `dist`. Verified: typechecks, runs, absent from tarballs. |
| B8 | Medium | No CSS budget, though TRD:480 and PRD:1090 both require one. | `styles.css <= 15 kB gzipped` added. |
| B10 | Medium | AC1 claims coverage is reported; its verifier `pnpm test` printed "coverage" zero times. `check:coverage-gate` was in no gate. | Verifier is `pnpm test:coverage`; the prover is wired into `pnpm check`. |
| B11 | Low | Points header/notes disagreed (8 vs 5); `Affects` named `./size-limit.json`. | Both corrected. |

### What the reviewer's throwaway component settled

- **CR-01M0HWDQ's core question is answered: the CSS Modules pipeline works.** A real
  `Button.module.css` compiled into `dist/styles.css` as hashed classes, merged with the seed.
- **Coverage stops being vacuous the instant real code exists** - `0% (0/8)` untested, `75%` partial,
  gate firing both times. The config was always sound; only its prover was broken.
- **Not caught, by anything:** a tier 1 token and a raw literal in component CSS shipped green.
  US-01M0GME0 owns that gate, so it is scheduled - but AGENTS.md's "a tier 1 reference or a raw
  literal fails CI" is **currently untrue** and should say so until that story lands.

`pnpm check` is 11 guards.

## Round 5 - REJECT x2. The A1 fix RELOCATED the defect instead of removing it.

| ID | Sev | Finding | Repair |
| --- | --- | --- | --- |
| C1 | **Critical** | The round-4 fix moved the `vite.config.ts` filename test out of the guard and into `finalize-dual.mjs` - where it **FABRICATED** a record claiming a hardcoded `src/generated/index.ts` it never checked existed. Rename the config, drop the plugin: a 77 KB React-bearing bundle with a synthetic clean record, all 11 guards green. The docblock two lines above read "No package may opt out of being checked by how one of its files is named." | Fabrication deleted. `scripts/lib/tsc-bundle-record.mjs` is invoked **explicitly** by the tokens build, derives the tsc mapping, and **verifies each source exists** - the step the fabricated version skipped. |
| C2 | High | Nothing bound the record to the `dist/` it described. A direct `npx vite build` with no clean left an honest-but-stale record over React-bearing output. | Per-chunk **sha256**, written by the plugin, verified by the guard against every emitted JS file. Closes C1 and C2 with one mechanism: a fabricated record must name real bytes, a stale one no longer matches. |
| C3 | Low | PASS banners reported counts, so a package silently skipped was invisible. | Banner names the package set: `3 package(s) [icons, react, tokens]`. |
| H1 | Medium | The coverage prover overrode the shipped `coverage.include`, so a scope pointing at nothing passed while the real gate went vacuous - B1's own finding one level in. | Asserts the shipped include resolves, and adds the fixture to that scope rather than replacing it. (The first attempt matched `test.include` instead of `coverage.include` and did not fire; caught by testing it.) |
| H2 | High | **`since` is not a Stryker option.** The schema has no such key and Stryker warns "Unknown stryker config option". D0015's changed-file scope was never in force, and AC3 asserted it. | `since` removed, AC3 corrected, recorded as **D0034**. |
| H3 | High | `testDir: './e2e'` could not see a co-located `<Name>.keyboard.spec.ts` - the path **TSD:419 mandates**. An author would write a focus-identity spec, see green, and it never ran. US-01M0GM69 hits this on day one. | `testMatch` reaches `packages/*/src/**/*.spec.ts`. Verified: a co-located spec now executes. |
| B4 | - | **The reviewer FIXED BG-01M0J70K.** `vitest.related` defaults to true, so Stryker ran `vitest --related <mutated>` and found nothing. The runner printed the answer in its own warning. **None of the five causes the bug listed as "ruled out" was responsible** - the diagnosis was thorough and beside the point. | `related: false`. Gate now runs; a weak test drives the score to **23.53** against the 70 break threshold. Guard runs a real `--dryRunOnly` instead of asserting a bug file exists. |

**Confirmed clean by the reviewer:** the allowlist itself survived every direct attack (M4-M7,
including React symlinked into `src/` and defeated by realpath); the a11y gap register re-censused
exactly (29/105, 8 WCAG-tagged); `tokens.public.lock.json` fires on add and remove; the TSD:419
component layout typechecks, runs, and stays out of tarballs.

**The pattern, five rounds on.** Each round's fix was correct about the thing it fixed and moved
the weakness one layer out: strings -> output text -> source text -> filename -> *the thing that
writes the record*. What finally closes it is not a better oracle but making "not checked"
unrepresentable - a record must name real bytes or it is a failure.

`pnpm check` is 11 guards; `prove-guards-fail.mjs` proves **13** named mutations killable, now
including a fabricated and a stale record - the space between "missing" and "honest" where both
Criticals lived.

## Round 6 - REJECT x3. Two Criticals, one of them shipping.

| ID | Sev | Finding | Repair |
| --- | --- | --- | --- |
| X2 | **Critical** | **A failing contrast pairing was in the built output.** The gate read the light pairing table only; `dist/themes/dark.css` shipped `text-on-accent` at **3.23** and **3.35** against a 4.5 floor. Falsified D0035 clause 2, PRD F02, and this project's own written claim that nothing ships failing. | The dark instance now emits its own pairing table and the gate measures **both themes**. Root cause fixed too: D0036 made `fg-on-emphasis` per-INTENT; dark proves it must also be per-THEME - at accent.500 the readable foreground is near-black (5.15), not white (3.23). |
| X1 | **Critical** | **The peer guard defeated a sixth time.** `tsc-bundle-record.mjs` DERIVED its `inlined` list instead of observing it; one line appended to a build script shipped 55,955 bytes of React with a hash-correct record naming one source file. The guard's own diagnostic pointed developers at the hole. | The asserting writer is **deleted**. One record writer remains and it is a Rollup plugin. `clara-tokens` observes its graph via a Rollup pass bound to the real tsc artifacts' hashes - both halves observed, neither asserted. |
| X3 | High | `minRatio` was authored in the file being checked; downgrading a text pairing to 3:1 silently removed WCAG 1.4.3. | Thresholds moved into the committed required file. |
| X4 | High | The waiver `count` was a hand-edited integer; growing it to 99 let 11 required rows vanish while the banner reported progress. | `highWaterMark` caps it; growing the waiver is now a failure. |
| X5 | High | 27 rows transcribed against 48 the reviewer counted from PRD Section 7. | Acknowledged; the full transcription lands with US-01M0GMAE, when the families exist to express it. |
| X6 | High | `success.600` carries white at **4.17** - worse than the `info.600` case that WAS recorded, and missed. | Recorded in the measurement table so US-01M0GMAE does not mirror accent/danger naively. |
| X7 | High | **H1 was never fixed.** The check reduced to `existsSync('packages')`. Two further attempts were also wrong. | Now distinguishes **zero files** (broken config, fail) from **files with zero statements** (vacuous but valid, reported) - conflating them was why three attempts failed. |
| X8 | High | Mutation score `NaN`, and `NaN >= 70` passes. The gate was unfalsifiable, not merely uninformative. | A run finding zero mutable files is now a failure; the banner reports the real count. |
| X9 | Medium | The banner claimed "generated source excluded" and "vitest.related=false" while asserting **neither** - the assertion had been lost in an edit. | Both restored and proven to fire. |
| X11 | Medium | **`oklch.mjs` was dead code** - zero importers, so "every colour generated in OKLCH" was unreproducible. `encode()` clipped silently, and **25 of 66 steps were out of gamut** - a third of the palette was not the specified colour. | Generator wired (`generate-ramps.mjs`), chroma capped to the measured in-gamut ceiling (**0 clipped**), and `check-token-output` now fails if the committed ramps drift from the generator. |
| X12/X19 | Medium/Low | The focus table mislabelled its dark row; D0036 cited a colour not in the shipped ramp. | Both corrected, with the exhaustive 66-token sweep recorded. |
| X14 | Medium | The prover's staged copy omitted `src/`, so `check-token-output`'s raw-literal section checked **zero files on every prover run**. | `src/` and the generator are staged. |
| X18 | Low | The guard layer - where all six Criticals have lived - has zero tests, zero coverage, zero mutation. | Filed as **CR-01M0MBGN** (High, size M) rather than half-done. |

**What the reviewer confirmed sound:** `wcag.mjs` is correct, re-derived from the spec and matched
to published references to 4dp. The OKLab matrices are Ottosson's, byte-for-byte. **The two-part
focus indicator holds against all 66 tier 1 tokens in both themes**, tightest margin 4.17:1. And the
hard case is *stronger* than claimed: no colour at all carries both white and near-black at 4.5:1,
not merely no amber.

**The pattern, six rounds on.** Round 5 said the fix was making "not checked" unrepresentable. It
half-landed: a record had to name real bytes, but could still describe them falsely. A hash proves
provenance of bytes; it cannot prove completeness of a list. What closes it is having exactly one
writer that can only report what a bundler did.
