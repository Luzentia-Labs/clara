{
  "//": "COMMITTED SNAPSHOT of the tier 2 public API surface (PRD F01). Not written by the build.",
  "//why": "tier-manifest.json is emitted by the same predicate the guard checks against, so narrowing isTier2 deleted four public tokens with every gate green (review R4). This file is the independent witness: CI diffs it and fails on any add OR remove. Update it deliberately, in the same commit as a recorded decision.",
  "keys": [
    "SemanticActionDanger",
    "SemanticActionPrimary",
    "SemanticActionPrimaryHover",
    "SemanticBorderDefault",
    "SemanticFocusOffset",
    "SemanticFocusRing",
    "SemanticSpacingLg",
    "SemanticSpacingMd",
    "SemanticSpacingSm",
    "SemanticSpacingXs",
    "SemanticSurfaceDefault",
    "SemanticSurfaceSubtle",
    "SemanticTextDefault",
    "SemanticTextMuted",
    "SemanticTextOnAccent"
  ],
  "//added-2026-08-22": "SemanticFocusRing and SemanticFocusOffset added under D0036/F00. PRD:251 requires focus ring and offset as TWO SEPARATE tokens, per F07: the offset gap renders the surface underneath, so a single ring colour cannot survive every emphasis surface. Measured across all six enumerated surfaces - at least one part carries >=3:1 on every one."
}
