/**
 * Public entry for @luzentialabs/clara-icons.
 *
 * Intentionally empty. This story (US-01M0GM9N) builds the PIPELINE; the icon set and the `Icon`
 * base component are US-01M0GMYZ. Nothing is exported yet on purpose: every named export here
 * becomes permanent public API the moment the package is first published, so the cheapest thing
 * to get right is to export nothing until the story that owns the surface decides it.
 */
export {}
